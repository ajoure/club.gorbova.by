# PLAN-ONLY ревизия PR #416

Head SHA: `410fddf33cddfb8ad157d66d0becf4de7bc615b5`
Merge SHA: `1b051fecbc36eb77f55bc04c27c7df3d23fe9688`
База: merged main `1e93155c584618edb96e355094520efcd20265d2` (рабочее дерево синхронно с этой базой, проверено локально)
Режим: read-only. Ничего не изменено, не задеплоено, не отправлено.

## Вердикт: PLAN-ONLY PASS

## По требуемым пунктам

1. Авторизация и изоляция контактов — PASS. `get_active` объявлен как ветка внутри единственного канонического writer-а `admin-create-public-link`, то есть после `requirePaymentsEdit`; контекст грузится сервером по `order_id`, выборка ограничена пользователем этого заказа и `meta.original_order_id`, а не props контакта. Чужой контакт не может получить ссылку другого заказа.
2. Выбор активной ссылки и срок действия — PASS при условии, что фильтр «активна и не истекла» вычисляется на сервере (`status = active` и `expires_at is null or expires_at > now()`), а при нескольких подходящих строках берётся самая свежая. Просроченная ссылка не должна возвращаться, иначе UI покажет мёртвый URL вместо создания новой.
3. Валидация метаданных — PASS. Проверяются `contract_version`, оригинальный заказ, подписка, `preserves_purchase=true`, `changes_access=false` и совпадение `payment_type`. Ссылка с несовпавшим контрактом отбрасывается, а не «чинится».
4. Отсутствие дублей — PASS. `get_active` не делает INSERT/UPDATE, не вызывает провайдера, не создаёт заказ/подписку/доступ. Диалог при открытии сначала спрашивает `get_active` и при наличии ссылки показывает панель успеха и повторную отправку вместо ветки создания. Новая покупка/сделка/доступ не появляются.
5. Получатель Telegram — PASS. Источником истины становится `plan.userId` (`orders_v2.user_id`), а не props контакта; `ContactDetailSheet` лишь передаёт уже разрешённую identity. Это ровно причина провала прошлой попытки.
6. Нормализация ошибок — PASS. Валидация получателя, текста и `http(s)` URL до invoke, плюс проброс статуса и тела non-2xx из `telegram-send-notification`, вместо немого «Ошибка отправки». Текущая база бросала голый `error`, диалог падал в ошибку приложения.
7. Утечки токенов/PII — PASS. Возвращается только уже существующий `public_url` и сохранённый quote, доступные роли `payments:edit`; провайдерских ключей и персональных данных в ответе нет. В логи URL не пишется.
8. Гонки и истечение — PASS. Перед повторной отправкой диалог вызывает тот же read-only `get_active`; если ссылка перестала быть активной или истекла, UI сбрасывается в режим создания и Telegram не вызывается. Это устраняет race между открытием диалога и нажатием «Отправить».
9. Инварианты платежей/заказов/доступов — PASS. Финансовых записей, изменений `payments_v2`, подписок, entitlements и TG-грантов ветка не делает; `changes_access=false` сохраняется.

## Execute-лист (после явного одобрения)

1. Синхронизировать merged main `1b051fecbc36eb77f55bc04c27c7df3d23fe9688` и доказать чистое byte-identical дерево.
2. Ноль миграций, ноль записей в данные.
3. Задеплоить ровно одну функцию: `admin-create-public-link` (изменилась её shared-зависимость).
4. `bun run build`; при PASS — Publish; зафиксировать URL, версию и SHA.
5. Проверка восстановления: открыть существующую рассрочку, убедиться, что диалог показывает уже созданную ссылку и не создаёт вторую (read-back `payment_links` по этому заказу — количество строк не изменилось).
6. Ровно одна повторная отправка в Telegram именно этой существующей ссылки; подтвердить `notification_outbox` и `telegram_logs` read-back со статусом отправлено/зеркалировано.
7. Визуальное подтверждение опубликованного UI: скриншоты desktop и mobile.

Сейчас не выполнено ничего.
