# План: публикация PR #511 — SHA `c283f6f2db18391ede33aabcfa3ecb5e026b2310`

## Диагностика и наблюдения (выполнено, read-only)

- Локальный HEAD, GitHub `origin/main` и целевой merge-коммит совпадают: `c283f6f2db18391ede33aabcfa3ecb5e026b2310`; дерево чистое; база `4fbd621e0` — прямой предок.
- Прикладной диф `4fbd621e..c283f6f2` ограничен ровно двумя файлами:
  - `src/components/ai-chat/PromptRunFlow.tsx` — подпись под зоной загрузки для `bank_statement_analysis` перечисляет только фактический whitelist: PDF, XLSX/XLS, CSV, TXT, DOCX, JPG/JPEG, PNG, WebP (точно совпадает с `BANK_STATEMENT_EXTENSIONS`); для остальных сценариев прежний общий текст без изменений;
  - `src/components/ai-chat/PromptRunFlow.test.tsx` — 4 теста: подсказка, файл >20 МБ, неподдерживаемый формат, более 5 файлов.
- Третье изменение в merge — служебный `.lovable/plan.md` (plan-archive), на приложение не влияет.
- Несоответствие подписи из PR #510 устранено. Целевой Vitest перезапущен: 4/4 PASS.
- Миграций, изменений базы, Edge Functions, Auth, доступов, тарифов, продуктов, подписок, платежей, секретов и production-данных нет.

## Предлагаемое выполнение

1. **Preflight / sync**
   - заново прочитать GitHub `refs/heads/main` без stale-cache;
   - подтвердить exact SHA `c283f6f2db18391ede33aabcfa3ecb5e026b2310`, чистое дерево и двухфайловый прикладной диф от `4fbd621e0`;
   - остановиться при любом новом коммите, грязном дереве или расширении scope.
2. **Dry run**
   - целевой Vitest `PromptRunFlow.test.tsx` (ожидание 4/4);
   - полный typecheck и production build штатными командами проекта;
   - без исправлений в рамках этого execute остановиться при любой ошибке.
3. **Publish**
   - опубликовать exact SHA `c283f6f2db18391ede33aabcfa3ecb5e026b2310` на существующий production-сайт `https://gorbova.lovable.app`;
   - не выполнять миграции, deploy функций и любые изменения данных или настроек.
4. **Read-back после публикации**
   - подтвердить опубликованный URL и SHA/версию, а не только HTTP 200;
   - desktop: в режиме «Анализ выписки» до загрузки видна подсказка с корректным списком форматов; три видимых ошибки — неподдерживаемый формат (например XML), файл >20 МБ, шестой файл; текст не обрезан и не перекрыт;
   - viewport 390×844: те же проверки, отсутствие обрезания, горизонтального выхода и ухода элементов за экран;
   - отдельные production-скриншоты desktop и mobile без PII и секретов, привязанные к URL, SHA и viewport.

## STOP-guards

- Published SHA не равен `c283f6f2…`.
- Прикладной диф выходит за два заявленных файла.
- Любой test/typecheck/build не проходит.
- На опубликованной странице подпись противоречит whitelist, ошибка не видна или мобильная компоновка обрезана/перекрыта.
- Обнаруживается изменение backend, данных, функций, доступа или коммерческой логики.

## DoD

- Exact SHA подтверждён до и после Publish.
- Vitest 4/4, typecheck и build PASS.
- Production desktop и 390×844 показывают корректную подсказку до загрузки и видимые ошибки для всех трёх невалидных случаев.
- Два скриншота опубликованного результата, привязанные к URL, SHA и viewport.
- Backend, данные, функции, доступы и коммерческие настройки не изменены.
