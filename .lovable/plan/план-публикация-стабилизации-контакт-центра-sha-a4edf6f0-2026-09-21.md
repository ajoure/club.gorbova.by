# План: публикация стабилизации контакт-центра (SHA a4edf6f0)

## Наблюдения (read-only)

Синхронизация: связанный удалённый репозиторий теперь отдаёт `refs/heads/main = a4edf6f00e89a704dd2ec1be972133753e20e015`; рабочее дерево проекта уже на этом SHA и чистое. `52d269ce…` (PR #504) — прямой предок, поэтому правка мобильных таблиц AI-отчёта входит в публикуемый код.

Состав изменений `93b8bb83 → a4edf6f0`: 21 файл, только frontend и документация.
- Нет миграций, изменений БД, Edge Functions, `.env`, `src/integrations`, Auth, платежей, правил доступа, продуктов, тарифов, подписок, секретов и production-данных (проверено диффом по этим путям — 0 файлов).
- Изменены: `ContactTelegramChat.tsx`, `UnifiedInboxView.tsx`, `TicketChat.tsx`, `useUnifiedInbox.ts`, `useIncomingMessageAlert.ts`, `appViewport.ts`, `index.css`, `ChatMessage.tsx`, 4 новых хука + 6 тестов + 1 документ аудита.

## Ревизия по критическим пунктам

| Пункт | Состояние |
|---|---|
| Пять независимых Telegram-отправителей | Логика выбора не изменена: `channels` передаются как есть, выбор `selectedSenderKey` и нижний компактный селектор не тронуты |
| Правило отправителя по умолчанию | Сохранено: `selectedSenderKey → is_primary → channels[0]` |
| Отсутствие крупных карточек ботов над чатом | Новых блоков нет; добавлена только тонкая строка-предупреждение при неудачной проверке отправителей |
| Мобильная клавиатура / visual viewport | Убран самопроизвольный сброс прокрутки и сдвиг `top` оболочки (источник «дрожания»); геометрия пишется только при реальном изменении; добавлено правило высоты для правой панели контакта |
| Сохранение черновика | Текст переживает перемонтирование и перезагрузку вкладки, ключ разделён по оператору/контакту/отправителю; файлы остаются только в памяти |
| Идентичность очереди | Диалог с неподтверждённой идентичностью больше не показывается как «не привязан»; ранее загруженные профили удерживаются в пределах одного оператора; выбранный диалог не теряется при смене страницы очереди |
| Направление уведомлений | Фильтр Instagram исправлен `incoming → inbound`; в production реально существуют только `inbound` (349) и `outbound` (18), то есть звук входящих раньше не срабатывал вовсе |

Замечания к рискам (не блокирующие):
- При сбое проверки отправителей отправка намеренно блокируется (fail-closed), черновик сохраняется — это изменение поведения, ожидаемое по задаче.
- Ошибка чтения профилей Telegram теперь поднимается наверх: очередь покажет ошибку вместо неверных строк «без привязки».
- `ChatPanel` перемонтируется при смене канала — черновик защищён новым хранилищем черновиков.

## Execute/Publish план

1. Preflight: повторно подтвердить `HEAD = a4edf6f00e89a704dd2ec1be972133753e20e015`, чистое дерево, `52d269ce…` как предок, отсутствие изменений в `supabase/`, `src/integrations/`, `.env`.
2. Проверки без изменений кода: `npx vitest run` по новым тестам (viewport, draft, identity, send, alert), `npx tsc -p tsconfig.app.json --noEmit`, `npm run build`.
3. Publish ровно этого SHA на существующий production-сайт. Никаких миграций, deploy функций и правок данных.
4. Read-back: подтвердить URL и опубликованный SHA/версию.
5. Проверка опубликованного сайта: контакт-центр на ПК 1280×800 и мобильном 390×844 — пять отправителей в нижнем селекторе, отправитель по умолчанию, отсутствие крупных карточек ботов, стабильность при открытой клавиатуре, сохранение черновика, удержание выбранного диалога; отдельные скриншоты для каждого viewport, без персональных данных.

Стоп-условие: любое несовпадение SHA или FAIL сборки/тестов отменяет публикацию.
