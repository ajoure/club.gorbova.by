# Phase 1 Ledger Contract Validation Proof
# Generated: 2026-03-30 (PATCH v22.3)
# Updated: 2026-03-30 (PATCH v22.5) — reconcile runtime smoke added
# Scope: All already-wrapped paths after foundation fix

## DDL CHECKs validated

### 1. grant-access-for-order/index.ts

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| action_type ↔ status | grant→granted, extend→extended | `actionType === 'grant' ? 'granted' : 'extended'` | ✅ PASS |
| reason_code | `chk_reason_code` | `'paid_order'` | ✅ PASS |
| target_type | `chk_target_type` | `'product'` | ✅ PASS |
| source_subject_type | `chk_source_subject_type` | `'order'` | ✅ PASS |
| chk_has_subject | ≥1 NOT NULL | `source_subject_ref=orderId`, `source_order_id=orderId`, `order_id=orderId` | ✅ PASS |
| chk_parent_keys_pair | both NULL or both NOT NULL | Both NULL (top-level event) | ✅ PASS |
| chk_batch_row_contract | N/A (not batch_start) | N/A | ✅ PASS |
| post_check keys | {entitlement, telegram, subscription, ledger_row, target_resolution} | Via `buildPostCheck()` with normalized keys | ✅ PASS |
| post_check schema | {applicability, status, details?, ref?} | Via `PostCheckItem` interface | ✅ PASS |
| no 'completed' outside batch_start | — | status is 'granted' or 'extended' | ✅ PASS |
| no 'telegram_access' target_type | — | target_type='product' | ✅ PASS |
| no 'subscription' target_type | — | target_type='product' | ✅ PASS |

**target_key contract**: `{user_id}:{product_id}` — deterministic, product-scoped
**source_event_key**: `gafo:webhook:{orderId}` — deterministic UUID-based

**result contract for grant/extend**:
| Field | Present | Source |
|-------|---------|--------|
| access_start | ✅ | `accessStartAt.toISOString()` |
| access_end | ✅ | `accessEndAt.toISOString()` |
| window_days | ✅ | `durationDays` |
| source_window_rule | ✅ | `'calendar_month'` / `'tariff_duration'` / `'default_30d'` |
| previous_end | ✅ | `existingProductSub?.access_end_at` |
| post_check | ✅ | Normalized schema |

### 2. subscription-charge/index.ts (renew/extend branch) — DEAD CODE (v22.8)

**v22.8 NOTE**: This entire section documents code that is **unreachable dead code**.
Guards at lines 556-574 return `skipped` for ALL billing_type values before this code executes.
Contract validation below is correct at code level but EXCLUDED_FROM_CUTOVER.

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| action_type ↔ status | extend→extended | `'extended'` | ✅ PASS (code-level, unreachable) |
| reason_code | `chk_reason_code` | `'subscription_renew'` | ✅ PASS (code-level, unreachable) |
| target_type | `chk_target_type` | `'subscription_tier'` | ✅ PASS (code-level, unreachable) |
| source_subject_type | `chk_source_subject_type` | `'subscription'` | ✅ PASS (code-level, unreachable) |
| source_event_type | real origin | `'cron'` (cron-triggered charge) | ✅ PASS (code-level, unreachable) |
| chk_has_subject | ≥1 NOT NULL | `source_subject_ref=id`, `source_subscription_id=id`, `source_order_id=renewalOrderId\|\|order_id` | ✅ PASS (code-level, unreachable) |
| chk_parent_keys_pair | both NULL or both NOT NULL | Both NULL (top-level event) | ✅ PASS (code-level, unreachable) |
| chk_batch_row_contract | N/A | N/A | ✅ PASS |
| post_check keys | normalized | Via `buildPostCheck()` | ✅ PASS (code-level, unreachable) |
| post_check schema | normalized | Via `PostCheckItem` | ✅ PASS (code-level, unreachable) |
| no 'completed' outside batch_start | — | status='extended' | ✅ PASS (code-level, unreachable) |
| no 'subscription' target_type | — | target_type='subscription_tier' | ✅ PASS (code-level, unreachable) |
| profile_id present | — | `subscription?.profile_id` | ✅ PASS (code-level, unreachable) |

**target_key contract**: `{user_id}:{tariff_id}` — deterministic, tariff-scoped
**source_event_key**: `sub-renew:{subscription_id}:{payment_id}` — deterministic UUID-based

**result contract for extend** (unreachable):
| Field | Present |
|-------|---------|
| access_start | ✅ |
| access_end | ✅ |
| window_days | ✅ |
| source_window_rule | ✅ |
| previous_end | ✅ |
| post_check | ✅ |

### 3. subscriptions-reconcile/index.ts

#### Block 1: Expired cancellations (cancel_at passed)

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| action_type ↔ status | revoke→revoked, skip→skipped | Via AccessRevoker | ✅ PASS |
| reason_code | `chk_reason_code` | `'subscription_expired'` | ✅ PASS |
| target_type | `chk_target_type` | `'subscription_tier'` | ✅ PASS |
| source_subject_type | `chk_source_subject_type` | `'subscription'` | ✅ PASS |
| chk_has_subject | ≥1 NOT NULL | `source_subject_ref=sub.id`, `source_subscription_id=sub.id` | ✅ PASS |
| reconcile_basis | caller-provided | `'cancel_at_passed'` | ✅ PASS |

**target_key**: `{user_id}:{product_id}`
**source_event_key**: `cron-reconcile:{jobRunId}:{sub.id}`

**result contract for revoke**:
| Field | Present |
|-------|---------|
| revoked_from | ✅ |
| previous_access_end | ✅ (null, can be enriched) |
| reconcile_basis | ✅ |
| other_active_sources_checked | ✅ |

#### Block 2: Trial expired (trial_end_at passed + canceled)

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| reason_code | `chk_reason_code` | `'trial_expired'` | ✅ PASS |
| target_type | `chk_target_type` | `'subscription_tier'` | ✅ PASS |
| source_subject_type | `chk_source_subject_type` | `'subscription'` | ✅ PASS |
| reconcile_basis | caller-provided | `'trial_end_at_passed_and_canceled'` | ✅ PASS |

**source_event_key**: `cron-reconcile:{jobRunId}:trial:{sub.id}`

#### Block 3: Access expired (access_end_at passed)

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| reason_code | `chk_reason_code` | `'subscription_expired'` | ✅ PASS |
| target_type | `chk_target_type` | `'subscription_tier'` | ✅ PASS |
| source_subject_type | `chk_source_subject_type` | `'subscription'` | ✅ PASS |
| reconcile_basis | caller-provided | `'access_end_at_passed'` | ✅ PASS |

**source_event_key**: `cron-reconcile:{jobRunId}:expired:{sub.id}`

#### Block 4: Telegram sync (no valid access for joined member)

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| reason_code | `chk_reason_code` | `'cron_cleanup'` | ✅ PASS |
| target_type | `chk_target_type` | `'club'` | ✅ PASS |
| source_subject_type | `chk_source_subject_type` | `'cron_job'` | ✅ PASS |
| reconcile_basis | caller-provided | `'no_valid_access_for_telegram_member'` | ✅ PASS |

**source_event_key**: `cron-reconcile:{jobRunId}:tg:{user_id}:{club_id}`

## Cross-cutting validations

### source_event_key determinism
All keys use UUID-based discriminators (orderId, subscription.id, payment.id, jobRunId, user_id, club_id).
No ISO timestamps, no Date.now(), no Math.random().
`validateSourceEventKey()` enforces this at runtime with throw on violation.

### Runtime DDL validation in writeLedgerEntry()
Before every insert, `validateLedgerEntry()` checks:
- action_type ↔ status compatibility
- reason_code ∈ dictionary (18 values)
- target_type ∈ dictionary (9 values)
- source_subject_type ∈ dictionary (7 values)
- parent_event_key / parent_execution_key pair constraint
- chk_has_subject (≥1 subject ref NOT NULL)
- batch_start → target_type='batch' constraint
Throws on any violation — no silent fail.

### writeLedgerEntry returns execution_key
`select('id, execution_key')` after insert.
Returns `{ id, execution_key, error }` for parent propagation.

### updateLedgerPostCheck uses MERGE
Reads existing result → merges post_check → preserves access window fields.
Does NOT change status. Does NOT overwrite access_start/access_end/window_days/source_window_rule/previous_end.

### post_check key set
Fixed set: `{entitlement, telegram, subscription, ledger_row, target_resolution}`.
No `telegram_grant` key anywhere.

### post_check item schema
All items use `{applicability: 'required'|'not_applicable', status: 'pass'|'warn'|'fail'|null, details?, ref?}`.
No `applicable: boolean` anywhere.

### result contract by action_type
- grant/extend/reactivate → MUST have: access_start, access_end, window_days, source_window_rule, post_check. MAY have: previous_end.
- revoke/expire → MUST have: revoked_from, reconcile_basis, other_active_sources_checked. MAY have: previous_access_end, kept_projections.
- skip → MUST have: skip_reason. MAY have: reconcile_basis, existing_ref, other_active_sources_checked.
- failed → SHOULD have: failed_at_step, error_message.
- batch_start → post_check IS NULL.

### batch rows
No batch_start rows in current wrapped paths. N/A.

### 'completed' status
Not used in any wrapped path. Only allowed for batch_start action_type.

### 'telegram_access' as target_type
Not used anywhere. Replaced with 'club' (block 4) or 'subscription_tier' (blocks 1-3) or 'product' (grant).

### 'subscription' as target_type
Not used anywhere. Replaced with 'subscription_tier' or 'product'.

## Runtime Validation Gate (v22.5, updated v22.8)

Added: 2026-03-30
Updated: 2026-03-30 — PATCH v22.5 reconcile fixture smoke completed
Updated: 2026-03-30 — v22.8 Dead Code Resolution (Path A)

### Status: 2/3 PASS + 1 EXCLUDED_FROM_CUTOVER (dead code)

| Path | Status | Ledger rows | Detail |
|------|--------|-------------|--------|
| grant-access-for-order | ✅ PASS | 1 (b604ba7c) | extend/extended, paid_order, product, 5 canonical post_check keys, merge verified |
| subscription-charge | ⛔ EXCLUDED_FROM_CUTOVER | 0 | Dead code: extend path unreachable under current guards (v22.8) |
| subscriptions-reconcile | ✅ PASS | 1 (9578416b) | revoke/revoked, cancel_at_passed, subscription_tier, DDL valid |

DB old-value guard: still clean (0 telegram_access, 0 bare subscription, 0 completed non-batch).

### Bug found (v22.4)
subscription-charge/index.ts line 2212: `todayUtc` undefined → fixed to `todayKey`. Audit-log only, no ledger impact.

### Linked proofs
- `p0_ledger_runtime_smoke_proof.txt` — runtime row validation (2/3 PASS + 1 EXCLUDED_FROM_CUTOVER)
- `p0_ledger_grep_guard_proof.txt` — code grep + helper contract freeze (PASS)
- `p0_runtime_candidate_eligibility_proof.txt` — gate-by-gate analysis + dead code finding
- `p0_runtime_fixture_lifecycle_proof.txt` — fixture creation, usage, cleanup tracking
- `p0_runtime_manual_smoke_steps.txt` — manual smoke-test procedure

### Cutover guard (updated v22.8)

phase1_ledger_cutover_at MUST NOT be set until:
- grant-access-for-order runtime_smoke = PASS ✅
- subscriptions-reconcile runtime_smoke = PASS ✅
- subscription-charge extend = EXCLUDED_FROM_CUTOVER (dead code) ✅
- grep_guard = PASS ✅
- contract_validation = PASS ✅
- orphan_downstream = 0 ✅
- missing_required_post_check = 0 ✅ (1 revoke = by design)
- phase1_ledger_schema_ready_at = SET ✅

All technical prerequisites met. Formal approval required.

## Conclusion
PATCH v22.3: all 3 wrapped paths DDL-compatible (code-level).
PATCH v22.4: grant-access-for-order PASS, todayUtc bug fixed.
PATCH v22.5: subscriptions-reconcile PASS via controlled fixture.
v22.8: subscription-charge extend = EXCLUDED_FROM_CUTOVER (dead code finding).
       Production renewals use bepaid-webhook → grant-access-for-order (PASS).

---

## Sub-patch B Finish (v22.6)

Added: 2026-03-30

### Duplicate post-switch ledger block removal

The shared post-switch block in `subscription-admin-actions/index.ts` (previously lines 1265-1355)
has been **fully removed**. All revoke branches now use exclusively inline ledger writes.

#### Machine-check: inline-only ledger in subscription-admin-actions

| Branch | Inline ledger path | Post-switch duplicate | Confirmed |
|--------|-------------------|----------------------|-----------|
| cancel | writeLedgerEntry + executeRevoke (lines ~710-765) | REMOVED | ✅ |
| revoke_access | writeLedgerEntry + executeRevoke (lines ~943-985) | REMOVED | ✅ |
| delete | writeLedgerEntry + executeRevoke (lines ~1078-1114) | REMOVED | ✅ |
| refund+revoke | executeRevoke (lines ~483-507) | never existed in post-switch | ✅ |

grep result after cleanup: `executeRevoke` / `writeLedgerEntry` appear ONLY within case branches + imports.
No second block outside case branches for revoke actions.

### Parent propagation wired

All primary callers now pass `parent_event_key` / `parent_execution_key` to downstream calls.
See `p0_downstream_parent_propagation_proof.txt` for full lineage table.

### Queue meta wired

`subscription-charge` renew path writes lineage into `telegram_access_queue.meta`
using the exact same `renewSourceEventKey` variable used for the primary ledger row (no drift).
Conditional spread ensures existing meta is NOT overwritten when `execution_key` is absent.

`telegram-club-members` reinvite_ghosts enqueue is autonomous/no-parent by design (no upstream ledger row).

### telegram-process-access-queue backward compatibility

Queue processor reads `meta` from queue rows and forwards parent keys downstream.
When `meta` is null (old rows or autonomous enqueues), parent keys are passed as null.
No crash, no forced lineage — backward compatible. ✅

## Sub-patch C: Batch/Import Tree (v22.6)

Added: 2026-03-30

### chk_has_subject verification for batch/import paths

| Path | source_subject_ref | Other subject FKs | chk_has_subject |
|------|-------------------|-------------------|-----------------|
| bepaid batch_start | batchId (UUID) | — | ✅ PASS |
| bepaid row-level | payment.uid / `hash:${canonicalHash}` | order_id, source_order_id, profile_id, user_id | ✅ PASS |
| getcourse file batch_start | batchId (UUID) | — | ✅ PASS |
| getcourse file row-level | externalId / `hash:${canonicalHash}` | order_id, source_order_id, source_subscription_id, profile_id, user_id | ✅ PASS |
| getcourse API batch_start | batchId (UUID) | — | ✅ PASS |
| getcourse API row-level | deal.id / `hash:${canonicalHash}` | order_id, source_order_id, source_subscription_id, profile_id, user_id | ✅ PASS |

> **proof-text sync fix**: Previously showed `row:{index}` — updated to match code (Sub-patch C).

### batch_start contract validation

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| action_type = 'batch_start' | batch_start | batch_start | ✅ PASS |
| status = 'completed' | completed | completed | ✅ PASS |
| target_type = 'batch' | batch | batch | ✅ PASS |
| reason_code = 'batch_orchestration' | batch_orchestration | batch_orchestration | ✅ PASS |
| source_subject_type = 'import_batch' | import_batch | import_batch | ✅ PASS |
| chk_batch_row_contract | batch_start → target_type='batch' | ✅ | ✅ PASS |
| post_check | NULL (batch rows) | Not provided | ✅ PASS |

### Row-level contract validation

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| action_type ↔ status | grant→granted, skip→skipped, grant→failed | Via outcome mapping | ✅ PASS |
| reason_code | bulk_import, duplicate_skip, no_matching_target | Per outcome | ✅ PASS |
| target_type | subscription_tier / product (NEVER batch) | Via target_type resolution | ✅ PASS |
| source_subject_type | import_batch | import_batch | ✅ PASS |
| parent keys | Both set (from batch_start) | parent_event_key + parent_execution_key | ✅ PASS |
| failed result | failed_at_step + error_message | In catch block | ✅ PASS |

### Machine-check: no duplicate post-switch ledger block (from Sub-patch B)

After cleanup in subscription-admin-actions, revoke branches have ONLY inline ledger path.
Duplicate post-switch revoke ledger path is ABSENT. ✅

### Volume guard order verification

| Import | Guard before batch_start | Confirmed |
|--------|------------------------|-----------|
| bepaid-report-import | ✅ (after parse, before batch_start) | ✅ |
| getcourse file import | ✅ (in main handler, before batch_start) | ✅ |
| getcourse API import | ✅ (in main handler, before batch_start) | ✅ |

### source_event_type by origin

| Path | Expected | Actual | Status |
|------|----------|--------|--------|
| bepaid-report-import | admin | admin | ✅ PASS |
| getcourse file import | admin | admin | ✅ PASS |
| getcourse API import | system | system | ✅ PASS |

## Sub-patch C addendum: entropy elimination & deterministic identity

### 1. Date.now / Math.random removal
- getcourse-import-deals/index.ts: `Date.now()` count = 0, `Math.random()` count = 0 ✅
- bepaid-report-import/index.ts: `Date.now()` count = 0, `Math.random()` count = 0 ✅

### 2. Index removed from identity
- rowSourceEventKey fallback: `{prefix}:row:hash:${canonicalHash}` (no index) ✅
- rowSubjectRef fallback: `hash:${canonicalHash}` (no index) ✅
- index only in result.row_index (debug trace) ✅

### 3. Deterministic order_number
- gc_file: `HASH-${canonicalHash}` → order_number `GC-HASH-${canonicalHash}` ✅
- No double GC- prefix: createOrder checks `.startsWith('GC-')` ✅
- Repeated import of same row without externalId → duplicate path (not new order) ✅

### 4. bepaid existence-check: .single() → select+length
- Uses `.select('id')` without `.single()` or `.maybeSingle()` on existence check ✅
- 0 rows → create, 1 row → skip, >1 rows → failed with 'multiple_existing_orders' ✅
- Remaining `.single()` calls are on INSERT (`.insert(...).select().single()`) — correct ✅

### 5. DB SoT contracts
- getcourse: tariff via `tariffs.getcourse_offer_id` (no OFFER_TARIFF_MAP) ✅
- bepaid: product via `bepaid_product_mappings` table ✅

### 6. stableRowHash drift guard
- Implementation byte-identical in both files ✅
- Sort rule: Object.keys().sort() ✅
- Serialization: JSON.stringify ✅
- Slice: .slice(0, 16) ✅

### Sprint status

| Component | Status |
|-----------|--------|
| Sub-patch A (revoke paths) | ✅ done |
| Sub-patch B (parent propagation) | ✅ done in code |
| Sub-patch C (batch/import — consolidated final) | ✅ done in code |
| Runtime verification | ⏸ deferred_non_blocking |
| Sub-patch D (consolidated proof finalization) | ✅ done |
| phase1_ledger_cutover_at | ❌ NOT SET (pending formal approval) |

Sprint v22.6 = implementation-complete.
Sprint v22.7 = readiness-complete.
Sprint v22.8 = cutover-ready, pending formal approval.
See `p0_invariant_report.txt` for consolidated deferred list and decision block.
