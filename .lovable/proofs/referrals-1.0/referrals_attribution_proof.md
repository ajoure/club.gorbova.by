# Отчет о выполненной работе: attribution proof

Первый активный referrer закрепляется уникальным partial index. Self-referral запрещён RPC и DB trigger. Один `order_id` может иметь только одну attribution. Existing contact привязывается admin RPC с обязательной причиной.
