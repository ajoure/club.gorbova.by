# Canonical Infrastructure (v1)

Status: ACTIVE
Owner: Lovable Cloud (managed Supabase)
Last updated: 2026-06-06 (v2 — Lovable-only Deploy Model)

## 1. Single Supabase project_ref

| Field | Value |
|---|---|
| **Canonical project_ref** | `hdjgkjceownmmnrqqtuz` |
| Supabase URL | `https://hdjgkjceownmmnrqqtuz.supabase.co` |
| Functions base URL | `https://hdjgkjceownmmnrqqtuz.functions.supabase.co` |
| Ownership | Lovable Cloud (managed) |
| Lovable project ID | `796a93b9-74cc-403c-8ec5-cafdb2a5beaa` |

**Legacy ref (forbidden, must never reappear):** `ypwsuumurrtkxatoyqhk`.
CI guard: `.github/workflows/verify-no-legacy-ref.yml`.

## 2. Canonical deploy paths

### 2.1 Edge functions

- **Single canonical channel:** Lovable Cloud agent-deploy (internal).
- **Webhook functions are currently BLOCKED-BY-PLATFORM.** Lovable agent-deploy
  has been observed to ignore per-function `verify_jwt = false` from
  `supabase/config.toml`, leaving webhook endpoints behind a platform-level
  JWT wall (`UNAUTHORIZED_NO_AUTH_HEADER` instead of application-level
  `signature_verification_failed`). See §8 (Webhook deploy moratorium).
- **GitHub Actions is NOT a deploy channel** for this project. The operator
  has no `SUPABASE_ACCESS_TOKEN` / `SUPABASE_DB_PASSWORD` and never will.
  `apply-migrations.yml` and `deploy-functions.yml` are no-op stubs.

### 2.2 Database migrations

- **Single canonical channel:** Lovable Cloud agent (`supabase--migration`).
- GitHub Actions: disabled (no-op stub).

### 2.3 Frontend

- Lovable publish (Lovable → CDN). `.env` and `src/integrations/supabase/client.ts`
  autogenerated and pinned to the canonical ref.

## 3. Active workflows

| Workflow | Type | Status |
|---|---|---|
| `verify-webhook-public.yml` | Read-only (config guard) | ACTIVE |
| `verify-webhook-runtime.yml` | Read-only (runtime smoke) | ACTIVE |
| `verify-payment-methods.yml` | Read-only | ACTIVE |
| `verify-no-legacy-ref.yml` | Read-only (legacy ref guard) | ACTIVE |
| `functions-full-audit.yml` | Read-only audit (manual) | ACTIVE (manual) |
| `apply-migrations.yml` | (was deploy) | **DISABLED (no-op stub)** |
| `deploy-functions.yml` | (was deploy) | **DISABLED (no-op stub)** |

The two disabled stubs intentionally have **no `push`, no `schedule`,
no `workflow_run` triggers** — they cannot create background red runs
in the Actions UI. Manual dispatch always exits 1 with a clear message
and never touches any Supabase secret.

## 4. Stripe webhook endpoints

Only allowed Stripe webhook endpoint URL:

```
https://hdjgkjceownmmnrqqtuz.functions.supabase.co/stripe-webhook
```

Anything else in the Stripe Dashboard must be removed by the operator.

## 5. Lovable platform issue (open)

`agent-deploy ignores verify_jwt = false for webhook functions in Lovable-managed
Supabase`. Full reproduction, proof, and copy-paste issue text live in:

- `.lovable/backlog/lovable_agent_deploy_verify_jwt_regression.md`
- `.lovable/proofs/stripe_phase_3_4_d2_bis_webhook_runtime_v1.md`
- `.lovable/discovery/stripe_webhook_redeploy_d2_bis_v1.md`

## 6. Legacy Supabase project (`ypwsuumurrtkxatoyqhk`)

- Not used by any workflow, code, env, or config in this repo.
- Must not be deployed to. Must not be referenced.
- Physical deletion is out of scope.

## 7. GitHub secrets policy

- `SUPABASE_ACCESS_TOKEN` in GitHub: **not used by any workflow**.
- `SUPABASE_DB_PASSWORD` in GitHub: **not used by any workflow**.
- Any old values stored in GitHub Secrets (presumed to belong to the legacy
  ref `ypwsuumurrtkxatoyqhk`) are considered dead. Rotation/removal is at
  the operator's discretion and does not block any canonical flow.
- Do not request these secrets from the operator. Do not add new workflows
  that consume them.

## 8. Webhook deploy moratorium

Until Lovable platform answers the issue in §5, the following rule is
binding:

- **No agent-redeploy of any webhook function.** Affected names:
  `stripe-webhook`, `bepaid-webhook`, `telegram-webhook`,
  `payment-methods-webhook`, `auth-email-hook`, `instagram-webhook`,
  `getcourse-webhook`, `amocrm-webhook`.
- `stripe-webhook` is currently in platform-401 state from a prior
  controlled-redeploy regression. **Status: BLOCKED-BY-PLATFORM. Do not
  attempt to "fix" via redeploy** — that is the action that caused it.
- Other 7 webhook functions: PASS (snapshot in
  `.lovable/discovery/stripe_webhook_redeploy_d2_bis_v1.md` section A).
  Do not touch.
- `verify-webhook-runtime.yml` continues to monitor. A FAIL on
  `stripe-webhook` is interpreted as known-blocked, not a new regression.

Current infrastructure status:

```
Infrastructure model     = CLEAN
GitHub deploy            = DISABLED (no-op stub)
Lovable webhook deploy   = BLOCKED-BY-PLATFORM
Phase 3.4 Runtime        = FROZEN
```
