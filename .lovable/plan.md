

# План: Уменьшить отступы на странице /ai — сделать компактнее, как в «Сделках»

## Проблема

Между хлебными крошками (header) и pill-табами, а также между табами и контентом — слишком большие отступы. Нужно сделать компактнее и симметрично.

## Изменения

### 1. `src/pages/AI.tsx`

**Корневой div** (строка 210): `space-y-4` → `space-y-2` — уменьшить вертикальные промежутки между блоками.

**Обёртка табов** (строка 214): `px-1 pt-1 pb-1.5` → `px-1 pt-0.5 pb-0.5` — симметричные мини-отступы сверху и снизу от pill-bar (≈2-3mm визуально).

**Pill-bar container** (строка 218): оставить как есть (`p-0.5 rounded-full`) — совпадает со «Сделками».

**Chat panel** (строка 243): уменьшить offset в calc — `280px` → `240px` (из-за сокращения отступов выше).

### 2. Единообразие — без изменений в других файлах

Страница «Сделки» (`AdminDeals.tsx`, строка 644) уже использует `px-1 pt-1 pb-1.5`. Приведём /ai к тем же минимальным значениям. Если нужно — потом подтянем Deals аналогично, но сейчас скоуп только /ai.

## Файлы

| Файл | Что |
|---|---|
| `src/pages/AI.tsx` | `space-y-4`→`space-y-2`, padding обёртки табов, calc offset |

