
# "Выписка BePaid" — Новая вкладка в Payments Hub

## Обзор задачи

Создать новую вкладку **"Выписка BePaid"** в меню Платежи, справа от переключателя "Ошибки". Эта вкладка будет отдельной страницей с:
- Эталонной базой данных всех транзакций bePaid
- Импортом Excel/CSV файлов
- Сводкой как в файле bePaid (вкладка "Отчет")
- Выделением транзакций с динамическим подсчётом суммы
- Поиском по любым полям
- Горизонтальным скроллом таблицы с копированием данных

---

## Структура файла bePaid (из образца)

### Вкладка "Отчет" (Сводка)
| Торговец | Магазин | Карта | Провайдер токена | Страна | Валюта | Тип транзакции | Статус | Количество | Оборот | Период | Сумма комиссий | Перечисленная сумма |

### Вкладка "Карточные транзакции" (60 колонок!)
| UID | ID заказа | Статус | Описание | Сумма | Валюта | Комиссия,% | Комиссия за операцию | Сумма комиссий | Перечисленная сумма | Тип транзакции | Трекинг ID | Дата создания | Дата оплаты | Дата перечисления | Действует до | Сообщение | ID магазина | Магазин | Категория бизнеса | ID банка | Имя | Фамилия | Адрес | Страна | Город | Индекс | Область | Телефон | IP | E-mail | Способ оплаты | Код продукта | Карта | Владелец карты | Карта действует | BIN карты | Банк | Страна банка | 3-D Secure | Результат AVS | Fraud | Код авторизации | RRN | Причина | Идентификатор оплаты | Провайдер токена | ID торговца | Страна торговца | Компания торговца | Сумма после конвертации | Валюта после конвертации | ID шлюза | Рекуррентный тип | Card BIN (8) | Код банка | Код ответа | Курс конвертации | Перечисленная сумма после конвертации | Сумма комиссий в валюте после конвертации |

---

## Архитектура решения

### 1. Новая таблица БД: `bepaid_statement_rows`

```sql
CREATE TABLE bepaid_statement_rows (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  uid TEXT NOT NULL,                           -- bePaid UID (ключ дедупликации)
  order_id_bepaid TEXT,                        -- ID заказа из bePaid
  status TEXT,                                 -- Статус
  description TEXT,                            -- Описание
  amount NUMERIC(12,2),                        -- Сумма
  currency TEXT DEFAULT 'BYN',                 -- Валюта
  commission_percent NUMERIC(5,2),             -- Комиссия,%
  commission_per_op NUMERIC(12,2),             -- Комиссия за операцию
  commission_total NUMERIC(12,2),              -- Сумма комиссий
  payout_amount NUMERIC(12,2),                 -- Перечисленная сумма
  transaction_type TEXT,                       -- Тип транзакции
  tracking_id TEXT,                            -- Трекинг ID
  created_at_bepaid TIMESTAMPTZ,               -- Дата создания
  paid_at TIMESTAMPTZ,                         -- Дата оплаты
  payout_date TIMESTAMPTZ,                     -- Дата перечисления
  expires_at TIMESTAMPTZ,                      -- Действует до
  message TEXT,                                -- Сообщение
  shop_id TEXT,                                -- ID магазина
  shop_name TEXT,                              -- Магазин
  business_category TEXT,                      -- Категория бизнеса
  bank_id TEXT,                                -- ID банка
  first_name TEXT,                             -- Имя
  last_name TEXT,                              -- Фамилия
  address TEXT,                                -- Адрес
  country TEXT,                                -- Страна
  city TEXT,                                   -- Город
  zip TEXT,                                    -- Индекс
  region TEXT,                                 -- Область
  phone TEXT,                                  -- Телефон
  ip TEXT,                                     -- IP
  email TEXT,                                  -- E-mail
  payment_method TEXT,                         -- Способ оплаты
  product_code TEXT,                           -- Код продукта
  card_masked TEXT,                            -- Карта
  card_holder TEXT,                            -- Владелец карты
  card_expires TEXT,                           -- Карта действует
  card_bin TEXT,                               -- BIN карты
  bank_name TEXT,                              -- Банк
  bank_country TEXT,                           -- Страна банка
  secure_3d TEXT,                              -- 3-D Secure
  avs_result TEXT,                             -- Результат AVS
  fraud TEXT,                                  -- Fraud
  auth_code TEXT,                              -- Код авторизации
  rrn TEXT,                                    -- RRN
  reason TEXT,                                 -- Причина
  payment_identifier TEXT,                     -- Идентификатор оплаты
  token_provider TEXT,                         -- Провайдер токена
  merchant_id TEXT,                            -- ID торговца
  merchant_country TEXT,                       -- Страна торговца
  merchant_company TEXT,                       -- Компания торговца
  converted_amount NUMERIC(12,2),              -- Сумма после конвертации
  converted_currency TEXT,                     -- Валюта после конвертации
  gateway_id TEXT,                             -- ID шлюза
  recurring_type TEXT,                         -- Рекуррентный тип
  card_bin_8 TEXT,                             -- Card BIN (8)
  bank_code TEXT,                              -- Код банка
  response_code TEXT,                          -- Код ответа
  conversion_rate NUMERIC(10,6),               -- Курс конвертации
  converted_payout NUMERIC(12,2),              -- Перечисленная сумма после конвертации
  converted_commission NUMERIC(12,2),          -- Сумма комиссий в валюте после конвертации
  raw_data JSONB,                              -- Полные исходные данные строки
  import_batch_id TEXT,                        -- ID батча импорта
  imported_at TIMESTAMPTZ DEFAULT now(),       -- Дата импорта
  updated_at TIMESTAMPTZ DEFAULT now(),        -- Дата обновления
  UNIQUE(uid)                                  -- Идемпотентность по UID
);

-- Индексы
CREATE INDEX idx_bepaid_statement_rows_paid_at ON bepaid_statement_rows(paid_at);
CREATE INDEX idx_bepaid_statement_rows_status ON bepaid_statement_rows(status);
CREATE INDEX idx_bepaid_statement_rows_amount ON bepaid_statement_rows(amount);
CREATE INDEX idx_bepaid_statement_rows_email ON bepaid_statement_rows(email);
CREATE INDEX idx_bepaid_statement_rows_card ON bepaid_statement_rows(card_masked);

-- RLS
ALTER TABLE bepaid_statement_rows ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Admins can read bepaid_statement_rows" ON bepaid_statement_rows
  FOR SELECT USING (public.is_admin_v2(auth.uid()));
CREATE POLICY "Admins can insert bepaid_statement_rows" ON bepaid_statement_rows
  FOR INSERT WITH CHECK (public.is_admin_v2(auth.uid()));
CREATE POLICY "Admins can update bepaid_statement_rows" ON bepaid_statement_rows
  FOR UPDATE USING (public.is_admin_v2(auth.uid()));
```

---

### 2. Навигация: добавить вкладку в PaymentsHub

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ [Платежи] [Автопродления] [Предзаписи] [Диагностика] [Выписка BePaid]       │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Файлы:**
- `src/pages/admin/AdminPaymentsHub.tsx` — добавить новую вкладку
- `src/App.tsx` — добавить роут `/admin/payments/statement`

---

### 3. Новый компонент: `BepaidStatementTabContent.tsx`

#### Структура UI:

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ [Импорт]                                     [🔍 Поиск по всем полям...]    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ╔═══════════════════════════════════════════════════════════════════════╗   │
│ ║ СВОДКА (как вкладка "Отчет" bePaid)                                   ║   │
│ ║ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           ║   │
│ ║ │Платежи  │ │Возвраты │ │Отмены   │ │Комиссия │ │Перечислено│         ║   │
│ ║ │51973.13 │ │2585.00  │ │628.00   │ │1125.06  │ │47823.07  │          ║   │
│ ║ │388 шт   │ │19 шт    │ │81 шт    │ │         │ │          │          ║   │
│ ║ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘           ║   │
│ ╚═══════════════════════════════════════════════════════════════════════╝   │
├─────────────────────────────────────────────────────────────────────────────┤
│ ╔═══════════════════════════════════════════════════════════════════════╗   │
│ ║ ВЫДЕЛЕНО: 5 транзакций | Сумма: 1 250.00 BYN                          ║   │
│ ╚═══════════════════════════════════════════════════════════════════════╝   │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────────────────────────────────────┐
│ │ ☑ │ UID │ ID заказа │ Статус │ Сумма │ Дата оплаты │ E-mail │ Карта │... │
│ │───│─────│───────────│────────│───────│─────────────│────────│───────│─── │
│ │ ☑ │ 99c │ 601950603 │ Успешный│ 250.00│ 03.01 19:08 │        │ *9310 │... │
│ │ ☐ │ 920 │ 602529918 │ Успешный│ 100.00│ 04.01 11:56 │ rubel..│ *6635 │... │
│ │ ☑ │ 1bd │ 602578875 │ Успешный│ 250.00│ 04.01 13:48 │ irka13 │ *5262 │... │
│ │ ...                                                                       │
│ └───────────────────────────────────────────────────────────────────────────┘
│   ← Горизонтальный скролл для всех 60 колонок →                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 4. Ключевые функции

#### A) Импорт Excel/CSV
- Парсинг через `xlsx` библиотеку
- Определение листов: "Карточные транзакции", "ЕРИП транзакции"
- Извлечение всех 60 колонок
- **Upsert логика**: если UID уже есть — обновляем данные
- Батчами по 100 записей

#### B) Сводка (Summary)
- Агрегация из `bepaid_statement_rows`:
  - Платежи успешные: count + sum
  - Возвраты: count + sum
  - Отмены: count + sum
  - Комиссии: sum
  - Перечислено: sum
- Группировка по `transaction_type` + `status`

#### C) Выделение + Динамическая сводка
- Чекбоксы на каждой строке
- "Выделить все" в шапке
- Панель "Выделено: N транзакций | Сумма: X BYN"
- Обновляется мгновенно при клике

#### D) Поиск по любому полю
- Один инпут поиска
- Поиск по: UID, order_id, email, phone, card_masked, card_holder, description, IP, tracking_id, shop_name, bank_name, etc.
- Debounce 300ms

#### E) Таблица с горизонтальным скроллом
- `overflow-x-auto` контейнер
- Sticky первая колонка (checkbox) + вторая (UID)
- Копирование при клике на ячейку
- Минималистичный стиль (как в остальной платформе)

---

### 5. Хук: `useBepaidStatement.ts`

```typescript
export function useBepaidStatement(dateFilter: DateFilter) {
  return useQuery({
    queryKey: ['bepaid-statement', dateFilter],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('bepaid_statement_rows')
        .select('*')
        .gte('paid_at', dateFilter.from)
        .lte('paid_at', dateFilter.to)
        .order('paid_at', { ascending: false });
      
      if (error) throw error;
      return data;
    }
  });
}
```

---

### 6. Edge Function: `admin-import-bepaid-statement`

```typescript
// Принимает массив распарсенных строк
// Делает upsert в bepaid_statement_rows по UID
// Возвращает статистику: created, updated, skipped, errors
```

---

## Файлы для создания/изменения

| Файл | Действие | Описание |
|------|----------|----------|
| `supabase/migrations/xxx_create_bepaid_statement_rows.sql` | CREATE | Таблица + индексы + RLS |
| `src/pages/admin/AdminPaymentsHub.tsx` | EDIT | Добавить вкладку "Выписка BePaid" |
| `src/App.tsx` | EDIT | Добавить роут `/admin/payments/statement` |
| `src/components/admin/payments/BepaidStatementTabContent.tsx` | CREATE | Основной компонент вкладки |
| `src/components/admin/payments/BepaidStatementTable.tsx` | CREATE | Таблица с горизонтальным скроллом |
| `src/components/admin/payments/BepaidStatementSummary.tsx` | CREATE | Сводка (как вкладка Отчет) |
| `src/components/admin/payments/BepaidStatementImportDialog.tsx` | CREATE | Диалог импорта |
| `src/hooks/useBepaidStatement.ts` | CREATE | Хук для запросов |
| `supabase/functions/admin-import-bepaid-statement/index.ts` | CREATE | Edge Function для upsert |

---

## Порядок выполнения

1. **Миграция БД** — создать таблицу `bepaid_statement_rows`
2. **Роутинг** — добавить вкладку в Hub и роут в App.tsx
3. **Хук** — `useBepaidStatement.ts`
4. **Edge Function** — `admin-import-bepaid-statement`
5. **Компоненты UI**:
   - `BepaidStatementTabContent.tsx`
   - `BepaidStatementSummary.tsx`
   - `BepaidStatementTable.tsx`
   - `BepaidStatementImportDialog.tsx`

---

## Технические детали

### Колонки таблицы (маппинг из Excel)

```typescript
const COLUMN_MAP = {
  'uid': 'uid',
  'id заказа': 'order_id_bepaid',
  'статус': 'status',
  'описание': 'description',
  'сумма': 'amount',
  'валюта': 'currency',
  'комиссия,%': 'commission_percent',
  'комиссия за операцию': 'commission_per_op',
  'сумма комиссий': 'commission_total',
  'перечисленная сумма': 'payout_amount',
  'тип транзакции': 'transaction_type',
  'трекинг id': 'tracking_id',
  'дата создания': 'created_at_bepaid',
  'дата оплаты': 'paid_at',
  'дата перечисления': 'payout_date',
  'действует до': 'expires_at',
  'сообщение': 'message',
  'id магазина': 'shop_id',
  'магазин': 'shop_name',
  'категория бизнеса': 'business_category',
  'id банка': 'bank_id',
  'имя': 'first_name',
  'фамилия': 'last_name',
  'адрес': 'address',
  'страна': 'country',
  'город': 'city',
  'индекс': 'zip',
  'область': 'region',
  'телефон': 'phone',
  'ip': 'ip',
  'e-mail': 'email',
  'способ оплаты': 'payment_method',
  'код продукта': 'product_code',
  'карта': 'card_masked',
  'владелец карты': 'card_holder',
  'карта действует': 'card_expires',
  'bin карты': 'card_bin',
  'банк': 'bank_name',
  'страна банка': 'bank_country',
  '3-d secure': 'secure_3d',
  'результат avs': 'avs_result',
  'fraud': 'fraud',
  'код авторизации': 'auth_code',
  'rrn': 'rrn',
  'причина': 'reason',
  'идентификатор оплаты': 'payment_identifier',
  'провайдер токена': 'token_provider',
  'id торговца': 'merchant_id',
  'страна торговца': 'merchant_country',
  'компания торговца': 'merchant_company',
  'сумма после конвертации': 'converted_amount',
  'валюта после конвертации': 'converted_currency',
  'id шлюза': 'gateway_id',
  'рекуррентный тип': 'recurring_type',
  'card bin (8)': 'card_bin_8',
  'код банка': 'bank_code',
  'код ответа': 'response_code',
  'курс конвертации': 'conversion_rate',
  'перечисленная сумма после конвертации': 'converted_payout',
  'сумма комиссий в валюте после конвертации': 'converted_commission',
};
```

### Дизайн таблицы (минималистичный)

```css
/* Sticky columns */
.sticky-col-1 { position: sticky; left: 0; z-index: 10; }
.sticky-col-2 { position: sticky; left: 40px; z-index: 10; }

/* Row selection */
.row-selected { background: rgba(59, 130, 246, 0.1); }

/* Copy on click */
.cell-copyable { cursor: pointer; }
.cell-copyable:hover { background: rgba(255,255,255,0.05); }
```

---

## DoD (Definition of Done)

1. ✅ Новая вкладка "Выписка BePaid" видна в меню Платежи
2. ✅ Таблица `bepaid_statement_rows` создана в БД
3. ✅ Импорт Excel работает (все 60 колонок)
4. ✅ Upsert логика: повторный импорт обновляет, не дублирует
5. ✅ Сводка показывает агрегации как в файле bePaid
6. ✅ Выделение строк работает с динамической суммой
7. ✅ Поиск по любому полю работает
8. ✅ Горизонтальный скролл с sticky колонками
9. ✅ Копирование ячеек работает (toast "Скопировано")
10. ✅ Стиль минималистичный, соответствует платформе
