
# План: Исправление навигации, хлебных крошек и удаление тестовых данных

## Выявленные проблемы (по порядку)

### 1. Хлебные крошки показывают "Уроки без модулей"
**Проблема:** На скриншоте `image-703.png` видно две системы хлебных крошек:
- Глобальная (вверху): `🏠 > База знаний > vypusk-100`
- Локальная (внутри страницы): `База знаний > Уроки без модулей > Выпуск №100`

Контейнер-модуль `container-knowledge-videos` имеет title "Уроки без модулей" — это техническое название, которое не должно показываться пользователям.

**Решение:**
- В `LibraryLesson.tsx` и `LibraryModule.tsx`: если модуль `is_container = true`, НЕ показывать его в хлебных крошках
- Хлебные крошки для standalone-уроков: `База знаний > Выпуск №100` (без промежуточного модуля)

---

### 2. Кнопка "К списку" ведёт на модуль-контейнер
**Проблема:** Кнопка на странице урока (строка 175-182 в `LibraryLesson.tsx`) ведёт на `/library/${moduleSlug}`, что для контейнера показывает страницу модуля.

**Решение:**
- Если `module.is_container = true`, кнопка "К списку" должна вести на `getMenuSectionPath(module.menu_section_key)` (например, `/knowledge`)

---

### 3. Дубли "База знаний"
**Проверка БД:** В `user_menu_sections` нет дублей — есть только одна запись с `key: knowledge`.

На скриншоте `image-704.png` показана страница `/library` (Library.tsx), а НЕ `/knowledge` (Knowledge.tsx).

**Проблема:** Хлебные крошки в `DashboardBreadcrumbs.tsx` (строка 23) определяют `/library` как "База знаний":
```typescript
"/library": "База знаний",
```

Это создаёт путаницу: две разные страницы называются одинаково.

**Решение:**
- `/library` переименовать в "Тренинги" или "Библиотека курсов"
- Или: если `/library` не используется (пользователь говорит "удали её"), перенаправлять `/library` на `/knowledge`

---

### 4. Тестовые выпуски #37-#42
**Проверка:** В БД есть только "Выпуск №100". Выпуски #37-#42 — это **mock-данные** в `Knowledge.tsx` (строки 93-100):

```typescript
const mockEpisodes = [
  { id: "42", title: "Выпуск #42", ... },
  { id: "41", title: "Выпуск #41", ... },
  ...
  { id: "37", title: "Выпуск #37", ... },
];
```

**Решение:** Удалить `mockEpisodes` и компонент `VideosContent`, который их отображает. Показывать только реальные уроки из базы данных.

---

### 5. Плашка "Выпуск #1" на карточках
**Проблема:** В `Knowledge.tsx` (строка 337-343) номер выпуска вычисляется автоматически:

```tsx
<LessonCard 
  key={lesson.id} 
  lesson={lesson} 
  moduleSlug={containerModuleSlug}
  episodeNumber={standaloneLessons.length - index}
/>
```

Для единственного урока это даёт `1` (1 - 0 = 1).

**Решение:**
1. **Вариант А:** Добавить поле `episode_number` в `training_lessons` и показывать только если заполнено
2. **Вариант Б (проще):** Не показывать номер выпуска вообще, убрать пропс `episodeNumber`
3. **Вариант В:** Добавить в настройки урока поле "Номер выпуска" (опционально)

---

### 6. AI-генерация обложек для уроков
**Текущее состояние:** Генерация работает только для модулей (`ModuleFormFields.tsx`).

**Решение:** Добавить ту же функциональность в:
- `LessonFormFieldsSimple.tsx` — для мастера создания урока
- `AdminTrainingLessons.tsx` — для редактирования урока

---

### 7. Кнопка "На сайт" ведёт в личный кабинет
**Проблема:** В `Learning.tsx` (строки 48-120) product для "Клуб «Буква Закона»":

```typescript
{
  title: "Клуб «Буква Закона»",
  purchaseLink: "/",  // <-- ведёт на главную страницу ЛК
  courseSlug: "club",
  ...
}
```

Кнопка "На сайт" вызывает `handleGoToSite()`, который для `purchaseLink: "/"` делает `navigate("/")` — это redirect на dashboard.

**Решение:**
- Изменить `purchaseLink` на `"https://club.gorbova.by"` для внешнего сайта
- Или: убрать кнопку "На сайт" для этого продукта, оставить только "Открыть курс"

---

### 8. "Открыть курс" показывает "Модуль не найден"
**Проблема:** `handleOpenCourse()` ведёт на `/library/club`, но модуля с `slug: "club"` не существует в БД.

**Решение:**
- Либо создать модуль `club` для контента клуба
- Либо: кнопка "Открыть курс" для клуба должна вести на `/knowledge` (база знаний)

---

### 9. Возврат из "Панели управления" должен быть на /dashboard
**Текущее состояние:** В `AdminSidebar.tsx` кнопка "Назад к сайту" ведёт куда?

Нужно проверить и убедиться, что выход из админки ведёт на `/dashboard`.

---

## Файлы для изменения

| Файл | Изменения |
|------|-----------|
| `src/pages/LibraryLesson.tsx` | Скрыть контейнер-модуль в хлебных крошках; изменить "К списку" для контейнеров |
| `src/pages/LibraryModule.tsx` | Если контейнер, перенаправить на секцию вместо показа страницы модуля |
| `src/pages/Knowledge.tsx` | Удалить mock-данные (`mockEpisodes`, `VideosContent`); убрать автоматический `episodeNumber` |
| `src/pages/Learning.tsx` | Исправить `purchaseLink` для клуба → `/knowledge` или внешний URL |
| `src/components/layout/DashboardBreadcrumbs.tsx` | Изменить `/library` на "Библиотека курсов" (или redirect) |
| `src/components/admin/trainings/LessonFormFieldsSimple.tsx` | Добавить поле thumbnail + кнопку AI-генерации |
| `src/pages/admin/AdminTrainingLessons.tsx` | Добавить AI-генерацию обложки в форму редактирования |
| `src/components/training/LessonCard.tsx` | Показывать `episodeNumber` только если он передан и > 0 |
| `src/components/admin/trainings/ContentCreationWizard.tsx` | Изменить title контейнера с "Уроки: {key}" на скрытое имя |

---

## Детали реализации

### Хлебные крошки без контейнера

В `LibraryLesson.tsx` (строки 132-146):

```typescript
// Текущий код:
<Link to={`/library/${moduleSlug}`} ...>{module.title}</Link>

// Новый код:
{!module.is_container && (
  <>
    <Link to={`/library/${moduleSlug}`} ...>{module.title}</Link>
    <ChevronRight className="h-4 w-4" />
  </>
)}
<span className="text-foreground">{currentLesson.title}</span>
```

### Кнопка "К списку" для контейнеров

```typescript
// Текущий код:
onClick={() => navigate(`/library/${moduleSlug}`)}

// Новый код:
onClick={() => {
  if (module.is_container) {
    navigate(getMenuSectionPath(module.menu_section_key));
  } else {
    navigate(`/library/${moduleSlug}`);
  }
}}
```

### Удаление mock-данных

В `Knowledge.tsx` удалить:
- `mockEpisodes` (строки 93-100)
- `VideosContent` компонент (строки 159-191)
- `MOCK_CONTENT_MAP["knowledge-videos"]` (строка 212-213)

### Исправление кнопки "На сайт" для клуба

В `Learning.tsx` изменить:

```typescript
// Текущий код:
purchaseLink: "/",

// Новый код:
purchaseLink: "https://club.gorbova.by", // или просто убрать кнопку "На сайт"
```

И для "Открыть курс":

```typescript
// Изменить courseSlug: "club" на реальный путь
// Или: обработать отдельно в handleOpenCourse()
const handleOpenCourse = () => {
  if (product.courseSlug === "club") {
    navigate("/knowledge"); // Клуб = База знаний
  } else {
    navigate(`/library/${product.courseSlug || product.id}`);
  }
};
```

---

## Результат после исправлений

1. **Хлебные крошки для standalone-уроков:** `🏠 > Знания > Выпуск №100` (без "Уроки без модулей")
2. **Кнопка "К списку"** ведёт обратно в секцию (`/knowledge`), а не на технический модуль
3. **Нет mock-данных** — только реальные уроки из БД
4. **Нет автоматического "Выпуск #1"** — номер показывается только если задан в настройках
5. **AI-генерация обложек** доступна для уроков
6. **"На сайт"** для клуба ведёт на внешний сайт (если нужен)
7. **"Открыть курс"** для клуба ведёт в `/knowledge`
8. **Нет путаницы** между `/library` и `/knowledge`
