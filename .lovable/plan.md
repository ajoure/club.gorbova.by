да, согласен, с учетом правок:

1. В `get_inbox_dialogs_v1` добавь поиск не только по `profiles.telegram_user_id::text`, но и нормализацию входной строки:
  &nbsp;
  &nbsp;
  - `trim()`
  - для username искать и с `@`, и без `@`
  - для numeric ID искать точное вхождение по нормализованной строке без пробелов  
  Это нужно, чтобы `julia19870524` и `@julia19870524` находились одинаково.
2. Во фронтенде зафиксируй единое правило:
  - если строка поиска пустая, работаем как сейчас со списком последних диалогов;
  - если строка поиска непустая, используем серверный поиск через RPC, а не локальный фильтр первых 100/200.  
  Иначе баг частично останется.
3. В `UnifiedInboxView.tsx` локальный `matchesSearch` не должен повторно “убивать” уже найденные сервером результаты по сокращённому индексу.  
Либо расширь индекс полностью, как написал, либо при активном серверном поиске не применяй дополнительное локальное отсеивание поверх RPC-результата.
4. Для legacy Telegram-вкладки и unified-вкладки поведение поиска должно быть одинаковым:
  - одинаковая нормализация строки,
  - одинаковая передача `p_search`,
  - одинаковая логика пустого/непустого поиска.  
  Иначе сотрудник найдёт контакт в одном месте и не найдёт в другом.
5. Лимит 200 подними только для режима без поиска.  
Для режима с поиском главное не лимит, а серверная фильтрация. Это надо явно зафиксировать в плане.
6. В DoD добавь ещё 2 проверки:
  - поиск по `@julia19870524`
  - поиск по подстроке телефона/email, если они есть в профиле  
  Потому что ты сам расширяешь unified search-index и это нужно проверить отдельно.
7. В verify добавь проверку на отсутствие регрессии:
  - пустой поиск по-прежнему показывает последние диалоги по свежести;
  - сортировка найденного диалога не ломается;
  - формат результата RPC не изменён.
8. Если debounce уже есть, не дублируй его. Если нет — добавь только на UI-слое, без изменения RPC-контракта.

&nbsp;

После этих правок план хороший.

План:

1. **Проблема**
  - Контакт Криштопик Юлия в базе есть, Telegram привязан: `@julia19870524`, Telegram ID `1312198533`.
  - Сообщения от неё тоже есть: найдено 110 Telegram-сообщений, последние — 3 июля.
  - В контакт-центре она не находится, потому что текущий UI загружает только первые 100 последних диалогов и затем ищет только внутри уже загруженного списка. У Юлии позиция по свежести примерно 111-я, поэтому она не попадает в локальный список и поиск пишет «Ничего не найдено».
  - Дополнительно unified-вкладка «Все сообщения» сейчас ищет только по имени, превью и источнику, но не по email/телефону/telegram_username/telegram_user_id.
2. **Диагностика**
  - Проверены существующие источники: `profiles`, `telegram_messages`, `support_tickets`, RPC `get_inbox_dialogs_v1`.
  - RPC `get_inbox_dialogs_v1` уже умеет серверный поиск по `full_name`, `email`, `phone`, `telegram_username`, но фронтенд передаёт `p_search: null`, поэтому эта возможность не используется.
  - В самой RPC не хватает поиска по числовому `telegram_user_id`, поэтому поиск по Telegram ID тоже может не находить контакт.
  - Ошибка не в правах администратора Екатерины: данные существуют, проблема в ограничении выборки и неполном поисковом индексе.
3. **Предлагаемое решение**
  - Включить серверный поиск для Telegram-диалогов: при вводе строки в поиске передавать её в `get_inbox_dialogs_v1`, а не искать только среди первых 100 строк.
  - Расширить RPC `get_inbox_dialogs_v1`, чтобы она искала также по `profiles.telegram_user_id::text`.
  - В unified-контакт-центре расширить локальную проверку `matchesSearch`: добавить поиск по Telegram username, Telegram numeric ID, profile/contact metadata, чтобы результаты не отсеивались после серверной загрузки.
  - Увеличить безопасный лимит загрузки для контакт-центра до максимума RPC — 200, чтобы админы видели больше недавних диалогов без поиска.
4. **Изменяемые компоненты**
  - База / RPC:
    - `public.get_inbox_dialogs_v1` — добавить поиск по `telegram_user_id`.
  - UI:
    - `src/hooks/useUnifiedInbox.ts` — добавить параметр поиска и прокинуть его в RPC.
    - `src/components/admin/communication/unified/UnifiedInboxView.tsx` — передавать search в hook и расширить локальный search-index.
    - `src/components/admin/communication/InboxTabContent.tsx` — для legacy Telegram-вкладки также использовать серверный поиск.
  - Автогенерируемые файлы не трогать вручную.
5. **Что не будет изменено**
  - Не меняю роли, права администраторов и RLS без доказанной необходимости.
  - Не меняю модель сообщений, импорт Telegram, карточки контактов, сделки и продукты.
  - Не делаю массовые UPDATE/DELETE по данным клиентов.
6. **Dry-run**
  - Перед изменениями проверить SQL-условие: контакт Криштопик Юлия находится по имени, username и Telegram ID через существующие данные.
  - Проверить, что изменение RPC только расширяет фильтр поиска и не меняет формат результата.
7. **Execute**
  - Создать миграцию с `CREATE OR REPLACE FUNCTION public.get_inbox_dialogs_v1(...)`, сохранив текущий контракт результата и добавив `p.telegram_user_id::text ILIKE ...`.
  - Обновить UI-компоненты поиска минимальными патчами.
  - Не создавать новые таблицы/RPC/edge functions.
8. **STOP-guards**
  - Остановиться, если обнаружится, что контакт-центр использует другой source of truth для Telegram-диалогов.
  - Остановиться, если текущий контракт RPC используется в несовместимом месте.
  - Не выполнять массовые изменения данных.
  - Не менять политики доступа без отдельной диагностики.
9. **DoD**
  - Поиск в контакт-центре находит Криштопик Юлию по:
    - `криштопик`
    - `julia19870524`
    - `1312198533`
  - Админ видит её диалог и последние сообщения.
  - Контакт не пропадает из-за лимита первых 100 диалогов.
  - Проверка подтверждена backend-запросом и UI/preview-тестом.
10. **Риски и зависимости**
  - Серверный поиск увеличит число RPC-запросов при вводе; при необходимости добавлю debounce, если его нет в текущем компоненте.
  - RPC ограничивает выдачу максимум 200 строк — для конкретного поиска достаточно, но это не полноценная пагинация всех диалогов.
  - Если сотрудники ищут одновременно во вкладке «Все» и конкретно «Telegram», нужно поправить оба пути, чтобы поведение было одинаковым.