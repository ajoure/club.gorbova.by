# План: база знаний «Ценный бухгалтер» — этап 1

Режим выполнения этого отчёта: READ-ONLY. Ни код, ни база, ни настройки, ни Storage
не изменялись; jobs, AI/STT вызовы, deploy/Publish и сообщения клиентам не запускались.

## 1. Отчёт READ-ONLY: что найдено

### Продукты семейства (все активные)

| Назначение | code | ID | Уроки | Модули |
|---|---|---|---:|---:|
| Основной курс 1 ступень 2.0 | cb20 | 7101ed3c-…-aa0660369b22 | 106 | 31 |
| 20 поток | prd_7222cb3152c3 | 3e43fb28-…-714731bdc630 | 92 (87 активных) | 29 |
| 21 поток | prd_8649986b7c9e | 2b7bf6d4-…-7f96c307c596 | 91 (84 активных) | 29 |
| 2 ступень, 3 поток | prd_0d01a2fdc477 | 87a8870f-…-faa76c3f2be3 | 39 | 4 |
| 9 отраслевых модулей-продуктов | cb_module_* / prd_08a84b2b7223 | — | 22 суммарно | 7 |
| Предзапись | prd_88985c67ff48 | 11309c6a-…-df6a342ea6eb | 0 | 0 |

Пустые сейчас: ПВТ, Строительство, Посредничество, Предзапись (0 уроков).
Иерархия модулей: всего 178 модулей, 150 с `parent_module_id`, 1 контейнер.
Тарифы 4 основных продуктов: 18, все активные.

### Видео и аудио

- Видеоблоки в дереве ЦБ: 227; уникальных источников Kinescope: **131**
  (потоки во многом переиспользуют одни и те же записи — это ключевая экономия).
- Провайдер везде один — Kinescope, ссылки хранятся в `lesson_blocks.content.url`.
  Ни у одного урока не заполнены `training_lessons.video_url` / `audio_url`.
- Аудиоблоки в ЦБ: 6, все из Storage (`storagePath`), без внешнего провайдера.
- Длительности видео в базе **нет ни одной** (`duration_minutes` пуст, у блоков
  `duration_seconds` заполнен ровно в 1 случае по всей базе). Длительности
  придётся получать из Kinescope на этапе dry-run — догадкой не заполняем.

### Тексты уроков

- `training_lessons.content` заполнен у **0 из 328** уроков четырёх основных продуктов.
- Отдельной таблицы расшифровок уроков в схеме **нет** (`transcript*` есть только
  у `calls` и у эфиров). Покрытие расшифровками уроков = 0%.
- Текстовый материал есть только в блоках: `text` 91, `heading` 75, `html_raw` 66
  по всей базе (не только ЦБ) — как источник знаний это несопоставимо с видео.

### Механизм расшифровки, который уже существует

- Только для эфиров: `live_event_audio_assets` (6 записей, все `ready`, бакет
  `live-event-media`, аудио копируется по `source_video_id`), клиентские джобы
  `live_event_client_transcription_jobs` (1 `ready`, 1 `transcribing`) с
  частями `…_job_parts` (окна, попытки, коды ошибок), результат —
  `live_event_transcripts` (2 записи, обе `ready`, есть summary/key_points/DOCX).
- Уроки этим механизмом **не поддерживаются**: у джоба обязательный
  `live_event_id`, привязки к `training_lessons` нет.
- Хранилища знаний: `ai_prompt_packages` 18, `ai_prompt_attachments` 6 (все
  `ready`, с `extracted_text`), бакет `prompt-attachments`. Отдельного индекса
  для курса нет (`legal_document_search_chunks` — про законодательство).

### Отзывы (только агрегаты, без ПД)

- Комментарии эфиров: 3 938 всего, упоминаний «ценн… бухгалтер» — 3.
- Сообщения Telegram: 22 499 всего, упоминаний «ценн… бухгалтер» — 1 216.
- Архив постов канала: 2 записи. Отдельной таблицы отзывов/NPS нет.
  Telegram — единственный реально массовый источник; там нужна ручная выборка
  и согласование владельца, автоматически как отзывы это считать нельзя.

### Главные выводы

1. Готовых текстов для переиспользования практически нет: обрабатывать нужно
   131 уникальное видео, а не 227 блоков.
2. Уроки не поддерживаются текущим сервисом расшифровки — нужны изменения схемы
   и кода (пишет Codex в GitHub).
3. Длительности и суммарная стоимость сейчас неизвестны и определяются только
   dry-run по Kinescope.

## 2. Что нужно добавить (для Codex, GitHub)

- Таблица расшифровок уроков (`lesson_id`, `source_video_id`, `revision`,
  `status`, `transcript_text`, `chars`, `duration_ms`, `error_code`, аудит),
  GRANT + RLS: чтение только админ/service_role, публичного доступа нет.
- Дедуп по `source_video_id`: одна расшифровка переиспользуется всеми уроками
  всех потоков, ссылающимися на то же видео.
- Обобщение существующих джобов: `live_event_id` перестаёт быть обязательным,
  добавляется альтернативная привязка к уроку; части, окна, попытки, heartbeat
  и коды ошибок остаются как есть — возобновление после сбоя уже работает.
- Продажная витрина хранится отдельной таблицей утверждённых тем/результатов,
  ссылается на урок/модуль, но **не содержит** фрагментов платного текста.

## 3. Порядок будущего запуска (только после отдельного EXECUTE)

1. Dry-run: реестр 131 видео, точные длительности и байты из Kinescope,
   ожидаемое число частей и оценка расходов; ни одного вызова STT.
2. Проба одного урока: одно видео, проверка сохранения, символов, языка,
   повтор — 0 изменений.
3. Малые идемпотентные партии (≤10 видео), между партиями read-back и STOP при
   первом новом расхождении; повтор партии не создаёт дублей.
4. Полный корпус — закрытый, отдельно от sales projection; в продажный слой
   попадают только вручную утверждённые формулировки.

## 4. Границы

- PR446 (OPEN, HEAD 5654e739…) остаётся оффлайн-утилитами; runtime не меняется.
- Предыдущий scope «Продукты 3» и его HOLD-контакты не трогаются.
- Никаких grants, платежей, сообщений, Telegram-операций, deploy и Publish.
- Машиночитаемый реестр метаданных (ID видео/уроков/модулей, ревизии, флаги
  покрытия, без ссылок, signed URL и ПД) выгружается файлом после одобрения —
  сейчас, в плановом режиме, файлы не создаются.
