# да, согласен, с учетом правок:

1. Не ограничивай только `/consultation` в хардкоде. Сделай **reuse-first**:
  - в `UniversalPricingSection` добавить явный prop/layout-режим, например `layout: "auto" | "vertical-grid"`;
  - default оставить `"auto"` без изменений;
  - `vertical-grid` должен быть reusable и доступен для других страниц/блоков в будущем, без page-specific логики внутри компонента.
2. Зафиксируй **точные правила раскладки**:
  - mobile `< md`: **1 карточка в ряд**;
  - desktop `>= md`: **2 карточки в ряд**;
  - без карусели, без dots, без стрелок, без horizontal scroll;
  - контейнер центрированный, с нормальным `gap`;
  - не 3 и не 4 колонки на широком экране — именно **2 колонки максимум**.
3. Отдельно зафиксируй **одинаковую высоту карточек** как обязательный DoD:
  - все карточки в рамках grid должны быть одной высоты;
  - кнопки/CTA в карточках должны визуально стоять на одном уровне;
  - длинные тексты не должны ломать высоту соседних карточек;
  - при 2×2 сетке оба ряда выглядят ровно.
4. В плане явно пропиши, что **сохраняется текущая логика оплаты без единого нового пути**:
  - тот же `productId`;
  - тот же `offerId`;
  - тот же `PaymentDialog`;
  - тот же checkout flow;
  - никаких новых EF/RPC/routes;
  - никаких изменений в данных тарифов и продукта.
5. Добавь явный verify не только на открытие модалки, но и на **правильный binding**:
  - клик по каждой карточке передаёт корректный `offerId`;
  - CTA не теряет популярный тариф / badge / цену / old price / скидки, если они есть;
  - порядок тарифов не меняется.
6. Не трогай `TariffCarouselGrid` без необходимости. Если можно, просто не использовать его в режиме `vertical-grid`. В плане прямо укажи:
  - shared carousel logic остаётся как есть;
  - для нового layout используется отдельная ветка рендера в `UniversalPricingSection`;
  - остальные страницы продолжают жить на текущем `auto`-поведении.
7. Добавь **regression scope**:
  - `/club`
  - `/course-accountant`
  - все builder-страницы, где используется `UniversalPricingSection` / pricing block
  - проверить, что нигде больше layout случайно не переключился.
8. В verify добавь **3 обязательных viewport proof**:
  - 375×812 — 1 колонка;
  - 768×1024 — 2 колонки;
  - 1280×720 — 2 колонки;
  - на всех трёх: нет горизонтального скролла, карточки одинаковой высоты, CTA работает.
9. Добавь **STOP-guard**:
  - не менять `TariffCard` API;
  - не менять `PaymentDialog` API;
  - не менять `usePublicProduct`;
  - не менять БД/миграции;
  - не менять site-builder pricing flow;
  - не смешивать этот патч с другими визуальными правками консультации.
10. Финальный DoD переформулируй строже:

- на `/consultation` тарифы всегда рендерятся grid-режимом 1/2/2;
- карусель на этой странице отсутствует полностью;
- все карточки одинаковой высоты;
- оплата проходит по существующему каноническому пути;
- остальные страницы не изменились;
- приложены 3 скрина по viewport + 1 proof открытия `PaymentDialog` с корректным offer binding.
- &nbsp;
- План: PATCH CONSULTATION-TARIFFS-VERTICAL — вертикальная сетка тарифов на /consultation

## 1. Diagnose

На странице `/consultation` сейчас 4 тарифа → `TariffCarouselGrid` автоматически переключается в режим карусели (count > 3). На мобильной/планшетной версии это даёт горизонтальный скролл с обрезанными соседними карточками (видно на скриншоте: «...сультация», «Срочная консультация», «Помощь...»).

Пользователь хочет:

- **Mobile (< md)**: вертикальный список — 1 карточка в ряд, скролл страницы вниз.
- **Tablet/Desktop (≥ md)**: 2 карточки в ряд, скролл страницы вниз.
- Никаких каруселей на этой конкретной странице.

`root_cause = carousel_mode_forced_by_count>3_on_consultation`.

## 2. Бизнес-правило

- Изменения **только визуальные** на `/consultation`.
- Динамическая привязка к продукту «Платная консультация» (`usePublicProduct`) сохраняется.
- Логика выбора оффера, `PaymentDialog`, фильтрация тарифов, ID-first контракт — **не трогаются**.
- Поведение `TariffCarouselGrid` на других страницах (Club, CourseAccountant, site-renderer блоки) — **не меняется**.

## 3. Изменения

### A. `src/components/landing/UniversalPricingSection.tsx`

- Добавить опциональный prop `layout?: "auto" | "vertical-grid"` (default `"auto"` — текущее поведение).
- При `layout="vertical-grid"` вместо `<TariffCarouselGrid>` рендерить простую CSS-сетку:
  ```
  grid grid-cols-1 md:grid-cols-2 gap-6 max-w-5xl mx-auto items-stretch
  ```
  с теми же `<AnimatedSection>` + `<TariffCard>` внутри (никаких изменений в карточке).
- Высота карточек выравнивается через `items-stretch` + `h-full` на `AnimatedSection` (как уже сделано) — соответствует `tariff-card-rendering-standard` (h-full, одинаковая высота).

### B. `src/pages/Consultation.tsx`

- Передать `layout="vertical-grid"` в `<UniversalPricingSection>`.
- Больше ничего не менять.

## 4. Файлы


| Файл                                                 | Изменение                                        |
| ---------------------------------------------------- | ------------------------------------------------ |
| `src/components/landing/UniversalPricingSection.tsx` | новый prop `layout`, ветка vertical-grid рендера |
| `src/pages/Consultation.tsx`                         | прокинуть `layout="vertical-grid"`               |


## 5. Не трогаем

- `TariffCarouselGrid.tsx` — остаётся как есть для всех остальных страниц.
- `TariffCard.tsx`, `PaymentDialog.tsx`, `usePublicProduct.ts` — не меняются.
- Логику фильтрации `tariff_filter_mode` в `PricingSection.tsx` (site-renderer) — не трогаем.
- БД, RLS, edge functions, продукт «Платная консультация» и его тарифы — не трогаем.
- Другие страницы с тарифами (Club, CourseAccountant, CloseYear, лендинги конструктора).

## 6. Verify

1. `/consultation` на 375×812 (mobile): 4 карточки расположены вертикально друг под другом, страница скроллится вниз, горизонтального скролла нет, стрелок и точек карусели нет.
2. `/consultation` на 768×1024 (tablet): 2 колонки × 2 ряда, вертикальный скролл.
3. `/consultation` на 1280×720 (desktop): 2 колонки × 2 ряда, центрировано.
4. Клик «Оплатить» на любой карточке открывает `PaymentDialog` с правильным `productId` + `offerId` (ID-first контракт).
5. Другие страницы с тарифами (например `/club`, `/course-accountant`) — рендер не изменился, карусель работает по-прежнему.
6. Высота всех 4 карточек одинаковая в каждом ряду.

## 7. STOP-guards

- НЕ менять props `TariffCard`, `PaymentDialog`, не трогать payment flow.
- НЕ удалять `TariffCarouselGrid` и не менять его поведение по умолчанию.
- НЕ менять данные продукта/тарифов в БД.
- Если `tariffs.length === 0` — fallback «Тарифы временно недоступны» сохраняется.

## 8. DoD

1. На `/consultation` mobile/tablet/desktop тарифы рендерятся вертикальной сеткой (1 / 2 / 2 колонки).
2. Карусель на `/consultation` отсутствует полностью (нет точек, стрелок, горизонтального скролла).
3. Оплата по любой карточке проходит штатно (ID-first, через `PaymentDialog`).
4. Карусельный режим на остальных страницах с 4+ тарифами не сломан.
5. Скриншоты mobile 375×812, tablet 768×1024, desktop 1280×720 приложены в отчёте.