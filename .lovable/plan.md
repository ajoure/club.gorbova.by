да, согласен, с учетом правок:

1. **Разделить патч на два независимых блока внутри одного плана**
  &nbsp;
  Сейчас в один план включены:
  - исправление резолвера документов;
  - улучшение обработки edge-function ошибок в UI.
  Это допустимо, потому что оба блока связаны с жалобой пользователя, но в плане нужно явно разделить:
  ```text
  PATCH A — Document resolver correctness:
  - ru-inflection.ts
  - typed-tokens-resolver.ts
  - canonical-document-generate-strict preview/proof

  PATCH B — Edge error normalization UX:
  - normalizeEdgeFunctionError.ts
  - useAiChat.ts
  - document-generation hooks/components with invoke()
  ```
  Для каждого блока должен быть отдельный DoD и отдельный proof. Это не новый pipeline, а точечная правка существующих функций, что соответствует safe workflow Diagnose → Dry-run → Execute → Verify.  
2. **Добавить обязательный языковой блок в начало плана**
  &nbsp;
  В начало вставить:
  ```text
  План должен быть составлен на русском языке.
  Отчет о выполненной работе должен быть составлен на русском языке.
  Вся переписка, пояснения, diff-summary, proof и результаты должны быть только на русском языке.
  ```
  Это обязательное правило для планов/отчетов проекта.  
3. **Уточнить, что “Функция временно недоступна” не чинит backend, а чинит диагностику UI**
  &nbsp;
  Добавить:
4. `normalizeEdgeFunctionError` **должен быть backward-compatible**
  &nbsp;
  Добавить guard:
5. **Учесть, что body stream можно прочитать только один раз**
  &nbsp;
  В план добавить:
6. **Применение async normalizer — только в местах, где можно await**
  &nbsp;
  Добавить:
7. **Для 402/429 определить точные сообщения**
  &nbsp;
  В DoD PATCH B добавить:
8. **Не смешивать resolver proof и UI error proof**
  &nbsp;
  В proof-файле лучше сделать два отдельных раздела:
9. `canonicalizeLegalEntity` **должен нормализовать** `rawOrgForm`**, а не только** `rawName/fullName`
  &nbsp;
  План это уже содержит, но надо усилить как DoD:
10. **Полная форма собственности должна сворачиваться независимо от регистра**

Добавить тесты:

```text
'закрытое акционерное общество' → ЗАО
'ЗАКРЫТОЕ АКЦИОНЕРНОЕ ОБЩЕСТВО' → ЗАО
'Закрытое   акционерное   общество' → ЗАО
```

11. `short_name` **никогда не должен использовать long-form**

Вынести в STOP/DoD:

```text
STOP, если FLD-000345 после патча всё ещё содержит:
- Закрытое акционерное общество
- Общество с ограниченной ответственностью
- любую long-form форму собственности

FLD-000345 всегда short org form + «name».
```

Это соответствует модели computed: краткое название — вычисляемое значение из карточки юрлица, а не новый primary input.  

12. `format=long` **не должен применяться к** `short_name` **даже случайно**

Добавить тест:

```text
FLD-000345|format=long → либо игнорирует format и возвращает ЗАО «АЖУР инкам», либо явно помечает modifier_not_supported.
Не допускается: Закрытое акционерное общество «АЖУР инкам».
```

13. **Проверить не только заказчика, но и исполнителя**

В DoD добавить:

```text
Для executor:
- FLD-000368 → ЗАО «АЖУР инкам»
- FLD-000363 genitive/nominative по должности исполнителя → мужской род
- executor.leg.short_name остается ЗАО «АЖУР инкам»
```

14. `normalizeMasculinePosition` **должен применяться и без падежа**

План содержит открытый вопрос, ответ — **да**. Зафиксировать:

```text
normalizeMasculinePosition() применяется всегда для director_position:
- до case;
- без case;
- для legacy customer.director_position / executor.director_position;
- для FLD-000339 / FLD-000363 и симметричных токенов.

Женская форма должности в документе не выводится.
```

15. **Исправить конкретное правило** `-щий/-ший/-чий`

Добавить точный ожидаемый алгоритм:

```text
Для слов на:
- ющий / ящий / ащий / ший / чий

родительный:
- убрать финальное `ий`;
- добавить `его`;
- Управляющий → Управляющего

дательный:
- + `ему`;
творительный:
- + `им`;
предложный:
- + `ем`.

Не должно получаться `Управляющия`.
```

Но лучше для частых должностей иметь словарь, чтобы не полагаться только на суффиксные эвристики.

16. **Добавить тест “Управляющий” по всем падежам**

В Verify добавить:

```text
Управляющий:
- nominative: Управляющий
- genitive: Управляющего
- dative: Управляющему
- accusative: Управляющего / Управляющий — если движок различает одушевленность; если нет, зафиксировать текущий accepted behavior
- instrumental: Управляющим
- prepositional: Управляющем
```

17. **Не деплоить без** `deno check` **и unit-like dry script**

В Execute добавить gate:

```text
До deploy:
- deno check ru-inflection.ts
- deno check typed-tokens-resolver.ts
- deno check canonical-document-generate-strict/index.ts, если импорт менялся
- local dry script по canonicalizeLegalEntity / normalizeMasculinePosition / inflectRu
```

18. `mode=preview` **должен быть первым доказательством, PDF — только после preview**

В Execute/Verify усилить:

```text
Не генерировать новый файл/PDF, пока mode=preview не покажет правильные resolved_tokens/source_trace.
Если preview неверный — остановиться и исправить resolver.
```

19. **Добавить source_trace proof по каждому исправленному FLD**

В DoD добавить:

```text
В source_trace / resolved_tokens показать:
- FLD-000339 raw value;
- normalizeMasculinePosition applied;
- case applied;
- final value;
- FLD-000345 rawOrgForm;
- normalizedOrgForm;
- nameClean;
- final short_name.
```

Мастер-план требует фиксировать, что именно было подставлено, какие токены использовались, warnings и source_trace.  

20. **Не создавать proof через production-документ без явного тестового контекста**

Добавить:

```text
Новый документ для proof создавать только на тестовом заказе `#ORD-TEST-MPFAPNTP`.
Не пересоздавать автоматически реальные документы клиентов.
```

21. **Внести в proof скрин результата PDF крупно**

Добавить:

```text
Скрин должен показывать читаемо:
- строку в шапке с `ЗАО «АЖУР инкам» в лице Управляющего...`;
- блок `ЗАКАЗЧИК`;
- при необходимости crop/zoom.
```

22. **Формулировку “Backend Lovable Cloud доступен” не считать полным proof**

Добавить:

```text
Для проблемы «Функция временно недоступна» proof должен быть не “backend доступен”, а:
- reproduced пример старого generic error;
- parsed JSON body после фикса;
- UI показывает конкретный message/code.
```

23. **Не трогать** `client.ts` **/** `types.ts` **— верно, оставить**

Этот guard корректен. Сохраняем.

24. **Финальный отчет должен быть правильно маркирован**

Добавить:

```text
Отчет должен начинаться:
Отчет о выполненной работе: Исправление FLD краткого названия, должности руководителя и edge error messages
```

Правила проекта требуют не смешивать план и отчет и проверять их по-разному.  

25. **Уточнить scope по “Функция временно недоступна”**

Если исправление edge error normalization окажется шире, чем 1–2 call-sites, перенести остаток в follow-up:

```text
Deferred:
- полный audit всех supabase.functions.invoke call-sites;
- единый async error wrapper для всех edge-functions;
- покрытие всех UI toast/error states.
```

Правила длинных спринтов требуют фиксировать не блокирующие хвосты в deferred list, а не тормозить основной scope.  

## **Короткий ответ исполнителю**

```text
Да, подтверждаю: должность в именительном падеже тоже должна выводиться в мужском роде. Если в БД сохранено «Управляющая», то токен без case должен дать «Управляющий», а с родительным падежом — «Управляющего». Женская форма должности в документах не допускается.

План в целом корректный, но обязательно добавь:
1. PATCH A/PATCH B как два отдельных блока proof.
2. short_name никогда не использует long-form: FLD-000345 всегда ЗАО «АЖУР инкам».
3. format=long применяется только к org_form, не к short_name.
4. normalizeMasculinePosition применяется всегда — и без case, и с case.
5. Сначала mode=preview и source_trace, потом генерация PDF.
```

После этих правок план можно отдавать в выполнение.

исправь все в режиме симуляции и подтрверди скринами.

План:

1. **Проблема**

- В опубликованном сайте периодически показывается «Функция временно недоступна», особенно в AI-общении и генерации.
- В документе счёт-акт после предыдущих правок фактически осталось две ошибки:
  - `Управляющий` в родительном падеже рендерится как `Управляющия`, должно быть `Управляющего`.
  - `Заказчик ЮЛ: краткое название` (`FLD-000345`) рендерится как `Закрытое акционерное общество «АЖУР инкам»`, должно быть `ЗАО «АЖУР инкам»`.

2. **Диагностика**

- Backend Lovable Cloud сейчас доступен; общая деградация backend не подтверждена.
- По текущему заказу `#ORD-TEST-MPFAPNTP` найден реальный proof в `ai_generated_documents`:
  - `FLD-000339|case=genitive` → `Управляющия`.
  - `FLD-000339` без case → `Управляющий`.
  - `FLD-000345` → `Закрытое акционерное общество «АЖУР инкам»`.
  - `executor.leg.short_name` уже корректно даёт `ЗАО «АЖУР инкам»`.
- Причина `Управляющия`: в `ru-inflection.ts` правило для слов на `-щий/-ший/-чий` режет только 2 символа (`ий`) и получает основу `Управляющ`, затем добавляет `его`; на практике из-за порядка правил/суффикса получилось неверное `Управляющия`. Нужно отдельное безопасное правило для `управляющий`/адъективных должностей.
- Причина `FLD-000345`: `client_legal_details.leg_org_form` хранит полную форму `Закрытое акционерное общество`, а `canonicalizeLegalEntity()` сейчас принимает явную `rawOrgForm` как есть и не нормализует её в короткую форму. Поэтому short_name строится из полной формы.
- Ошибка «Функция временно недоступна» в UI появляется потому, что `normalizeEdgeFunctionError()` не извлекает JSON-body из `FunctionsHttpError.context` асинхронно. В итоге реальные коды (`no_real_payment`, `pdf_conversion_failed`, `402 AI limit`, etc.) маскируются общим текстом. В логах также есть 402 по `gorbova-ai-chat`, который фронт может не распознать из-за generic wrapper.

3. **Предлагаемое решение**

- `ru-inflection.ts`:
  - Исправить склонение адъективных должностей на `-щий/-ший/-чий`: `Управляющий` → `Управляющего`, `Управляющему`, `Управляющим`, `Управляющем`.
  - Добавить/проверить unit-like dry cases внутри proof: `Управляющая` → normalize → `Управляющий`; genitive → `Управляющего`.
- `typed-tokens-resolver.ts`:
  - В `canonicalizeLegalEntity()` нормализовать `rawOrgForm`, если туда пришла полная форма: `Закрытое акционерное общество` → `ЗАО`, `Общество с ограниченной ответственностью` → `ООО`, и т.д.
  - Строить `short_name` только из короткой формы: `ЗАО «АЖУР инкам»`.
  - `format=long` оставить только для `*.leg.org_form`, не применять к `short_name`.
- `normalizeEdgeFunctionError.ts` и места вызова:
  - Добавить асинхронный helper, который читает `error.context.json()/text()` и передаёт parsed fallback в существующий нормализатор.
  - Применить его в UI-вызовах генерации документов и AI-чата, чтобы показывались реальные причины, а не общий «временно недоступна».
- `useAiChat.ts`:
  - Не определять 402/429 только по `error.message`; использовать нормализованный текст и/или parsed body.
  - Для 402 показывать «Лимит AI исчерпан», для 429 — rate limit, для остальных — реальную нормализованную причину.

4. **Изменяемые компоненты**

- Edge/shared:
  - `supabase/functions/_shared/ru-inflection.ts`
  - `supabase/functions/_shared/typed-tokens-resolver.ts`
  - при необходимости `supabase/functions/canonical-document-generate-strict/index.ts` только если потребуется добавить trace/debug-proof, без изменения архитектуры генерации.
- Frontend:
  - `src/utils/normalizeEdgeFunctionError.ts`
  - `src/hooks/useAiChat.ts`
  - хуки/компоненты документа, где вызывается `canonical-document-generate-strict` и сейчас показывается generic error.
- Proof:
  - новый файл в `.lovable/proofs/` с SQL/edge preview/output и ссылками на сгенерированный документ.

5. **Что не будет изменено**

- Не создавать новые placeholder/FLD.
- Не менять `fields_registry`, `document_token_registry`, шаблонную архитектуру и SOT.
- Не менять таблицы, RLS, RPC, cron/jobs.
- Не править реквизиты пользователя вручную как «решение» бага.
- Не трогать `src/integrations/supabase/client.ts` и `types.ts`.

6. **Dry-run**

- До патча зафиксировать контрольные значения для заказа `#ORD-TEST-MPFAPNTP`:
  - `FLD-000339|case=genitive` сейчас неверный.
  - `FLD-000345` сейчас неверный.
- После патча вызвать `canonical-document-generate-strict` в `mode=preview` на том же заказе/шаблоне и проверить `resolved_tokens/source_trace` без создания нового файла.
- Прогнать локальный script/dry-check для функций:
  - `canonicalizeLegalEntity('Закрытое акционерное общество', 'АЖУР инкам')` → `org_form=ЗАО`, `short_name=ЗАО «АЖУР инкам»`.
  - `normalizeMasculinePosition('Управляющая')` → `Управляющий`.
  - `inflectRu('Управляющий','genitive',{forceGender:'m'})` → `Управляющего`.

7. **Execute**

- Внести минимальные точечные изменения в перечисленные файлы.
- Задеплоить затронутые backend functions/shared edge code, если потребуется для проверки published/preview.
- Создать/перегенерировать тестовый документ для заказа, где заказчик и исполнитель — реквизиты `АЖУР инкам`.
- Скачать PDF/DOCX, конвертировать/проверить страницу изображением, приложить скрины/артефакты proof.

8. **STOP-guards**

- Остановиться, если preview показывает, что шаблон использует не `FLD-000339/343/345`, а legacy или другой FLD.
- Остановиться, если у текущего шаблона активная версия изменилась и в ней нет нужных плейсхолдеров.
- Остановиться, если `mode=preview` не возвращает корректный trace или заказ не авторизуется.
- Не выполнять массовые UPDATE/DELETE.
- Не создавать новый документ как proof, если preview уже показывает несоответствие после патча — сначала исправить resolver.

9. **DoD**

- `mode=preview` для контрольного заказа показывает:
  - `{{field:FLD-000339}}` → `Управляющий`.
  - `{{field:FLD-000339|case=genitive}}` → `Управляющего`.
  - `{{field:FLD-000345}}` → `ЗАО «АЖУР инкам»`.
  - `{{field:FLD-000343}}` → `ЗАО`.
  - `{{field:FLD-000343|format=long}}` → `Закрытое акционерное общество`.
- Повторно сгенерированный PDF визуально проверен: в шапке и блоке `ЗАКАЗЧИК` видно `ЗАО «АЖУР инкам»` и `в лице Управляющего Коврижкина Алексея Игоревича` / в родительном падеже `Управляющего`, согласно шаблону.
- Для исполнителя с теми же реквизитами краткое название остаётся `ЗАО «АЖУР инкам»`.
- UI больше не маскирует реальные edge-function ошибки общим текстом, когда backend возвращает JSON с кодом/сообщением.
- Создан proof-файл `.lovable/proofs/...` с SQL/preview/generate evidence и скрином/описанием PDF.

10. **Риски и зависимости**

- Если опубликованный сайт использует старый деплой функции, нужно явно деплоить затронутую edge function после правки.
- Если лимит Lovable AI реально исчерпан, код не сможет «починить» лимит, но UI будет показывать точную причину вместо временной ошибки.
- Если Gotenberg/PDF-сервис временно падает, генерация может возвращать `pdf_conversion_failed`; UI должен показывать именно это.

11. **Требуется дополнительная информация**

- Не требуется для старта: заказ, шаблон, реквизиты и фактические неверные значения уже найдены в базе/trace.