да, согласен, с учетом правок:

1. Удалить показ t.code полностью, без скрытых tooltip/secondary text. Служебный код не должен отображаться редактору нигде в строке тарифа.
2. Для строки тарифа зафиксировать такой принцип верстки:
  - контейнер: flex items-start gap-2 min-w-0
  - чекбокс: shrink-0 mt-0.5
  - текстовая часть: flex-1 min-w-0
  - название тарифа: text-sm break-words
  - бейдж Популярный: shrink-0 whitespace-nowrap self-start
3. Если название тарифа длинное, оно должно переноситься в 2+ строки, а бейдж оставаться внутри карточки справа сверху. Нельзя допускать горизонтальный overflow ни у строки, ни у всей карточки.
4. Проверить не только 440px, но и 390px. Proof нужен на двух ширинах:  

  - 440px
  - 390px  
  На обеих ширинах бейдж не вылезает, кодов нет, чекбокс не съезжает.
5. Ничего не менять в логике:
  - pricingContentSchema
  - tariff_filter_mode
  - tariff_ids
  - PricingSection.tsx
  - EF / БД / checkout
6. В финальный proof приложить:
  - before/after скрин строки с Популярный
  - скрин на 440px
  - скрин на 390px
  - короткое подтверждение, что выбор тарифов и сохранение страницы не сломаны
7. Если в списке есть очень длинные названия тарифов, дополнительно проверить, что при выборе чекбокса строка не прыгает по высоте и бейдж не переносится посимвольно.

&nbsp;

&nbsp;

Пользователь жалуется на:

1. На скриншоте `IMG_3623.png` бейдж "Популярный" вылазит за правую границу карточки на узком экране (440px). Также `CONSULTATION_STANDARD`, `CONSULTATION_URGENT` — это `code` тарифа, который показывается моноширинным шрифтом справа.
2. Нужно убрать английские коды (`code`) — это служебная информация, не нужная редактору.

Смотрю текущий layout в `PricingBlockEditor.tsx` (строки чекбоксов): `flex items-center gap-2` + название (`flex-1`) + код (моноширинный) + бейдж "Популярный". На узком экране всё в одну строку — переполнение.

План:

## План: чистка дизайна списка тарифов в редакторе

### Что меняется

Файл: `src/components/admin/site-builder/blocks/PricingBlockEditor.tsx`, блок рендера чекбокс-строки тарифа.

1. **Убрать показ `t.code**` — служебный slug, редактору не нужен. Удалить блок `{t.code && <span>...</span>}`.
2. **Исправить переполнение бейджа "Популярный"**:
  - Сделать строку устойчивой к узким экранам: `flex items-start gap-2 min-w-0`.
  - Название тарифа: `flex-1 min-w-0 text-sm` с `break-words` — чтобы длинное название переносилось, а не толкало бейдж за край.
  - Бейдж "Популярный": `shrink-0` — никогда не сжимается и не вылазит.
3. **Выравнивание чекбокса**: `mt-0.5` чтобы при двухстрочном названии чекбокс оставался у первой строки.

### Что НЕ меняется

- Логика выбора, сохранения, фильтрации.
- Схема `pricingContentSchema`.
- Рендерер `PricingSection.tsx`.
- EF, БД, оплата.

### DoD

- На viewport 440px бейдж "Популярный" не выходит за границу карточки.
- Английские коды тарифов (`CONSULTATION_STANDARD`, `help`, `strategy` и т.п.) больше не отображаются в редакторе.
- Длинные названия тарифов корректно переносятся внутри строки.
- Чекбокс, название и бейдж визуально выровнены.
- Функциональность выбора тарифов не сломана.