да, согласен, с учетом правок:

1. **Не менять UNIQUE без dependency audit**
  - сначала проверить все ссылки/ожидания на `UNIQUE (task_id, notification_type)`;
  - только потом менять на `(task_id, notification_type, recipient_user_id)`;
  - старый constraint/index удалять только безопасной миграцией.
2. **Добавить anti-duplicate guard для** `assigned`
  - при UPDATE не создавать уведомление, если `OLD.assignee_user_id IS NOT DISTINCT FROM NEW.assignee_user_id`;
  - при INSERT/UPDATE использовать `ON CONFLICT DO NOTHING`.
3. **Проверить Telegram eligibility**
  - если у ответственного нет `telegram_user_id`, уведомление не должно падать;
  - статус/ошибка должны логироваться понятно.
4. **Remind presets**
  - если пользователь меняет `due_at`, пересчитать `remind_at`;
  - если `remind_at` стал в прошлом — показать warning или очистить reminder.
5. **Staff options**
  - исключать только чистых клиентов `user`;
  - если у пользователя есть `user + support/admin/...`, он должен попадать в список.
6. **DoD дополнить**
  - проверить сотрудника с ролью `support`;
  - проверить сотрудника с двумя ролями `user + staff`;
  - проверить контакт-клиента без staff-роли — не показывается.

Остальное можно выполнять.

&nbsp;

План:

## 1. Полный список сотрудников в «Ответственный»

Файл: `src/hooks/useStaffOptions.ts`

- Сейчас фильтр включает только коды `employee | admin | super_admin`. В БД реально существуют роли: `admin` (3), `super_admin` (2), `menedzher` (2), `support` (1), плюс `employee` на будущее. Роль `user` — обычные клиенты.
- Изменю правило: в staff-список попадают все пользователи, у которых есть хотя бы одна роль в `user_roles_v2` с кодом ≠ `user` (т.е. любой, у кого есть доступ к панели управления). Так же учтётся и любая новая служебная роль без правки кода.
- Фолбэк на «все профили» уберу: он маскирует ошибку RLS и показывает клиентов.
- В UI (`CreateCrmTaskDialog` / `EditCrmTaskDialog`) ничего не меняем — уже маппится по `staff.map`.

## 2. Симметричный лэйаут «Сделка / Контакт»

Файл: `src/components/admin/tasks/TaskRelationsField.tsx` (+ вызов в `CreateCrmTaskDialog` / `EditCrmTaskDialog`).

- Сейчас поля «Сделка» и «Контакт» рендерятся в вертикальную колонку, из-за чего «Контакт» уезжает вниз.
- Перестрою секцию в `grid-cols-2 gap-3`: слева — «Сделка», справа — «Контакт», обе на одной высоте под блоком «Ответственный». Выравнивание меток и высот полей — идентичное блоку «Дедлайн / Напомнить».
- Если один из relations lock-нут (создание из карточки), вторая колонка остаётся на своём месте, чтобы форма не «прыгала».

## 3. Пресеты напоминания вместо даты

Файлы:

- Новый компонент `src/components/admin/tasks/RemindOffsetSelect.tsx`
- `CreateCrmTaskDialog.tsx`, `EditCrmTaskDialog.tsx`

Заменяю `DateTimePickerField` в «Напомнить» на выпадающее меню пресетов относительно дедлайна:

```
Не напоминать
За 5 минут
За 15 минут
За 30 минут
За 1 час
За 3 часа
За 1 день
За 2 дня
За 3 дня
За неделю
Своё значение…
```

Логика:

- Значение хранится локально как `remind_offset_minutes` (или `null`).
- При submit: `remind_at = due_at - offset` (только если задан `due_at` и offset ≠ null). Если дедлайна нет — селект дизейблится с подсказкой «Сначала укажите дедлайн».
- «Своё значение…» открывает мини-поповер с двумя полями «число + единица (мин/час/день)».
- В `EditCrmTaskDialog` при открытии реконструирую пресет по разнице `due_at - remind_at`; если не совпадает ни с одним — показываю «Своё значение (Х мин)».
- Значения по-умолчанию из `crm_task_types.default_reminder_offset_minutes` продолжают работать.

Никаких миграций: поле `remind_at timestamptz` в БД остаётся.

## 4. Telegram-уведомление при постановке задачи

Файлы:

- Миграция `supabase/migrations/<ts>_crm_task_assigned_notify.sql`

Что делаю:

- Добавляю триггер `AFTER INSERT` и `AFTER UPDATE OF assignee_user_id` на `public.crm_tasks`, который вставляет строку в `crm_task_notifications`:
  - `notification_type = 'assigned'` (значение уже разрешено CHECK-констрейнтом),
  - `channel = 'telegram'`,
  - `recipient_user_id = NEW.assignee_user_id`,
  - `scheduled_at = now()`,
  - `status = 'pending'`,
  - `metadata = { source: 'task_created' | 'assignee_changed', previous_assignee: … }`.
- Условия: только если `assignee_user_id IS NOT NULL`, задача не `done|canceled`, и (для UPDATE) реально сменился ассайни.
- Уникальность: у таблицы уже есть `UNIQUE (task_id, notification_type)`. Для повторного переназначения это заблокирует дубль. Решение: расширяю уникальный индекс до `(task_id, notification_type, recipient_user_id)` через миграцию — сохранит идемпотентность и позволит отправить уведомление новому ответственному.

Файл: `supabase/functions/crm-task-notify-worker/index.ts`

- Добавляю ветку рендера текста для `notification_type = 'assigned'`:
  ```
  🆕 Вам назначена задача
  <Тип>: <Название>
  Описание …
  Дедлайн: …
  ID: …
  ```
  Ветки `reminder` / `overdue` остаются без изменений.
- Ничего в `crm_tasks_schedule_due_notifications` не трогаю — «assigned» вставляется триггером сразу с `scheduled_at = now()`, и уже существующий цикл выборки `status='pending' AND scheduled_at <= now()` подхватит.
- pg_cron уже тикает воркер каждую минуту (было настроено при v1) — отдельного расписания не нужно.

DoD:

- В «Новая задача» селект «Ответственный» показывает всех сотрудников с любой ролью админ-панели (admin, super_admin, menedzher, support, employee, …); клиенты (`user`) не появляются.
- «Сделка» и «Контакт» стоят в одной строке под «Ответственным», симметрично «Дедлайн / Напомнить».
- Поле «Напомнить» — селект пресетов, `remind_at` вычисляется от `due_at`; открытие существующей задачи корректно определяет пресет.
- При создании задачи с назначенным ответственным (или смене ответственного) — в Telegram ответственного приходит сообщение «Вам назначена задача …» (при условии, что у профиля привязан `telegram_user_id`).
- Существующие reminder / overdue-уведомления продолжают доставляться.
- Проверка: создаю задачу на себя → приходит TG-нотификация; смена ответственного → TG-нотификация уходит новому; выбор «за 1 час» + дедлайн через 2 часа → в `crm_task_notifications` появляется `reminder` со `scheduled_at = due_at - 1h`.