# План: разбор PAY-26-B9F8AA2916 (единственный реальный пропуск доступа)

Финальный аудит подтвердил: из 8 заказов без доступа реальный пропуск ровно один — PAY-26-B9F8AA2916 (4500.00, банковский перевод, 2026-07-24, продукт «Платная консультация»). Остальные 7 классифицированы как trial/служебные/нулевые/синтетические.

## Что предлагается сделать

1. Проверка перед действием (только чтение)
   - Подтвердить, что платёж 4500.00 provider `bank` живой (`succeeded`, не удалён) и не является дублем уже учтённого платежа по ORD-26-00200.
   - Подтвердить, что у пользователя нет активного доступа к продукту «Платная консультация» и нет отложенной выдачи.
   - Уточнить у владельца: услуга по этому платежу уже оказана офлайн или доступ в системе действительно должен быть выдан.

2. Если владелец подтверждает выдачу доступа
   - Идемпотентный вызов `grant-access-for-order` для этого заказа (тот же безопасный путь, что применялся к ORD-26-02848).
   - Read-back: entitlement `active`, одна запись в `access_grant_ledger`, отсутствие дублей подписок.
   - Одна запись в журнал аудита с указанием причины и результата.

3. Если услуга оказана офлайн
   - Изменений не вносить, зафиксировать классификацию заказа как «доступ не требуется» записью в аудит.

## Что не входит в объём

- Никаких изменений по остальным 7 заказам, по past_due-подпискам, провайдеру bePaid, платежам, возвратам, ролям, шаблонам, коду, миграциям, деплою и Publish.
- Отдельные monitoring findings (telegram-revoke, external-document-form 502, `uuid ~* unknown`, FK delete-user, charge-reminders) в этот план не входят и разбираются отдельной задачей.

## Технические детали

- Заказ: `9f36c068-6fac-4550-89ea-34ff54599bbd`, платёж `72e9d178…` (provider `bank`, 4500.00, `succeeded`).
- Продукт `9d0d6de8…`, тариф `a0f9ecc2…`; у пользователя entitlement по этому продукту `expired 2026-07-24 13:58 UTC` (от ORD-26-00200).
- STOP-условия: любой признак дубля платежа, коллизия `order_id` в entitlements или расхождение пользователя — остановиться и доложить.
