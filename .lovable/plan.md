План: закрытый импорт субтитров ЦБ20 «Конференция 5» (13.09.2026) как исторического источника

## Вывод проверки

Сейчас в проекте **нет** подходящего способа сохранить эту запись как закрытый исторический источник без урока. Проверены три существующих пути:

| Путь | Почему не подходит |
|---|---|
| Импорт проверенных субтитров курса | **Сама функция базы подходит.** Она только для служебной роли, работает с правами вызывающего, проверяет версию источника, отпечаток субтитров и качество, а привязку к уроку не проверяет. Урок требует только команда импорта: её пробный прогон и запись. |
| Расшифровка эфира в разделе «Эфиры» | Готовые субтитры этот путь не принимает. Ручная загрузка текста сразу отправляет его в ИИ за кратким итогом, а результат не попадает в корпус курса. |
| Редактор знаний | Считает источник готовым, только если у него есть привязка к уроку. Источник без такой привязки останется «не готов» и в продавца не попадёт. |

**Поправка к первой версии плана:** новая функция импорта не нужна. Действующую функцию проверенных субтитров можно вызвать повторно, как есть. Новыми будут только привязка к эфиру и отдельный режим команды импорта.

## Связь записи Kinescope с карточкой эфира «Конференция 5» (проверено, без номеров)

| Признак | Есть ли | Совпадает? |
|---|---|---|
| Запись в списке видео самого эфира Kinescope (через его эфир) | есть | да: у эфира ровно 1 запись. Это самое сильное доказательство. |
| Проект записи и проект в карточке эфира | есть у обоих | да |
| Проект у самого эфира Kinescope | поля нет в ответе | НЕИЗВЕСТНО |
| Отдельное поле «эфир» или «поток» у записи | нет (есть только папка, она со ссылками на эфир не совпадает) | НЕИЗВЕСТНО |
| Название | есть | начинается с названия карточки эфира, дальше добавлены дата и время; дословного совпадения нет |
| Даты | есть | эфир Kinescope создан 11.09; эфир начался 13.09 в 07:26 UTC и закончился в 12:18 UTC; запись создана 13.09 в 12:14 UTC — сходится |
| Длительность | есть | ~4 ч 38 мин при окне эфира ~4 ч 52 мин — сходится |

Надёжная связь: эфир Kinescope, его список видео (ровно одно), совпадение проекта и дата. Название — дополнительная проверка по началу строки.

## Что построить (PR в GitHub)

**1. Схема (одна миграция, только добавления)**
- Новая таблица привязок исторических источников. Она связывает источник расшифровки с **карточкой эфира**, а не с уроком.
  - Хранит: эфир, продукт ЦБ20 20 поток, эфир и проект Kinescope, дату, версию данных эфира.
  - Права: запись и чтение — только служебная роль; владелец может только читать; для остальных доступа нет.
  - Защита от повтора: уникальность по паре «источник — эфир».
  - Защита источника: запись без урока допустима, только если у источника нет привязок к урокам.
- Новой функции импорта нет. Действующие функции импорта и их права не меняются. Существующие таблицы, правила доступа и тарифы CB21 не меняются.

**2. Отдельный режим команды импорта проверенных субтитров («исторический эфир»)**
- Запускает только владелец (главный администратор). Есть режим пробного прогона и режим записи; пробный прогон по умолчанию.
- Проверка урока в этом режиме заменяется проверкой эфира: запись должна стоять в списке видео эфира Kinescope (ровно одна), проект совпадает, дата попадает в окно эфира, название начинается с названия карточки.
- Защита области: запись должна отсутствовать среди видео уроков курса и среди уже подключённых источников. Иначе остановка: такие записи идут обычным путём курса.
- Порядок записи: источник → привязка к эфиру → действующая функция проверенных субтитров → повторный вызов без дубля → проверка результата.
- Из Kinescope берутся только служебные данные записи и ровно одна готовая русская дорожка субтитров. Берутся через уже защищённый способ скачивания, без ключей.
- Проверка качества субтитров (провал = остановка без записи):
  - дорожка одна, русская, статус «готово», файл целый;
  - субтитры покрывают почти всю запись, без длинных пропусков — по правилам аудита пробелов;
  - нет повторяющегося мусора;
  - длительность совпадает с записью.
  - Если найдены пропуски, запись получает статус «требует проверки», а не «готово».
- Происхождение: сохраняются версия источника, способ получения, отпечаток субтитров и отпечаток итогового текста.
- Пробный прогон выдаёт только числа и статусы: без текста, номеров и ссылок.

**3. Защита от продавца**
- Продавец по-прежнему получает только одобренные факты.
- Исторический источник не попадает в выбор по урокам CB21. Отдельный тест проверяет, что его текст не доходит до ИИ продавца.
- В редакторе знаний исторический источник можно показать только как основу для общей справки. Эту доработку лучше вынести в отдельный PR.

**4. Тесты (PGlite и модульные)**
- Права доступа и правила строк.
- Повторный вызов без дубля.
- Отказ, если: нет привязки к эфиру; дорожек несколько; субтитры изменились после пробного прогона; запись не 20 потока.
- Урок не создаётся, доступы CB21 не меняются, в продавца текст не попадает.

## Порядок выполнения в Lovable (после слияния PR, отдельной командой)

1. Предварительные проверки:
   - точный SHA слияния, чистая рабочая копия;
   - миграции нет в журнале;
   - источников расшифровки 59, расшифровок 59, фактов 47;
   - кампания выключена, ручная пауза, задач в работе 0.
2. Применить ровно одну миграцию через управляемый путь Lovable. Проверить результат: таблица и функция есть, права только у служебной роли, 0 строк.
3. Пробный прогон импорта: все проверки качества пройдены, ожидается 1 новый источник, 1 привязка к эфиру, 0 уроков.
4. Запись: выполнить один раз, затем повторить и убедиться, что повтор не создал дубль.
5. Проверка результата: 60 источников и 60 расшифровок; привязок к урокам не прибавилось; уроки, модули и правила доступа CB21 без изменений; фактов по-прежнему 47; кампания без изменений.

Остановка при любом расхождении: другой SHA, изменились субтитры, провалена проверка качества, неожиданный повтор или затронут урок.

## Откат

- Если импорт прошёл неудачно: удалить только созданную запись источника и её привязку к эфиру, строго по отпечаткам из пробного прогона и отдельной командой. Затем проверить, что снова 59 источников.
- Схему откатывать не нужно: она только добавляет новое и ничего не ломает.

## Нужно ли решение владельца

- **Для закрытого импорта** — нет, если вы поручите выполнение отдельной командой. Текст остаётся закрытым, продавец его не видит.
- **Для фактов продавца — да, обязательно и отдельно.** Эта запись не входит в обучение CB21. Возможна только общая справка о прошлом потоке, и только после:
  - проверки текста владельцем;
  - отдельного разрешения;
  - проверки и применения в редакторе знаний.
  - Формулировки вроде «разберём» или «в курсе будет» запрещены.
- Эфир 26.08 в эту задачу не входит, пока владелец не подтвердит, что это нужная запись.

## Не делается

Уроки, доступы и тарифы CB21, кампания, распознавание речи, ИИ, публикация, сообщения клиентам.
