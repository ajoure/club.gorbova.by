да, согласен, с учетом правок:

&nbsp;

1. Нельзя в ТЗ писать, что формула по created_at + last_charge_at + interval_days дает точное количество списаний. Это только расчетный fallback. Иначе UI будет показывать пользователю ложные цифры.
2. Источник количества списаний нужно описать так:  

  - сначала взять точное значение из данных подписки, если оно уже есть у провайдера в явном виде (charge_count / raw_data / meta);
  - если точного поля нет, использовать расчетное значение по датам как fallback;
  - если данных недостаточно для надежного расчета, не писать ложный 0, а показывать нейтрально: — или скрывать бейдж по согласованной логике.
3. &nbsp;
4. Не фиксировать сейчас правило last_charge_at = null -> 0 спис. как обязательное. Это может означать не “0 списаний”, а “нет подтвержденных данных для расчета”.
5. Визуально бейдж лучше делать не текстом 2 спис., а:  

  - круглый/овальный премиальный badge с крупной цифрой;
  - рядом мелкая подпись списаний или tooltip/aria-label;
  - чтобы число читалось быстро и не перегружало карточку.
6. &nbsp;
7. Добавить явную сортировочную/смысловую задачу: бейдж должен помогать отличать новые и старые подписки, но не заменять дату создания и дату последнего списания.
8. Если используется fallback-расчет, это не должно ломать дизайн:  

  - без ярких предупреждений;
  - но логика должна быть честной и расширяемой, чтобы потом легко заменить на точный счетчик без переделки UI.
9. &nbsp;
10. В DoD добавить проверку на 3 типа кейсов:  

  - новая подписка без подтвержденного списания;
  - подписка с 1 списанием;
  - подписка с несколькими списаниями.
11. &nbsp;
12. В DoD отдельно зафиксировать, что бейдж не должен визуально спорить со статусом подписки и датами.

&nbsp;

&nbsp;

Копируемый блок для Lovable:

Дополни текущий патч по карточке подписки в ContactDetailSheet.

&nbsp;

Нужно добавить на карточку подписки аккуратный визуальный бейдж количества списаний по этой подписке.

&nbsp;

Важно: не выдавать расчет за точный факт.

&nbsp;

Логика источника данных:

1. Сначала использовать точное количество списаний, если оно уже есть в данных подписки у провайдера (например, charge_count / raw_data / meta).

2. Если точного поля нет, использовать fallback-расчет по уже доступным полям:

   - created_at

   - last_charge_at

   - interval_days

3. Если данных недостаточно для надежного расчета, не показывать ложный 0 как факт. В таком случае:

   - либо показывать нейтральное значение,

   - либо скрывать бейдж по согласованной логике.

4. Не писать в коде и в UI, что fallback-формула является точной.

&nbsp;

Что сделать в UI:

1. Добавить компактный круглый или овальный бейдж с числом.

2. Бейдж должен выглядеть аккуратно и премиально:

   - мягкий бледный фон

   - легкая обводка

   - спокойный цвет

   - без кислотных акцентов

3. Число должно быстро считываться визуально.

4. Бейдж разместить в строке с текущими метаданными подписки так, чтобы он не ломал верстку и не спорил со статусом.

5. Бейдж должен помогать быстро понять, какая подписка новая, а какая давно активна.

6. Даты создания и последнего платежа оставить как отдельные поля — бейдж их не заменяет.

&nbsp;

Технические правки:

1. Добавить в select все уже доступные поля, которые нужны для точного или fallback-расчета количества списаний.

2. Сделать helper/utility для вычисления display-count, чтобы потом можно было легко заменить fallback на точный источник без переделки UI.

3. Не добавлять новые edge functions и миграции без доказанной необходимости.

&nbsp;

DoD:

- на карточке подписки отображается бейдж количества списаний;

- если есть точный источник — показывается точное значение;

- если точного источника нет, используется fallback-расчет;

- если данных недостаточно, UI не показывает ложный 0 как подтвержденный факт;

- бейдж не ломает текущую верстку;

- визуально выглядит сдержанно и аккуратно;

- проверены минимум 3 кейса: новая подписка, подписка с 1 списанием, подписка с несколькими списаниями.

&nbsp;

&nbsp;

## План: Добавить бейдж счётчика списаний на карточку подписки

### Источник данных

Прямой связи `provider_subscriptions → payments_v2` по subscription ID нет (только 15 из 437 recurring-платежей имеют `bepaid_subscription_id` в meta). Вместо этого — **расчётная формула** из уже загруженных полей:

```
charge_count = last_charge_at && interval_days > 0
  ? Math.floor((last_charge_at - created_at) / interval_days) + 1
  : last_charge_at ? 1 : 0
```

Распределение по активным подпискам: 0 списаний — 100, 1 — 48, 2 — 46. Формула точна для рекуррентных подписок с фиксированным интервалом.

Поле `interval_days` уже есть в таблице, но **не в текущем select**. Нужно добавить.

### Изменения

**Файл: `src/components/admin/ContactDetailSheet.tsx**`

**1. Запрос (~строка 768):** добавить `interval_days` в select:

```
id, provider, state, provider_subscription_id,
next_charge_at, amount_cents, currency, card_brand, card_last4,
created_at, last_charge_at, interval_days,
subscription_v2_id, meta,
```

**2. Рендер (~строка 1947, рядом с бейджами статуса и провайдера):** добавить вычисляемый бейдж:

- Вычислить `chargeCount` по формуле выше
- Добавить круглый бейдж после существующих бейджей (state, provider)
- Стиль: `text-[10px]`, мягкий фон `bg-slate-100 dark:bg-slate-800`, слабая обводка `border-slate-200/60 dark:border-slate-700/60`, текст `text-slate-500 dark:text-slate-400`
- Содержимое: `{chargeCount} спис.` (например «2 спис.»)
- Если `chargeCount === 0` — показывать `0 спис.` (не скрывать, чтобы не создавать путаницу)

### Визуальное размещение

Бейдж встаёт в строку `flex items-center gap-2` (~строка 1947) после бейджа провайдера и перед картой:

```text
[active] [bePaid] [2 спис.] VISA •••• 4242
```

### Что НЕ меняется

- Остальные поля карточки (даты, статус, доступ до)
- Другие разделы карточки контакта
- Никаких миграций, edge functions, новых запросов

### DoD

- На каждой карточке подписки отображается бейдж с числом списаний
- Число вычислено из `created_at`, `last_charge_at`, `interval_days`
- При `last_charge_at = null` → показывается «0 спис.»
- Бейдж не ломает текущую вёрстку
- Визуально сдержанный, компактный, премиальный