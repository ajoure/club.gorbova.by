# План подготовки режима «Анализ выписки» (PR #490) — PLAN-ONLY

Ревизия выполнена только чтением. Никаких изменений кода, данных, функций, секретов, доступов, deploy и Publish не производилось.

## 0. Ключевое ограничение

Коммит `53b2783327…` в рабочей копии недоступен: `git fetch` вернул `not our ref`, локальный HEAD — `4215b0b1c`. Поэтому содержимое двух миграций PR и код `bank-statement-analyzer` проверены не были. Перед любым выполнением нужно синхронизировать точный SHA и подтвердить побайтовое совпадение миграций.

## 1. Наблюдения по production (read-only)

| Пункт | Факт |
|---|---|
| `app_sections.ai_bank_statement_analysis` | ОТСУТСТВУЕТ. Есть только `ai` (`d0136a5c…`, active, non-public) и `ai_asset_classifier` (`9d049abf…`, active, non-public, route `/ai?sub=chat&scenario=asset_classifier`) |
| Сценарий `bank_statement_analysis` в `ai_user_prompts` | ОТСУТСТВУЕТ. Активны только `balance_analysis`, `107NK`, `asset_classifier` |
| `grp-lookup` | СУЩЕСТВУЕТ (`supabase/functions/grp-lookup/index.ts`): нормализация УНП (9 цифр), запрос в ГРП, ответ `found/data/raw`; авторизация — пользовательский JWT либо внутренний service-role вызов |
| `bank-statement-analyzer` | В репозитории ОТСУТСТВУЕТ (нет каталога функции) |
| Секреты | `LOVABLE_API_KEY` есть (AI-шлюз). Отдельных секретов под анализ выписки нет и, по аналогии с `asset-classifier`, не требуется. Если PR вводит новый внешний источник — потребуется отдельный секрет (в плане PR его нужно явно назвать) |
| Управление доступом | Правила `section_access` хранятся в `access_rules` (`grant_target_type='section_access'`, `target_ref` = id секции) и уже редактируются в Admin Access Rules — для новой секции модель та же, новых таблиц не нужно |
| Текущие правила к AI-секциям | `ai_asset_classifier` — ровно 2 активных product-level правила; тарифов ЦБ20/ЦБ21 среди них нет |

## 2. Применимость двух миграций PR

Ожидаемое безопасное содержание: (а) вставка секции `ai_bank_statement_analysis`, (б) вставка сценария-промпта `bank_statement_analysis`. Такие вставки не меняют существующие доступы, если выполняются условия:

- миграции идемпотентны (`ON CONFLICT (code) DO NOTHING/UPDATE`), без `DELETE`/`UPDATE` по чужим строкам;
- не изменяют существующие `access_rules`, `app_sections` других кодов, `ai_user_prompts` других кодов;
- не создают `is_public = true` (иначе раздел откроется всем);
- не задают доступ жёстко в коде — только через `access_rules`.

Любое отклонение = STOP без применения.

## 3. Порядок выполнения (после отдельного APPROVE)

1. Preflight-снимок: количество строк `app_sections`, `ai_user_prompts`, активных `access_rules` по `section_access`; список деплоенных функций.
2. Синхронизация точного SHA, проверка чистого дерева и побайтового совпадения обеих миграций.
3. Применение миграции секции → read-back: ровно одна новая строка `ai_bank_statement_analysis`, `is_active=true`, `is_public=false`, остальные секции без дельты.
4. Применение миграции сценария → read-back: ровно одна новая строка `ai_user_prompts.code='bank_statement_analysis'`, прочие промпты без изменений.
5. Deploy функций — точный список: `bank-statement-analyzer` (новая), `grp-lookup` (только если PR её меняет; иначе НЕ деплоить), `gorbova-ai-chat` (только если PR меняет роутинг сценариев). Деплой строго по одной функции.
6. Настройка правил доступа для тарифов ЦБ20/ЦБ21 — вручную в Admin Access Rules, не миграцией.

## 4. Read-back и безопасный runtime smoke

- Auth: вызов функции без токена → 401; с невалидным токеном → 401; с валидным пользовательским токеном без прав → отказ доступа (не 500).
- Доступ ЦБ20/ЦБ21: проверка резолвера секции для тестового набора правил; подтверждение, что без активного правила раздел не виден, с правилом — виден.
- Отчёт о несовпадении: прогон на синтетической выписке (без PII, без реальных клиентских файлов) — проверить, что несовпадения контрагент/УНП отображаются и запрос к ГРП не падает при «не найдено».
- Нулевые дельты: деньги, платежи, очередь, доступы, документы, подписки, автоматизации — без изменений.

## 5. Критические риски

1. **Недоступный SHA PR** — содержимое миграций и функции не проверено; выполнение без синхронизации запрещено.
2. **Семантика резолвера доступа**: фактически используется `get_user_section_access` (принимает `active`/`trial`, игнорирует `access_end_at`, допускает product-fallback для tariff-scoped правил), а не строгий `user_has_access_to_rule`. Новый раздел унаследует ослабленную проверку — это отдельное решение, не часть данного PR.
3. **`is_public=true` по недосмотру** — мгновенное открытие платного раздела всем.
4. **Массовый деплой функций** запрещён политикой проекта; вебхуки не затрагиваются.
5. **Стоимость/лимиты AI-шлюза**: анализ выписки — тяжёлые файловые запросы; нужен лимит размера файла и защита от повторных прогонов.
6. **PII**: выписки содержат персональные и платёжные данные — в логах, скриншотах и отчётах их быть не должно; нужен явный запрет логирования содержимого файла.
