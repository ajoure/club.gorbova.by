## да, согласен, с учетом правок:

1. **Сначала проверить фактическое поле HTML-контента.**  
В плане указано `blocks[1].content.code`, но раньше использовалось `content.html`. Перед UPDATE обязательно определить реальную структуру:
  &nbsp;
  ```sql
  SELECT blocks->1->'content' FROM site_pages WHERE id = '7e672fed-13f1-4ff1-8786-71a228a0c011';
  ```
  Менять нужно фактическое поле, а не предположительное.
2. **Удаление blocks[0] и blocks[2] допустимо, но только как cleanup дублей.**  
Это не нарушение add-only, потому что эти блоки были ошибочно добавлены как внешние iframe-дубли. Но в отчёте обязательно указать:
  - удалены только внешние дублирующие header/footer-блоки;
  - основной landing HTML сохранён;
  - старые блоки сохранены в evidence `site-018-blocks-before.json`.
3. **Перед удалением внешних блоков проверить их роли/содержимое.**  
Не удалять просто по индексам вслепую. Проверить, что:
  - `blocks[0]` действительно внешний header «Буква закона»;
  - `blocks[2]` действительно внешний footer «Буква закона»;
  - `blocks[1]` действительно основной landing «ИдеоБизнес».
  Если порядок отличается — STOP.
4. **Вставить header/footer внутрь одного HTML-документа — правильно.**  
Это решает главную проблему: якоря, модалка и скрипты будут жить в одном iframe/document, без cross-iframe bridge.
5. **CTA в header не должен одновременно “открывать модалку и скроллить”.**  
Нужно выбрать одно поведение. Для кнопки **«Настроить идеологическую работу»** лучше:
  &nbsp;
  ```html
  onclick="openModal('setup')"
  ```
  или фактический существующий обработчик. Не добавлять одновременный scroll к `#paths/#timeline`, чтобы не получить конфликт: модалка открылась, а страница уехала.
6. **Проверить фактическое имя функции модалки.**  
В плане указано `openLeadModal('header_cta')`, но ранее в HTML были `openModal('setup')` / `openModal('access')`. Перед вставкой проверить реальное имя функции в blocks[1].  
Если `openLeadModal` не существует — не использовать.
7. **Добавить маркеры в новый inline header/footer.**  
Для идемпотентности добавить явные комментарии:
  &nbsp;
  ```html
  <!-- lovable-bz-header-v1 -->
  ...
  <!-- /lovable-bz-header-v1 -->

  <!-- lovable-bz-footer-v1 -->
  ...
  <!-- /lovable-bz-footer-v1 -->
  ```
  Повторный запуск должен обновлять эти секции, а не добавлять новые.
8. **Не делать бренд кликабельным — согласен.**  
В header и footer бренд должен быть `<div>`, не `<a>`, чтобы не было перехода на `#` или главную.
9. **Ссылку на кабинет оставить в той же вкладке.**  
Корректно:
  &nbsp;
  ```html
  <a href="https://gorbova.by/auth">Войти в личный кабинет</a>
  ```
  Без `target="_blank"`.
10. **Footer documents оставить внешними ссылками на gorbova.by.**  
Документы — это не якоря текущего лендинга, поэтому ссылки на публичную оферту, политику и т.п. можно оставить абсолютными URL.
11. **Добавить проверку отсутствия второго iframe-сценария.**  
После UPDATE должен быть один html-блок, но он всё равно рендерится внутри одного iframe через `HtmlIframePreview`. Это нормально. Важно, чтобы header/main/footer были внутри одного iframe, а не в трёх разных.
12. **В Playwright добавить проверку DOM/iframe.**  
Помимо скриншотов проверить:

```text
количество html iframe-блоков на странице = 1
внутри iframe есть lovable-bz-header-v1
внутри iframe есть lovable-bz-footer-v1
внутри iframe есть #paths, #benefits, #what-we-do, #roles, #db, #timeline, #payment
```

13. **Добавить mobile-проверку header navigation.**  
Если на мобильном 7 пунктов не помещаются, должен быть безопасный вариант:

- горизонтальный scroll nav; или
- компактная строка/перенос; или
- скрыть часть пунктов без поломки CTA.  
Не должно быть горизонтального скролла всей страницы.

14. **В отчёте обязательно указать Publish-status.**  
Если preview исправлен, но `gorbova.by` ещё показывает старое — написать:

```text
Код/данные исправлены, для публичного домена требуется Publish/пересборка/очистка кеша.
```

15. **Обязательное требование для Lovable.dev:**

```text
План должен быть составлен на русском языке.
Отчет о выполненной работе должен быть составлен на русском языке.
Вся переписка, все пояснения и все результаты должны предоставляться только на русском языке.

План: единый header/footer для SITE-000018 «Идеологическая работа»
```

### Проблема

Сейчас на странице два header'а и два footer'а: внешние html-блоки «Буква закона» (blocks[0], blocks[2]) добавлены поверх внутренних блоков лендинга «ИдеоБизнес». Внешние блоки рендерятся в отдельных `<iframe>`, поэтому их якоря (`#paths`, `#payment`, …) НЕ работают — целевые id находятся внутри другого iframe, а sandbox без `allow-same-origin` блокирует мост.

### Решение

Не использовать отдельные iframe-блоки. Отредактировать **inline внутри блока лендинга (blocks[1])**: вырезать встроенный «ИдеоБизнес»-header и встроенный «ИдеоБизнес»-footer и заменить их на новые «Буква закона»-секции прямо в той же HTML-странице. Внешние html-блоки header/footer — удалить. Тогда якоря, форма `leadModal` и CTA работают нативно в одном документе.

### Источники данных (уже проверено)

- Page id: `7e672fed-13f1-4ff1-8786-71a228a0c011`, slug `ideologicheskaya-rabota`, 3 блока: `[html header, html landing, html footer]`.
- Существующие якоря в лендинге: `#paths`, `#benefits`, `#what-we-do`, `#roles`, `#db`, `#timeline`, `#payment`.

### Маппинг навигации (используем существующие якоря)


| Пункт меню  | Якорь         |
| ----------- | ------------- |
| 3 пути      | `#paths`      |
| Выгоды      | `#benefits`   |
| Что делаем  | `#what-we-do` |
| Для кого    | `#roles`      |
| База знаний | `#db`         |
| Как начать  | `#timeline`   |
| Оплата      | `#payment`    |


### Новый header (внутри лендинга, вместо «ИдеоБизнес»-шапки)

- Тёмно-бордовый фон `#1a0a0e` (тон сайта), одна полоса (без второго чёрного бара сверху).
- Слева: иконка + «БУКВА ЗАКОНА / Клуб по законодательству» — **некликабельные** (`<div>`, не `<a>`).
- Центр: nav по 7 якорям из таблицы выше.
- Справа: первичная CTA-кнопка **«Настроить идеологическую работу»** (бордовая, как сейчас) — вызывает `openLeadModal('header_cta')` (уже существующая функция в скриптах лендинга) и скроллит к `#paths`/`#timeline`. Рядом — **вторичная маленькая** кнопка **«Войти в личный кабинет»**: `<a href="https://gorbova.by/auth">` БЕЗ `target="_blank"` → открытие в той же вкладке.
- Удаляется: верхняя чёрная полоса c «БУКВА ЗАКОНА» + «Преимущества/Наполнение/Тарифы/FAQ/Контакты» (это блок blocks[0]) и встроенный «ИдеоБизнес» header (внутри blocks[1]).

### Новый footer (внутри лендинга, вместо «ИдеоБизнес»-футера)

- Удаляется встроенный «ИдеоБизнес»-футер (с «3 Пути / Налоговая экономия / Функционал системы / Цифровая база ответов» и контактами «[consulting@ideobusiness.by](mailto:consulting@ideobusiness.by)») — это часть blocks[1].
- Удаляется отдельный html-блок blocks[2] (текущий «Буква закона»-footer с ссылками на gorbova.by).
- Новый единый footer-секция (вставляется в blocks[1] в конец body, перед `</body>`):
  - Логотип + «БУКВА ЗАКОНА / Клуб по законодательству» — **некликабельно**.
  - Реквизиты: ЗАО «АЖУР инкам», УНП 193405000, юр./почтовый адрес, тел., email, режим работы (как сейчас в «Буква закона»-футере).
  - **Колонка «Навигация»** — те же 7 якорей, что в шапке (`#paths`, `#benefits`, `#what-we-do`, `#roles`, `#db`, `#timeline`, `#payment`).
  - **Колонка «Документы»** — оставить как есть: Публичная оферта, Заказ и оплата услуг, Политика конфиденциальности, Согласие на обработку данных, Инструкция по оформлению расходов (ссылки на gorbova.by/* — это статические юр-документы, остаются).
  - Платёжные логотипы (Visa/MC/Белкарт/bePaid/Samsung Pay/G Pay/ЕРИП) — как сейчас.
  - Низ: «© 2026 ЗАО «АЖУР инкам». Все права защищены.»

### Технические шаги

1. **Read** текущего `blocks[1].content.code` (полностью, ~196 строк).
2. Локально подготовить отредактированный HTML лендинга:
  - вырезать секцию внутреннего «ИдеоБизнес»-header (по идентифицируемым маркерам);
  - вырезать секцию внутреннего «ИдеоБизнес»-footer;
  - вставить новые header/footer-секции прямо в DOM лендинга, чтобы они жили в одном документе с `#paths..#payment` и `leadModal`;
  - кнопка «Настроить идеологическую работу» в шапке использует тот же обработчик, что и существующие CTA лендинга;
  - кнопка «Войти в личный кабинет» = `<a href="https://gorbova.by/auth">` (без `target`).
3. **UPDATE** `site_pages.blocks` через SQL (`jsonb_set`) — заменить `blocks[1].content.code` на новый HTML и **удалить** `blocks[0]` и `blocks[2]` (оставить массив из одного элемента). Идемпотентно: проверка наличия маркера `lovable-bz-header-v1` в новом HTML.
4. Сохранить evidence: `.lovable/artifacts/site-018-blocks-before.json`, `…-after.json` и `…-landing-final.html`.
5. **Verify**:
  - `SELECT jsonb_array_length(blocks)` = 1; 
  - Playwright (desktop+mobile) на превью: проверить что верхняя чёрная полоса исчезла, бренд «Буква закона» некликабелен, клик по «3 пути» скроллит к `#paths`, клик по «Настроить идеологическую работу» открывает `leadModal`, клик по «Войти в личный кабинет» переходит на `/auth` в той же вкладке. Скриншоты в `.lovable/artifacts/`.

### Что НЕ трогается

- `HtmlIframePreview.tsx`, SiteRenderer, маршрутизация.
- Контент лендинга между header и footer (hero, тарифы, FAQ, модалки) — без изменений.
- SITE-000017 (Gorbova Club) и другие страницы.
- Документы footer'а (ссылки на оферту/политику) — оставлены как есть.

### DoD

- `site_pages.blocks` для SITE-000018 содержит ровно 1 блок (объединённый лендинг).
- На опубликованной странице нет двойной шапки и двойного футера.
- Все 7 пунктов навигации скроллят к существующим секциям без перезагрузки и без новой вкладки.
- «Настроить идеологическую работу» открывает лид-форму, «Войти в личный кабинет» открывается в той же вкладке.
- Бренд «БУКВА ЗАКОНА / Клуб по законодательству» — некликабельный и в шапке, и в футере.
- Evidence-файлы и Playwright-скриншоты сохранены.