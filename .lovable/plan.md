# План: публикация PR #512 — SHA `f8c4f5aa7abbc9d7216ef9a8cf07e22ede7b638b`

## Диагностика (read-only, выполнено)

- Текущий checkout: HEAD = `f8c4f5aa7abbc9d7216ef9a8cf07e22ede7b638b`, дерево чистое, предок `3d913dcb9`.
- Прикладной диф `3d913dcb9..f8c4f5aa7` ограничен ровно тремя заявленными файлами (+65/−9):
  - `src/hooks/useAiChat.ts` — добавлен `buildBankStatementErrorMessage`; ошибка анализа выписки теперь сохраняется как assistant-сообщение в чате с заголовком, именем файла (`**Файл:** …`), причиной и пронумерованными шагами восстановления (XLSX/CSV, PDF с текстом, один счёт и один месяц, повторный запуск). Метаданные: `is_error`, `scenario_code: bank_statement_analysis`, `launcher_title_snapshot: "Анализ выписки"`, `file_names`, `exclude_from_ai_history`. Вызов `toast(...)` при ошибке удалён — временное всплывающее уведомление больше не основной канал ошибки. Функция возвращает `boolean`.
  - `src/components/ai-chat/AiPageContent.tsx` — форма сценария закрывается только при успехе (`if (succeeded) setActiveScenario(null)`), то есть после ошибки форма загрузки остаётся открытой для повтора.
  - `src/hooks/useAiChat.reliability.test.ts` — +1 тест: ошибка остаётся в чате, содержит имя файла, «Что сделать», «XLSX или CSV», корректные метаданные, `toast` не вызывается.
- Успешный путь без изменений: тот же `setMessages` с ответом, `setActiveScenarioContext(null)`, добавлен только `return true`.
- Backend не затронут: миграций, изменений базы, Edge Functions, доступов, тарифов, платежей, секретов и production-данных в дифе нет.

## Проверки (выполнено локально на этом SHA)

- Vitest `useAiChat.reliability.test.ts` — 7/7 PASS; `PromptRunFlow.test.tsx` — 4/4 PASS; суммарно 11/11 PASS.
- Typecheck `tsc -p tsconfig.app.json --noEmit` — PASS (exit 0).
- Production build — PASS (44.49s, только стандартные предупреждения о размере чанков).
- GitHub checks: подтверждаются отдельно на стороне GitHub перед Publish (см. preflight).

## Предлагаемое выполнение

1. **Preflight / sync**
   - заново прочитать GitHub `refs/heads/main` без stale-cache и подтвердить exact SHA `f8c4f5aa7…`;
   - подтвердить чистое дерево и трёхфайловый прикладной диф от `3d913dcb9`;
   - подтвердить зелёные GitHub checks на этом SHA;
   - остановиться при любом новом прикладном коммите или расширении scope.
2. **Dry run** — перезапуск двух целевых тестов (ожидание 11/11), typecheck и production build. Никаких правок в рамках execute.
3. **Publish** — опубликовать ровно этот прикладной SHA на существующий production-сайт `https://gorbova.lovable.by/…` → `https://gorbova.lovable.app`. Без миграций, без deploy функций, без изменений данных и настроек.
4. **Read-back и проверка отказа**
   - подтвердить опубликованный URL и SHA/версию (не только HTTP 200);
   - смоделировать контролируемый отказ анализа выписки без реальных PII: синтетический файл, вызывающий ошибку распознавания/таймаут (никаких реальных выписок и персональных данных);
   - desktop 1280×800: сообщение об ошибке видно в ленте чата, содержит имя файла, причину и шаги; форма загрузки остаётся открытой; повторный запуск доступен; текст не обрезан;
   - mobile 390×844: те же проверки, без горизонтального выхода, обрезания и перекрытия;
   - два скриншота опубликованного результата (desktop и mobile), привязанные к URL, SHA и viewport, без PII и секретов.
   - Для аутентифицированной проверки требуется доступ к режиму ЦБ20/ЦБ21; если нужен временный tagged-тестовый аккаунт, это отдельное разрешение владельца (стоп-правило по созданию пользователей).

## STOP-guards

- Published SHA не равен `f8c4f5aa7…`.
- Прикладной диф выходит за три заявленных файла.
- Любой тест/typecheck/build/GitHub check не проходит.
- На опубликованной версии ошибка не остаётся в чате, форма закрывается после отказа, либо мобильная компоновка обрезана/перекрыта.
- Обнаруживается изменение backend, данных, функций, доступа или коммерческой логики.

## DoD

- Exact SHA подтверждён до и после Publish.
- 11/11 тестов, typecheck, build и GitHub checks PASS.
- Production desktop и 390×844 показывают постоянное сообщение об ошибке с файлом, причиной и шагами, открытую форму и возможность повтора.
- Два скриншота опубликованного результата, привязанные к URL, SHA и viewport.
- Backend, данные, функции, доступы и коммерческие настройки не изменены.
