## да, согласен, с учетом правок:

1. **Не удалять данные “Буква закона”, “АЖУР инкам” и реквизиты компании.**  
Это не тестовые данные, а данные общей компании/клуба. Их нужно сохранить.
2. **Хедер и футер копировать полностью по структуре с** `gorbova.by`**.**  
Не изобретать новый футер/хедер под продукт. Логика такая:
  - компания одна;
  - «Идеологическая работа» — отдельный продукт этой же компании;
  - поэтому хедер/футер должны быть теми же по содержанию и структуре.
3. **Меняется только визуальная адаптация под тёмный стиль страницы.**  
То есть копируем весь хедер/футер, но перекрашиваем под дизайн страницы «Идеологическая работа»:
  - тёмный фон;
  - светлый текст;
  - бордовые/винные акценты;
  - без светлого фона как на главной;
  - без визуального конфликта с основной HTML-страницей.
4. **Содержимое футера оставить как у компании.**  
Сохраняем:
  - «БУКВА ЗАКОНА»;
  - «Клуб по законодательству»;
  - ЗАО «АЖУР инкам»;
  - УНП;
  - адреса;
  - email;
  - режим работы;
  - документы;
  - навигацию;
  - платежные логотипы, если они есть в оригинальном футере.
5. **Навигацию можно адаптировать только там, где это нужно для текущей страницы.**  
Если в хедере/футере есть пункты, которые должны вести по сайту компании — оставить.  
Если есть внутренние якоря страницы идеологии — они должны работать внутри `/ideologicheskaya-rabota`.
6. **Исправленный текст для Lovable.dev:**

```text
Важно: хедер и футер нужно скопировать по структуре и содержанию с главной gorbova.by, потому что это одна и та же компания, а страница “Идеологическая работа” — отдельный продукт этой компании.

Данные “БУКВА ЗАКОНА”, “Клуб по законодательству”, ЗАО “АЖУР инкам”, УНП, адреса, email, документы, платежные логотипы и прочие корпоративные данные НЕ удалять.

Нужно не изобретать новый футер/хедер, а полностью использовать корпоративный хедер и футер gorbova.by, но визуально адаптировать их под тёмную стилистику страницы /ideologicheskaya-rabota: тёмный фон, светлый текст, бордовые/винные акценты, без светлого шаблонного оформления.

Итог должен выглядеть как единый тёмный лендинг одного продукта внутри общей компании.
```

7. **Остальные требования плана остаются в силе:**

- вставка через `site_pages.blocks`;
- не трогать `HtmlIframePreview.tsx`;
- не трогать renderer;
- не трогать БД-схему/RLS/edge functions;
- не трогать SITE-000017;
- сохранить идемпотентность по `metadata.role = site_header/site_footer`;
- отчет и план Lovable — только на русском языке.
- &nbsp;
- План: добавить хедер и футер с gorbova.by на страницу «Идеологическая работа»

### Цель

Целевая страница: `https://gorbova.by/ideologicheskaya-rabota#db` = `site_pages` SITE-000018 (id `7e672fed-13f1-4ff1-8786-71a228a0c011`, slug `ideologicheskaya-rabota`, status `published`).

Источник разметки: главная `https://gorbova.by/` — компоненты `LandingHeader` / `LandingFooter`.

### Подход

Согласно ранее выбранному варианту «Скопировать как HTML с живого gorbova.by» — вставляем хедер и футер не как React-компоненты, а как `html`-блоки в `site_pages.blocks`. Так не трогаем рендерер site_pages (`SiteRenderer` / `HtmlIframePreview`) и не ломаем правки прошлых задач по iframe-мосту.

### Шаги

1. **Diagnose**
  - Уже подтверждено: главная gorbova.by содержит `<header>` (≈2.8 KB) и `<footer>` (≈3.3 KB). На странице `/ideologicheskaya-rabota` их сейчас нет.
  - Подтверждено: SITE-000018 — отдельная site_page; SITE-000017 (Gorbova Club), открытая в редакторе, не затрагивается.
2. **Извлечь HTML с живого сайта (одноразово, через Playwright)**
  - Открыть `https://gorbova.by/` в headless-браузере.
  - Снять `document.querySelector('header').outerHTML` и `document.querySelector('footer').outerHTML`.
  - Дополнительно: пройти по всем `<a href="/...">` в этих фрагментах и переписать на абсолютные `https://gorbova.by/...`, чтобы ссылки работали внутри `html`-блока (он рендерится в той же React-аппке, но клик по `/club` должен вести на нужный маршрут — `<a>` всё равно сработает).
  - Сохранить в `.lovable/artifacts/gorbova-header.html` и `.lovable/artifacts/gorbova-footer.html` как evidence.
3. **Dry run / Verify**
  - Распечатать длину, первые/последние 200 символов фрагментов, чтобы убедиться, что мы взяли именно `<header>` и `<footer>`, без `<body>`-обвязки.
  - Убедиться, что классы Tailwind, используемые в фрагментах, уже есть в бандле (они используются гл. страницей → присутствуют).
4. **Execute — обновить `site_pages.blocks` для SITE-000018**
  Прочитать текущий `blocks` массив через `supabase--read_query`, добавить два новых блока без удаления существующих:
   Запись — одной транзакцией через `supabase--migration` или сервисным UPDATE. `updated_by`, `updated_at` — выставить.
   **Идемпотентность:** перед вставкой проверяем `blocks[*].metadata.role` — если уже есть `site_header` / `site_footer`, обновляем их содержимое, не плодим дубли.
5. **Verify**
  - `supabase--read_query` по SITE-000018 → убедиться, что первый блок — `html` с `role=site_header`, последний — `html` с `role=site_footer`, остальные блоки в прежнем порядке.
  - Playwright: открыть опубликованный `https://gorbova.by/ideologicheskaya-rabota`, дождаться рендера, сделать скриншот верха и низа страницы. Убедиться, что хедер/футер видны и совпадают с gorbova.by по виду, ссылки в навигации кликабельны.

### Что НЕ делаем

- Не меняем `HtmlIframePreview.tsx`, `SiteRenderer`, маршрутизацию, миграции схемы.
- Не трогаем SITE-000017 («Gorbova Club»), открытую сейчас в редакторе.
- Не добавляем зависимостей, не вводим новых типов блоков (используем существующий `type: "html"`).

### DoD

- В `site_pages` (id `7e672fed-13f1-4ff1-8786-71a228a0c011`) `blocks[0].type='html'` с `metadata.role='site_header'` и `blocks[last].type='html'` с `metadata.role='site_footer'`.
- HTML-содержимое совпадает с актуальным `<header>` / `<footer>` главной gorbova.by на момент копирования (фиксируем `copied_at`).
- Сохранены evidence-файлы: `.lovable/artifacts/gorbova-header.html`, `.lovable/artifacts/gorbova-footer.html`, скриншот после рендера.
- Старые блоки SITE-000018 сохранены в прежнем порядке.
- Запуск плана повторно не создаёт дубли (идемпотентность по `metadata.role`).