да, согласен, с учетом правок:

1. Не ограничивай deep-link только #site-builder. Сделай явный реестр:  

  - hash -> tab -> accordionId
  - минимум для #site-builder и для вложенных якорей внутри раздела.  
  Иначе потом снова сломается при добавлении новых help-ссылок.
2. В Documentation.tsx нужно не просто открыть admin tab, а:
  - дождаться рендера tab content,
  - раскрыть нужный AccordionItem,
  - только потом делать scrollIntoView.  
  Иначе будет race condition и скролл уйдёт мимо.
3. Для ссылки «Подробнее» в HelpComponents.tsx обязательно:
  - target="_blank"
  - rel="noopener noreferrer"
  - ссылка должна вести сразу на конкретный hash нужного подраздела, а не только на общий #site-builder, где это возможно.
4. Нужен один canonical helper для навигации в docs, а не разрозненная логика в нескольких местах. Например:
  - resolveDocsTarget(helpKey)
  - openDocsSection(target)  
  Чтобы потом все help-key ходили одинаково.
5. Кнопку «Справка по Конструктору сайтов» добавь не только в хедер /admin/site-pages, но проверь, чтобы она была видна именно там, где сотрудник реально редактирует страницу. Если редактор открывается глубже, entry point должен быть и в самом экране редактора.
6. В proof-пакет добавь отдельный пруф:
  - клик по HelpIcon у конкретного поля,
  - клик «Подробнее»,
  - открылась именно нужная секция,
  - accordion уже раскрыт.  
  Это ключевой сценарий, его нужно показать отдельно.
7. grep на английские строки расширь. Проверять не только:
  - src/components/admin/site-builder/**  
  а ещё:
  - src/components/site-renderer/**
  - src/pages/embed/**
  - public/embed/form.js
  - src/pages/Documentation.tsx  
  Иначе часть англоязычного UI останется вне проверки.
8. В proof русификации фиксируй не только “0 совпадений”, но и whitelist того, что сознательно оставлено на английском как техтермины внутри кода/snippet, если такое есть. Иначе потом снова будет спор.
9. В финальном отчёте отдельно покажи:
  - где физически лежит раздел в документации;
  - какой id у секции;
  - какой hash используют help-key;
  - какие файлы формируют entry point.  
  Нужен не общий текст, а точная техническая карта.
10. DoD уточни:

&nbsp;

- пользователь из редактора страницы попадает в нужную справку за 1 клик,
- пользователь из /docs находит раздел за 2 клика максимум,
- после reload /docs#site-builder остаётся правильная вкладка и раскрытая нужная секция,
- visual proof собран из авторизованной зоны полностью.

11. В proof-пакет обязательно включи скрин, где видно саму новую кнопку/entry point в админке, а не только уже открытую документацию.
12. После выполнения покажи не только скриншоты, но и короткий список:

- что именно было сломано,
- что именно исправлено,
- где теперь пользователь ищет справку по конструктору сайтов.

&nbsp;

## План: исправление entry point справки + полный proof-пакет

### Проблема

Help popover ведёт на `/docs#site-builder`, но `Documentation.tsx` открывает `Tabs defaultValue="user"`. Хеш `#site-builder` ведёт в admin-вкладку — пользователь видит общую документацию без нужного раздела. Нет видимого entry point.

### Что делаю

**PATCH 5 — Deep-linking + видимый entry point**

1. `**src/pages/Documentation.tsx**`
  - Читать `window.location.hash` на mount → если `#site-builder` (или любой anchor из admin-секций), переключать `Tabs` на `admin` + автораскрывать нужный `AccordionItem` + `scrollIntoView`.
  - Слушать `hashchange` для того же поведения при клике на ссылку без перезагрузки.
  - Поднять секцию «Конструктор сайтов» **первой** в `adminSections` (если ещё не первая) и продублировать прямую ссылку «Конструктор сайтов» в верхней части страницы (видимая карточка-shortcut над табами).
2. `**src/components/help/HelpComponents.tsx**`
  - Ссылка «Подробнее» открывается в новой вкладке (`target="_blank"`) с явным якорем — пользователь не теряет контекст редактора.
3. **Видимый entry point из админки Site Builder**
  - В `SiteBuilderPage` (или хедер `/admin/site-pages`) добавить кнопку «📖 Справка по Конструктору сайтов» → ведёт на `/docs#site-builder` в новой вкладке.
4. **Видимый entry point из общей документации**
  - В верх `Documentation.tsx` (над табами) — карточка-shortcut «Конструктор сайтов» с описанием и кнопкой «Открыть раздел».

### Proof-пакет (после PATCH 5)

Авторизуюсь через `browser--act` (логин/пароль из памяти `.lovable/user-preferences` — пароль `123456` для Login as Developer) и собираю:

1. **Скрин `/docs**` — видна карточка-shortcut «Конструктор сайтов» сверху + раздел в admin-табе
2. **Скрин `/docs#site-builder**` — раздел сразу открыт, accordion раскрыт
3. **Скрин Help popover в `/admin/site-pages**` — открыт popover на поле «Якорь» / «Действие кнопки»
4. **Скрин клика «Подробнее»** — переход в `/docs#site-builder` именно в нужный раздел
5. **Скрин пути «с нуля»**: создание страницы → добавление блока → preview → publish (4 скрина)
6. **Скрин формы / embed-диалога / анкеты** (3 скрина)
7. **Скрин `/admin/forms**` с записью тестовой заявки
8. **Скрин карточки контакта** с вкладкой «Анкеты и заявки»
9. **grep-proof** — вывод `grep -rn` по `src/components/admin/site-builder/**` на английские UI-строки (Title|Description|Submit|Cancel|Save и т.д.) с явным результатом «0 совпадений» или явным списком оставшегося
10. **Список help-key** — `grep "site_builder\." src/constants/helpTexts.ts` с подсчётом

### Финальный отчёт

Один блок с:

- **Точный путь к разделу:** route `/docs`, hash `#site-builder`, вкладка `admin`, первая позиция в `adminSections`, видимая shortcut-карточка сверху
- **Entry points:** (1) кнопка в хедере `/admin/site-pages`, (2) shortcut-карточка в `/docs`, (3) Help popover «Подробнее» в каждом редакторе
- **Какие help-key ссылаются:** все 57 ключей `site_builder.*` → `/docs#site-builder`
- **Где встроены HelpIcon:** точные пути 4 файлов + строки
- **Proof русификации:** grep-вывод
- **Visual proof:** 10+ скриншотов выше
- **Чек-лист сотрудника:** 10 шагов, по 1 строке

### Что НЕ делаю

- Не меняю архитектуру Site Builder
- Не трогаю backend / RPC / RLS
- Не растягиваю на несколько сообщений

### DoD

Спринт закрыт когда: (1) клик «Подробнее» в любом popover открывает видимый раздел за 1 клик; (2) пользователь находит раздел из `/docs` за ≤2 клика через shortcut; (3) собран полный visual proof из авторизованной зоны; (4) grep подтверждает 0 английских UI-строк.