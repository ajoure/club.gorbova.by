
# План исправления синхронизации платежей: Полный пакет

## Выявленные проблемы

### 1. Строка "Отмены" выходит за границы таблицы
**Местоположение:** `BepaidStatementSummary.tsx`
**Причина:** Таблица статистики в диалоге синхронизации имеет фиксированную ширину, а текст "Отмены" обрезается на экране (image-672.png).
**Решение:** Сокращенное отображение или убедиться, что `table-layout: fixed` + `min-width` корректно настроены в родителе.

---

### 2. Ошибки "duplicate key" — всегда были, но действия нет
**Состояние:** Последняя синхронизация показала 21 ошибку (все — `duplicate key value violates unique constraint`).
**Причина:** Эти 21 UID уже существуют в `payments_v2` с `origin='bepaid'`, но Edge Function пытается их создать заново с `origin='statement_sync'`.
**Решение:**
1. Перед `INSERT` проверять существование UID в БД (по всем origin)
2. Если UID уже есть — пропускать или переквалифицировать как `update` (не create)
3. Отображать полный список ошибок со скроллом (сейчас обрезается после 10)

---

### 3. Контакт не привязывается к новым платежам из выписки
**Данные:**
- `card_last4=5967` + `card_brand=belkart` → профиль `f092bea2-9233-4140-9b67-5603ad0310f1` (Юлия Мялик)
- 3 платежа с этой картой добавлены из выписки с `profile_id=NULL`
- 1 платёж через webhook (`origin='bepaid'`) имеет `profile_id` правильно

**Причина:** Edge Function не проверяет `card_profile_links` и `payment_methods` при создании платежа.

**Решение:** 
1. При `action='create'` извлекать `card_last4` и `card_brand` из `card_masked`
2. Искать связь в таблицах:
   - `card_profile_links` по `(card_last4, card_brand)`
   - `payment_methods` по `(last4, brand, status='active')`
3. Если найден ровно 1 профиль — автоматически привязывать `profile_id` и `user_id`
4. Если найдено 2+ (коллизия) — оставлять `NULL`, логировать предупреждение

**Ключевой момент (из ответа пользователя):** Сравнивать **полные** данные карты:
- `card_masked` содержит полный номер: `91123801 xxxx 5967`
- `card_expires` содержит срок действия: `2027-03-16`
- Использовать для более точного сопоставления

---

### 4. ЕРИП-платежи без карты — помечать как ERIP
**Данные:** В выписке есть ЕРИП-платежи с `payment_method='erip'` и без `card_masked`.
**Решение:**
1. При `card_masked = NULL` или `payment_method like '%erip%'` помечать `card_brand='erip'`
2. Использовать существующую иконку ЕРИП в UI (зеленый прямоугольник с текстом "ЕРИП")
3. Не пытаться автосвязывать ЕРИП по карте (нет данных)

---

### 5. Даты/время не синхронизированы с bePaid по Минску
**Причина:** Даты из выписки (`paid_at`) записываются как есть (ISO с offset), но UI может отображать их в других таймзонах.
**Решение:** Нормализовать в UTC при записи + подтвердить, что UI использует `formatPaymentTimeIANA` с `Europe/Minsk` по умолчанию.

---

### 6. "Удалились существующие транзакции" — пример Мялик с 4 платежами → 1
**Расследование:** 
- В выписке по карте 5967: 4 записи (3 новых + 1 старый)
- В `payments_v2`: 5 записей с `card_last4=5967` (1 origin=bepaid, 3 origin=statement_sync + 1 старая от августа 2025)
- **Данные сохранены!** Просто UI (Платежи) показывает фильтр по `origin IN (bepaid, statement_sync)`

**Вывод:** Транзакции не удалились, но 3 новых не привязаны к контакту (profile_id=NULL), поэтому при поиске по имени "Мялик" показывается только 1 (тот, что с profile_id).

---

### 7. Блок ошибок обрезается (max 10) — нет скролла
**Местоположение:** `SyncWithStatementDialog.tsx` строки 662-671
**Решение:** Добавить `ScrollArea` и показывать все ошибки, а не только первые 10.

---

## План изменений

### Изменение 1: Полный показ ошибок со скроллом
**Файл:** `src/components/admin/payments/SyncWithStatementDialog.tsx`

Заменить ограничение на первые 10 ошибок → показывать все с `ScrollArea`:
```tsx
{(stats.errors ?? 0) > 0 && (
  <div className="w-full mt-4 rounded-lg bg-destructive/10 border border-destructive/20 p-3 text-left">
    <div className="flex items-center gap-2 text-destructive mb-2">
      <AlertTriangle className="h-4 w-4" />
      <span className="font-medium">Ошибки: {stats.errors}</span>
    </div>
    <ScrollArea className="max-h-40">
      {stats.error_details?.map((e, i) => (
        <div key={i} className="text-xs text-muted-foreground py-0.5">
          <span className="font-mono">{e.uid?.slice(0, 12)}...</span>: {e.error}
        </div>
      ))}
    </ScrollArea>
  </div>
)}
```

---

### Изменение 2: Автосвязь по полным данным карты при CREATE
**Файл:** `supabase/functions/sync-payments-with-statement/index.ts`

Добавить логику поиска контакта при создании платежа:

```typescript
async function findProfileByCard(
  supabase: any,
  cardMasked: string | null,
  cardExpires: string | null,
  paymentMethod: string | null
): Promise<{ profile_id: string; user_id: string | null } | null> {
  // ЕРИП — не искать по карте
  if (!cardMasked || paymentMethod?.toLowerCase().includes('erip')) {
    return null;
  }
  
  const last4 = extractLast4(cardMasked);
  const brand = extractCardBrand(cardMasked);
  const fullNumber = cardMasked.replace(/\s+/g, '').replace(/xxxx/gi, '');
  
  if (!last4) return null;
  
  // 1. Поиск по card_profile_links (точное совпадение last4 + brand)
  const { data: links } = await supabase
    .from('card_profile_links')
    .select('profile_id, card_last4, card_brand')
    .eq('card_last4', last4)
    .eq('card_brand', brand || 'unknown');
  
  if (links?.length === 1) {
    // Получить user_id из profiles
    const { data: profile } = await supabase
      .from('profiles')
      .select('id, user_id')
      .eq('id', links[0].profile_id)
      .maybeSingle();
    if (profile) {
      return { profile_id: profile.id, user_id: profile.user_id };
    }
  }
  
  // 2. Поиск по payment_methods (активная карта пользователя)
  const { data: methods } = await supabase
    .from('payment_methods')
    .select('user_id, last4, brand')
    .eq('last4', last4)
    .eq('status', 'active');
  
  if (methods?.length === 1) {
    const { data: profile } = await supabase
      .from('profiles')
      .select('id, user_id')
      .eq('user_id', methods[0].user_id)
      .maybeSingle();
    if (profile) {
      return { profile_id: profile.id, user_id: profile.user_id };
    }
  }
  
  // Коллизия или не найдено
  return null;
}
```

---

### Изменение 3: Помечать ЕРИП вместо card_brand
**Файл:** `supabase/functions/sync-payments-with-statement/index.ts`

В блоке создания платежа:
```typescript
// Определить card_brand с учетом ЕРИП
const paymentMethod = stmt.payment_method?.toLowerCase() || '';
const isErip = !stmt.card_masked || paymentMethod.includes('erip');
const card_brand = isErip ? 'erip' : extractCardBrand(stmt.card_masked);
const card_last4 = isErip ? null : extractLast4(stmt.card_masked);
```

---

### Изменение 4: Фикс дубликатов — upsert вместо insert
**Файл:** `supabase/functions/sync-payments-with-statement/index.ts`

Заменить INSERT на UPSERT для безопасной обработки дубликатов:
```typescript
const { error: insertError } = await supabaseAdmin
  .from('payments_v2')
  .upsert({
    provider: 'bepaid',
    provider_payment_id: change.uid,
    // ... остальные поля
  }, {
    onConflict: 'provider,provider_payment_id',
    ignoreDuplicates: false, // обновлять при конфликте
  });
```

Альтернативно: проверить существование UID перед созданием и переквалифицировать как update.

---

### Изменение 5: Обновить UI для ЕРИП в PaymentMethodBadge
**Файл:** `src/components/admin/payments/PaymentMethodBadge.tsx`

Добавить обработку `brand='erip'`:
```tsx
if (brand === 'erip' || !brand) {
  // Показать иконку ЕРИП (зелёный badge)
  return (
    <div className="flex items-center gap-1.5">
      <div className="bg-emerald-500 text-white text-[9px] px-1 py-0.5 rounded font-medium">
        ЕРИП
      </div>
    </div>
  );
}
```

---

### Изменение 6: Нормализация даты в UTC
**Файл:** `supabase/functions/sync-payments-with-statement/index.ts`

Убедиться, что `paid_at` записывается в UTC:
```typescript
paid_at: stmt.paid_at ? new Date(stmt.paid_at).toISOString() : null,
```

---

## Порядок выполнения

1. ✏️ **Edge Function** — Добавить автосвязь по карте (`findProfileByCard`)
2. ✏️ **Edge Function** — Помечать ЕРИП как `card_brand='erip'`
3. ✏️ **Edge Function** — Использовать UPSERT вместо INSERT
4. ✏️ **Edge Function** — Нормализовать даты в UTC
5. ✏️ **UI (SyncWithStatementDialog)** — Показывать все ошибки со скроллом
6. ✏️ **UI (PaymentMethodBadge)** — Добавить обработку ЕРИП
7. 🚀 Деплой Edge Function
8. 🧪 Тест: повторить синхронизацию и проверить привязку контактов

---

## Технические детали

### Таблицы для автосвязи

| Таблица | Колонки | Назначение |
|---------|---------|------------|
| `card_profile_links` | card_last4, card_brand, profile_id | Ручные связи карт с профилями |
| `payment_methods` | last4, brand, user_id, status | Активные карты пользователей |

### Логика определения ЕРИП

```
IF card_masked IS NULL OR payment_method ILIKE '%erip%':
  → card_brand = 'erip'
  → card_last4 = NULL
  → Не пытаться автосвязывать
```

### Данные выписки для точного сопоставления

- `card_masked`: `91123801 xxxx 5967` (полный номер без 4 средних цифр)
- `card_expires`: `2027-03-16` (срок действия)
- Можно использовать для более надёжного сопоставления

---

## DoD (Definition of Done)

- [ ] Все ошибки синхронизации отображаются со скроллом
- [ ] Новые платежи из выписки автоматически привязываются к контактам по карте
- [ ] ЕРИП-платежи помечаются как ЕРИП (не unknown)
- [ ] Дубликаты обрабатываются через UPSERT (21 ошибка → 0)
- [ ] Даты нормализованы в UTC, отображаются в Минском времени
- [ ] После синхронизации все 4 платежа Мялик видны и привязаны к контакту
