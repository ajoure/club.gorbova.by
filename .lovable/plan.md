
# Исправление Выписки BePaid — Динамические плашки + Sticky header

## Текущий статус данных

Проверил базу данных — **комиссии СОХРАНЕНЫ корректно**:
- 682 строки с данными
- Все 682 имеют `paid_at` (даты заполнены)
- Все 682 имеют `commission_total` (комиссии заполнены)
- Пример: `amount=250.00`, `commission_percent=2.40`, `commission_total=6.00`, `payout_amount=244.00`

**Вывод**: Если таблица показывает нули — нужно обновить страницу или проверить фильтр дат.

---

## Изменения для реализации

### 1. Плашки с динамическим подсчётом (как Excel)

**Логика:**
- Каждая плашка РЕАЛЬНО считает count + sum из отфильтрованных данных
- Клик на плашку = фильтр транзакций
- Повторный клик = сброс фильтра

**Типы фильтров:**
| Плашка | Фильтр | Условие |
|--------|--------|---------|
| Платежи | `payments` | status содержит "успешн" ИЛИ "successful" И transaction_type = "Платеж" |
| Возвраты | `refunds` | transaction_type содержит "возврат" ИЛИ "refund" |
| Отмены | `cancellations` | transaction_type содержит "отмена" ИЛИ "void" |
| Ошибки | `errors` | status содержит "ошибк" ИЛИ "failed" ИЛИ "declined" |

**Комиссия и Перечислено** — не фильтруемые, показывают суммы по всем транзакциям.

### 2. Sticky header таблицы

```css
/* Контейнер с ограниченной высотой */
.table-container {
  max-height: 70vh;
  overflow-y: auto;
}

/* Заголовки остаются на месте */
thead {
  position: sticky;
  top: 0;
  z-index: 30;
  background: hsl(var(--muted) / 0.95);
  backdrop-filter: blur(8px);
}

/* Sticky колонки (checkbox + UID) приоритетнее */
thead th.sticky-col {
  z-index: 40;
}
```

---

## Файлы для изменения

| Файл | Изменение |
|------|-----------|
| `src/components/admin/payments/BepaidStatementSummary.tsx` | Кликабельные плашки + активное состояние + добавить "Ошибки" |
| `src/components/admin/payments/BepaidStatementTable.tsx` | Sticky header (thead) |
| `src/components/admin/payments/BepaidStatementTabContent.tsx` | State `typeFilter` + фильтрация данных |
| `src/hooks/useBepaidStatement.ts` | Добавить `errors_count` и `errors_amount` в статистику |

---

## Детали реализации

### BepaidStatementSummary.tsx

```tsx
type FilterType = 'payments' | 'refunds' | 'cancellations' | 'errors' | null;

interface Props {
  stats: BepaidStatementStats;
  activeFilter: FilterType;
  onFilterChange: (filter: FilterType) => void;
}

// Плашка с кликом
<StatCard
  title="Платежи"
  value={formatAmount(stats.payments_amount)}
  subtitle={`${stats.payments_count} шт`}
  icon={<CreditCard />}
  variant="success"
  onClick={() => onFilterChange(activeFilter === 'payments' ? null : 'payments')}
  isActive={activeFilter === 'payments'}
/>

// Визуальное состояние активной плашки:
// - ring-2 ring-primary
// - scale-[1.02]
// - shadow-lg
```

### BepaidStatementTabContent.tsx

```tsx
const [typeFilter, setTypeFilter] = useState<FilterType>(null);

// Фильтрация данных
const filteredRows = useMemo(() => {
  if (!typeFilter) return rows;
  
  return rows.filter(row => {
    const txType = (row.transaction_type || '').toLowerCase();
    const status = (row.status || '').toLowerCase();
    
    switch (typeFilter) {
      case 'payments':
        return (status.includes('успешн') || status.includes('successful')) 
          && txType.includes('платеж');
      case 'refunds':
        return txType.includes('возврат') || txType.includes('refund');
      case 'cancellations':
        return txType.includes('отмена') || txType.includes('void');
      case 'errors':
        return status.includes('ошибк') || status.includes('failed') 
          || status.includes('declined') || status.includes('error');
      default:
        return true;
    }
  });
}, [rows, typeFilter]);

// Индикатор активного фильтра
{typeFilter && (
  <div className="flex items-center gap-2 px-3 py-1.5 rounded bg-primary/10">
    <span>Фильтр: {filterLabels[typeFilter]}</span>
    <Button size="sm" variant="ghost" onClick={() => setTypeFilter(null)}>
      ✕ Сбросить
    </Button>
  </div>
)}
```

### BepaidStatementTable.tsx — Sticky Header

```tsx
<div className="rounded-lg border overflow-hidden">
  {/* Контейнер со скроллом внутри */}
  <div className="max-h-[70vh] overflow-auto">
    <table className="w-full text-xs">
      {/* Sticky header */}
      <thead className="sticky top-0 z-30 bg-muted/95 backdrop-blur-sm">
        <tr className="border-b border-border/50">
          {/* Checkbox - sticky left + sticky top */}
          <th className="sticky left-0 top-0 z-40 bg-muted/95 ...">
          {/* UID - sticky left + sticky top */}
          <th className="sticky left-10 top-0 z-40 bg-muted/95 ...">
          {/* Остальные заголовки */}
        </tr>
      </thead>
      <tbody>...</tbody>
    </table>
  </div>
</div>
```

### useBepaidStatement.ts — Добавить статистику ошибок

```typescript
// В BepaidStatementStats добавить:
errors_count: number;
errors_amount: number;

// В подсчёте:
if (status.includes('ошибк') || status.includes('failed') || status.includes('error')) {
  stats.errors_count++;
  stats.errors_amount += Math.abs(amount);
}
```

---

## UI после изменений

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ [Импорт]  [Январь 2026 ▾]                      [🔍 Поиск...]                │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────┐  │
│ │▶Платежи │ │Возвраты │ │ Отмены  │ │ Ошибки  │ │Комиссия │ │Перечислено│  │
│ │ 51973   │ │ 2585    │ │  628    │ │  1250   │ │ 1125    │ │  47823    │  │
│ │ 388 шт  │ │ 19 шт   │ │ 81 шт   │ │ 45 шт   │ │         │ │           │  │
│ │[АКТИВНО]│ │         │ │         │ │         │ │         │ │           │  │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └───────────┘  │
│                                                                             │
│ ℹ️ Фильтр: Платежи (388 из 640)  [✕ Сбросить]                              │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────────────────────────────────────┐
│ │☑│UID  │ID заказа│Статус │Сумма│Комиссия│Перечислено│...  ← ЗАКРЕПЛЕНО    │
│ │═│═════│═════════│═══════│═════│════════│═══════════│═══                   │
│ │☐│99c..│601950603│Успешный│250  │ 6.00   │ 244.00    │...  ↓               │
│ │☐│920..│602529918│Успешный│100  │ 2.40   │  97.60    │...  ↓ СКРОЛЛ        │
│ │☐│1bd..│602578875│Успешный│250  │ 6.00   │ 244.00    │...  ↓               │
│ └───────────────────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Порядок выполнения

1. ✏️ `useBepaidStatement.ts` — добавить `errors_count/errors_amount`
2. ✏️ `BepaidStatementSummary.tsx` — кликабельные плашки + плашка "Ошибки"
3. ✏️ `BepaidStatementTabContent.tsx` — state `typeFilter` + фильтрация
4. ✏️ `BepaidStatementTable.tsx` — sticky header

---

## DoD (Definition of Done)

- Клик на "Платежи" → таблица показывает только успешные платежи
- Клик на "Возвраты" → только возвраты
- Клик на "Отмены" → только отмены
- Клик на "Ошибки" → только ошибки
- Повторный клик → сброс фильтра
- Активная плашка визуально выделена
- При скролле вниз заголовки таблицы остаются на месте
- Колонки комиссий показывают реальные данные (6.00, 2.40, 244.00)
