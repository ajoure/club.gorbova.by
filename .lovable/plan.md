да, согласен, с учетом правок:

&nbsp;

1. В разделе про навигацию зафиксируй не абстрактно tab=access, а **единый реальный контракт**:
  &nbsp;
  - вкладка продукта = access_rules;
  - открыть нужный экран нужно через один согласованный механизм: либо query/state, либо location state, но **не смесью разных способов**;
  - этот же механизм должен уметь:
    &nbsp;
    - открыть вкладку доступов,
    - открыть create training_content,
    - открыть edit конкретного [rule.id](http://rule.id).
    &nbsp;
  &nbsp;
2. В ProductAccessRulesTab добавь явный пункт:
  &nbsp;
  - если controlled-open для create/edit извне отсутствует, нужно сделать **минимальный add-only API** для внешнего открытия;
  - не создавать второй отдельный wizard и не дублировать flow настройки правил.
  &nbsp;
3. В блоке lesson counts зафиксируй жёстче:
  &nbsp;
  - нужен **один shared helper / один источник вычисления** для:
    &nbsp;
    - списка “Тренинги этого продукта”,
    - диалога “Привязать тренинг”;
    &nbsp;
  - если count не может быть посчитан корректно, **не показывать бейдж вообще**, а не выводить 0 уроков.
  &nbsp;
4. В разделе bind/rebind/unbind добавь:
  &nbsp;
  - для строки “уже привязан к этому продукту” должны быть доступны не только статус, но и **явные действия**:
    &nbsp;
    - Открыть,
    - Отвязать,
    - при необходимости Перепривязать заново;
    &nbsp;
  - не оставлять ни одного disabled-состояния без полезного действия.
  &nbsp;
5. В partial tree picker отдельно зафиксируй expected behavior модуля:
  &nbsp;
  - выбор модуля = выбор **всей его ветки**;
  - снятие модуля = снятие всей его ветки;
  - если вручную выбраны все уроки ветки, модуль должен переходить в checked;
  - если выбрана часть ветки, модуль в indeterminate.
  &nbsp;
6. В DoD добавь отдельным пунктом proof на root/module actions:
  &nbsp;
  - Весь тренинг реально выбирает весь root;
  - Выбрать всё и Снять всё реально меняют state дерева;
  - клик по модулю не требует выбирать уроки по одному.
  &nbsp;
7. В русификации зафиксируй замену всех текущих меток:
  &nbsp;
  - legacy → старый контур,
  - Другой продукт → перепривязать оставить на русском единообразно,
  - Привязан, Не привязан, Отвязать, Перепривязать, Создать правило, Редактировать правило — использовать последовательно во всех экранах.
  &nbsp;
8. В ProductAccessInfoBlock добавь, что это должен быть не просто информер, а **рабочая панель действий**:
  &nbsp;
  - показать, какой именно продукт управляет доступом;
  - показать summary правил именно для текущего тренинга;
  - дать прямой переход к настройке этого тренинга в доступах продукта.
  &nbsp;
9. В STOP-guards добавь ещё один:
  &nbsp;
  - если выяснится, что для корректной перепривязки/отвязки детей недостаточно текущего дерева и есть риск затронуть чужие связи, остановиться и сначала вывести dry-run preview того, какие module_id будут изменены.
  &nbsp;
10. В proof-пакет добавь отдельный consistency proof:

&nbsp;

&nbsp;

&nbsp;

- один и тот же тренинг в экране продукта и в диалоге привязки показывает одинаковый count уроков;
- product-linked тренинг больше нигде не показывает старый локальный selector;
- непривязанный тренинг честно показывает старый контур, без ложной “сквозной” настройки.

&nbsp;

&nbsp;

План:

1. Проблема

Нужно закрыть единый пакет по контуру доступа тренингов: убрать ложные “0 уроков”, сделать рабочие сценарии привязки/перепривязки/отвязки, починить древовидный partial-access picker, довести ProductAccessInfoBlock до рабочего workflow и устранить текущий конфликт навигации, из‑за которого все три кнопки ведут на тарифы вместо настройки доступов.

2. Диагностика

Факты по коду:

- `docs/ENGINEERING_RULES.md` прочитан; порядок Diagnose → Plan → Dry run → Execute → Verify соблюдаю.
- `ProductAccessInfoBlock` уже рендерится в `AdminTrainingModules` и `ContentCreationWizard` для `product_id != null`, но все действия ведут на `/admin/products-v2/${productId}?tab=access`, тогда как страница продукта использует вкладку `access_rules` и вообще не читает query params. Поэтому пользователь попадает на дефолтный таб `tariffs`.
- `AdminProductDetailV2.tsx` использует `<Tabs defaultValue="tariffs">`; deep-link на вкладку доступов сейчас отсутствует.
- `TrainingContentTreePicker.tsx` хранит выбор как два плоских массива и вычисляет состояние модуля не по полному поддереву. `Весь тренинг`, `Выбрать всё`, `Снять всё`, выбор модуля и indeterminate работают непоследовательно, потому что descendants/уроки не собираются рекурсивно.
- `useProductTrainings.ts` уже содержит `bindTraining`, `rebindTraining`, `unbindTraining`, preview-диалоги и рекурсивный helper `getAllDescendantIds`.
- Но `BindTrainingDialog` сейчас для уже привязанного к текущему продукту тренинга делает disabled-тупик, без рабочего сценария “перепривязать / отвязать / уже привязан”.
- `ProductLinkedTrainingsBlock` считает уроки через `countTreeLessons`, но в bind-диалоге счётчики вообще не показываются, а в основном списке возможны ложные нули из-за несинхронного/неунифицированного источника вычисления.
- В `useProductTrainings` bind/rebind/unbind сейчас массово обновляют весь descendants tree по `product_id`; отдельно нужно сохранить guard “не трогать чужие product_id вне дерева”.
- `ProductAccessRulesTab` уже умеет edit/create `training_content` rules и восстанавливает состояние из `conditions`, значит можно привязать переходы из readonly-блока к конкретному правилу/созданию.
- Термин `legacy` в UI уже частично заменён, но в диагностике/подсказках ещё нужна полная системная русификация и единообразие.

3. Предлагаемое решение

PATCH разделю на 5 связанных подзадач без смены Source of Truth:

A. Починить навигацию и workflow из `ProductAccessInfoBlock`

- Перевести все переходы на вкладку `access_rules`.
- Добавить deep-link/state для открытия нужной вкладки продукта и, при наличии `moduleId`/`rule.id`, открывать экран с фокусом на нужном тренинге/правиле.
- Кнопки блока должны различаться по действию:
  - Открыть продукт → карточка продукта
  - Вкладка «Доступы» → таб `access_rules`
  - Создать правило → таб `access_rules` + предзаполненный сценарий `training_content` для текущего `moduleId`
  - Редактировать правило → таб `access_rules` + открытие edit-dialog для `rule.id`

B. Нормализовать partial tree picker

- Перестроить логику выбора как иерархическую:
  - root = весь тренинг;
  - модуль = все descendant-модули и все descendant-уроки в ветке;
  - урок = только конкретный урок.
- Вынести helpers:
  - сбор всех descendants модулей/уроков;
  - вычисление checked / indeterminate по полному поддереву;
  - toggle root/module/lesson;
  - select all / clear all;
  - нормализацию payload без дублирования уроков, покрытых выбранным модулем.
- Сохранить контракт `allowed_module_ids` / `allowed_lesson_ids`, но сделать вычисление детерминированным.

C. Привязка / перепривязка / отвязка

- Доработать `BindTrainingDialog`, чтобы для каждого root-тренинга было понятное действие:
  - свободный → Привязать;
  - привязан к этому продукту → уже привязан + Отвязать;
  - привязан к другому продукту → Перепривязать к этому продукту.
- Убрать disabled-тупик без действий.
- Использовать уже существующие `bindTraining`, `rebindTraining`, `unbindTraining`, `getRebindPreview`, `getUnbindPreview`.
- При отвязке/перепривязке сохранить каскад только по descendants текущего дерева.
- Если потребуется guard на children с собственным `product_id`, сначала проверить, как сейчас устроено наследование в данных, и только затем добавить более точное условие обновления.

D. Единый источник lesson counts

- Унифицировать расчёт количества уроков для:
  - “Тренинги этого продукта”
  - “Привязать тренинг”
- Для root считать сумму по всей иерархии.
- Для child считать только свои уроки.
- Если надёжный count недоступен в конкретной поверхности, скрывать бейдж, а не показывать массовый “0 уроков”.
- По возможности считать из одного hook/helper, чтобы оба экрана использовали одинаковую формулу.

E. Русификация и единый сценарий

- Везде убрать английские слова из пользовательского UI.
- Для тренинга без `product_id` показать понятное объяснение: “Этот тренинг ещё не привязан к продукту. Пока используются старые настройки доступа.”
- Для тренинга с `product_id` закрепить модель: доступ настраивается только через продукт.
- В `ProductAccessInfoBlock` явно показать, почему локальная настройка недоступна и какой продукт управляет доступом.

4. Изменяемые компоненты

Основные файлы:

- `src/components/admin/trainings/ProductAccessInfoBlock.tsx`
- `src/pages/admin/AdminProductDetailV2.tsx`
- `src/components/admin/product/ProductAccessRulesTab.tsx`
- `src/components/admin/product/TrainingContentTreePicker.tsx`
- `src/components/admin/product/ProductLinkedTrainingsBlock.tsx`
- `src/hooks/useProductTrainings.ts`

Возможные дополнительные точки:

- `src/pages/admin/AdminTrainingModules.tsx`
- `src/components/admin/trainings/ContentCreationWizard.tsx`

БД / новые таблицы / RPC / edge functions:

- не планируются, если текущих hook/mutation достаточно.

5. Что не будет изменено

- Source of Truth остаётся прежним: `training_modules.product_id` для связи продукт ↔ тренинг.
- Формат `access_rules` и модель `training_content` не меняются.
- Клиент `src/integrations/supabase/client.ts` не трогаю.
- Не добавляю новый локальный контур доступа для product-linked тренингов.

6. Dry-run

Перед реализацией:

- Проверить все места, где ожидается `tab=access`, и заменить на единый контракт вкладки.
- Проверить, есть ли уже в `ProductAccessRulesTab` способ открыть create/edit-диалог извне; если нет — добавить минимальный controlled API через query/state, не создавая второй workflow.
- На дереве выбора прогнать на бумаге 5 сценариев:
  - root on/off;
  - select all / clear all;
  - модуль on/off;
  - часть уроков;
  - восстановление edit-state.
- Для привязки/перепривязки убедиться, что используются только root-тренинги и только descendants текущего дерева.

7. Execute

Порядок выполнения:

1. Навигация и deep-link вкладки “Доступы”.
2. Actionable workflow в `ProductAccessInfoBlock`.
3. Рекурсивная логика `TrainingContentTreePicker`.
4. Нормализация bind/rebind/unbind UI в `ProductLinkedTrainingsBlock`.
5. Унификация lesson counts.
6. Русификация helper-текстов/бейджей/диагностики.
7. Финальная проверка consistency на экранах тренинга и продукта.
8. STOP-guards

Остановить выполнение и пересогласовать, если:

- для открытия create/edit нужного правила из продукта потребуется новый параллельный wizard вместо переиспользования существующего `ProductAccessRulesTab`;
- окажется, что `training_modules.product_id` в children иногда хранит независимые значения и нельзя безопасно каскадно менять дерево без дополнительного правила различения “унаследован/явно задан”;
- lesson counts в bind-диалоге нельзя получить без тяжёлых новых запросов или дублирования логики;
- query/state для открытия вкладки/правила ломает текущую навигацию страницы продукта.

9. DoD

Патч считается выполненным, когда:

- в `ProductAccessInfoBlock` все кнопки ведут в правильные сценарии, а не на тарифы;
- “Открыть продукт” открывает карточку продукта;
- “Вкладка «Доступы»” открывает именно вкладку доступов;
- “Создать правило” реально ведёт к созданию `training_content` rule для текущего тренинга;
- “Редактировать правило” реально ведёт к редактированию конкретного правила;
- в partial tree picker работают:
  - Весь тренинг
  - Выбрать всё
  - Снять всё
  - выбор модуля как всей ветки
  - корректный indeterminate;
- сохранение partial rule даёт корректные `allowed_module_ids` и `allowed_lesson_ids`;
- edit-dialog восстанавливает состояние дерева корректно;
- в списке тренингов продукта нет ложных массовых `0 уроков`;
- экран “Привязать тренинг” поддерживает Привязать / Перепривязать / Отвязать без тупиковых состояний;
- все пользовательские подписи в этом контуре на русском;
- для `product_id != null` доступ визуально и логически настраивается только через продукт;
- для `product_id == null` UI честно объясняет, что пока используется старый контур.

10. Риски и зависимости

- Главная зависимость — возможность мягко открыть нужный state в `ProductAccessRulesTab` без большого рефакторинга.
- Есть риск, что для “Редактировать правило” придётся добавить controlled mode в таб доступов.
- Есть риск неоднозначности при отвязке/перепривязке descendants, если в данных встречаются нестандартные деревья.
- Есть риск, что lesson counts сейчас считаются в нескольких местах и потребуется вынести shared helper.

11. Требуется дополнительная информация

Критичных блокеров для плана нет. В реализации отдельно проверю, можно ли открыть create/edit нужного правила через query/state без расширения scope.

12. Proof-пакет после реализации

Обязательный proof:

- UI proof:
  - все 3 кнопки из `ProductAccessInfoBlock` ведут в правильные сценарии;
  - “Создать правило” и “Редактировать правило” реально рабочие;
  - bind/rebind/unbind доступны из экрана продукта;
  - partial tree picker работает по всем базовым сценариям.
- Data proof:
  - выбор модуля целиком сохраняет `allowed_module_ids`;
  - выбор отдельных уроков сохраняет `allowed_lesson_ids`;
  - select all / clear all дают валидный payload.
- Consistency proof:
  - “Тренинги этого продукта” и “Привязать тренинг” показывают одинаково рассчитанные lesson counts;
  - ложных 0 уроков нет;
  - product-linked тренинг не показывает вводящий в заблуждение локальный selector.