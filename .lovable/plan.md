да, согласен, с учетом правок:

1. **Расширить scope:** сейчас есть явный запрос проверить проблему доставки писем в спам. Поэтому DNS/DKIM/DMARC/SPF больше не out of scope.
2. **UI-правки оставить как есть:**
  - email step: кнопка **«Отправить»**;
  - details step: **«Получить код»**;
  - OTP step: подсказка про «Спам/Промоакции».
3. **Добавить DNS/email-deliverability диагностику через hoster.by:**
  - проверить SPF `gorbova.by`;
  - проверить DKIM для фактического отправителя OTP-писем;
  - проверить DMARC;
  - проверить MX;
  - проверить, нет ли лишних/конфликтующих SPF include;
  - проверить, какой домен реально стоит в `From`, `Return-Path`, `DKIM d=`, `SPF domain`.
4. **Обязательно определить фактического отправителя OTP.**  
Нужно взять реальное письмо с кодом и проверить заголовки:
  - `From`;
  - `Return-Path`;
  - `DKIM-Signature d=`;
  - `SPF`;
  - `DMARC`;
  - `Authentication-Results`.
5. **Исправлять не “на глаз”, а по результату заголовков.**  
Если OTP отправляет Lovable/Supabase/Resend/другой sender — править DNS именно под него.  
Если отправитель Яндекс — оставить только Яндекс.  
Если используется `sent.gorbova.by`/`notify.gorbova.by` — проверить, что он реально подтверждён и корректно делегирован.
6. **DNS-изменения делать только после dry-run.**  
Перед правками сохранить:
  - полный DNS before;
  - что именно ломает доставку;
  - какие записи будут удалены/изменены/добавлены.
7. **Не удалять рабочие записи Lovable/Яндекс без проверки.**  
SPF/DKIM/DMARC/MX менять только если подтверждено, что это влияет на текущую отправку OTP.
8. **DoD дополнить:**
  - OTP-письмо приходит;
  - в Gmail/другом тестовом ящике `SPF=PASS`, `DKIM=PASS`, `DMARC=PASS`;
  - письмо не попадает в спам на тестовом Gmail;
  - DNS before/after приложены;
  - UI-скриншоты двух шагов заявки приложены.
9. **Отчёт строго на русском.**

```text
План должен быть составлен на русском языке.
Отчет о выполненной работе должен быть составлен на русском языке.
Вся переписка, все пояснения и все результаты должны предоставляться только на русском языке.

План:
```

## Контекст

Заявка (`LeadRequestDialog`) и все inline-flows идут через `InlineEmailOtpForm`. На шаге ввода email кнопка сейчас показывает «Получить код» — из-за override в `InlineAuthForm.tsx:58`. Пользователь просит:

1. На первом шаге (email) — кнопка «Отправить» (не «Получить код» / «Отправить код»). Логически: код на этом шаге ещё не отправляется, идёт только проверка на существующего пользователя.
2. На втором шаге (details: имя/фамилия/телефон для новых) — оставить «Получить код» (уже так).
3. На шаге OTP («Код отправлен на …») добавить подсказку: письмо может попасть в спам — проверьте папку «Спам».

Отдельно: проблему реального попадания писем в спам (DNS/DKIM/DMARC/warmup) в этой задаче не трогаем — пользователь пишет «постараюсь ещё раз проверить»; вернёмся отдельным пунктом при явном запросе.

## Изменения (только UI, без бизнес-логики)

### 1. `src/components/auth/InlineAuthForm.tsx`, строка 58

Убрать override, который превращает «Продолжить» в «Получить код» для OTP-режима:

```tsx
emailCtaLabel={emailCtaLabel}
```

То есть форма заявки, передавая `emailCtaLabel="Отправить"`, получит именно «Отправить». Другие вызовы (PublicPayPage) продолжат передавать своё значение.

### 2. `src/components/lead/LeadRequestDialog.tsx`, строка 192

Заменить `emailCtaLabel="Продолжить"` на `emailCtaLabel="Отправить"`.

### 3. `src/pages/PublicPayPage.tsx`

Проверить текущий `emailCtaLabel` — если стоит «Продолжить», оставляем; менять только по явному запросу. (Смысловая правка касается только формы заявки.)

### 4. `src/components/auth/InlineEmailOtpForm.tsx`, блок `auth.step === "sent"` (строки 193–197)

Под строкой «Код отправлен на … Введите 6 цифр из письма» добавить мягкое предупреждение о спаме:

```tsx
<p className="text-xs text-muted-foreground">
  Если письма нет во «Входящих» — проверьте папку «Спам»/«Промоакции».
</p>
```

Стиль: `text-xs text-muted-foreground`, без иконок, без alert-цвета — не пугает, просто подсказывает.

## Definition of Done

- На `/ideologicheskaya-rabota` в модалке «Оставить заявку» на шаге email кнопка называется **«Отправить»**.
- После отправки email на экране OTP под строкой «Код отправлен на …» видна подсказка про папку «Спам».
- На шаге деталей (новый пользователь: имя/фамилия/телефон) кнопка остаётся **«Получить код»** (уже так, не трогаем).
- Верификация: preview → скриншот двух шагов формы заявки.

## Что НЕ входит

- Диагностика/фикс реального попадания писем в спам (DKIM/DMARC/warmup домена `notify.gorbova.by`) — по запросу пользователя откладываем.
- Правки других форм авторизации (payment, site-renderer форм) — тексты не меняем.