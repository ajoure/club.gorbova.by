## да, согласен, с учетом правок:

1. **Не использовать** `supabase--insert` **для обновления правила.**  
Нужно именно безопасное обновление существующей строки:
2. **Перед UPDATE сохранить before/after правила.**
3. **В коде не подгружать все тарифы без фильтра по product_id.**  
Только:
4. **Если у продукта нет активных тарифов — не открывать доступ bypass-ом.**  
В таком случае rule должен считаться gated, но без успешных покупок → закрыто.
5. **Добавить защиту от дублей payload.**  
Один и тот же `(lesson_id, tariff_id, content_month)` не должен уходить в RPC несколько раз.
6. **Проверить** `content_month` **на sub-module/lesson уровне.**  
Не брать месяц с родителя «База знаний», если фактический месяц хранится на дочернем модуле/уроке.
7. **В Verify добавить конкретный тест по продукту** `prd_cb0dc5b0d157`**.**  
Проверить:
  - оплаченный месяц открыт;
  - неоплаченный месяц закрыт;
  - модуль «Видеоответы» продолжает работать как раньше.
8. **Отчет по выполнению должен приложить:**
  - diff `conditions` до/после;
  - количество payload-тюплов в `has_month_purchase_bulk`;
  - proof по BUSINESS / FULL / Club ИДЕОЛОГИЯ / продукту «Gorbova Club — идеология».
9. **Обязательное требование для [Lovable.dev](http://Lovable.dev):**

```text
План должен быть составлен на русском языке.
Отчет о выполненной работе должен быть составлен на русском языке.
Вся переписка, все пояснения и все результаты должны предоставляться только на русском языке.

Отчет о выполнении диагностики
```

Проверил, кто по факту видит «Итоги месяца» (модуль `itogi-mesyaca`, родитель — «База знаний»).


| Кто                                                         | Правило `access_rules`                                             | Что реально видит сейчас            |
| ----------------------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------- |
| **Club — BUSINESS**                                         | `full` + `match_purchase_month=true`                               | Итоги только за оплаченные месяцы ✅ |
| **Club — FULL**                                             | `partial` с allowed_module_ids=[Видеоответы, Итоги], гейт выключен | Все Итоги без ограничений ✅         |
| **Club — ИДЕОЛОГИЯ** (тариф)                                | `full` + `match_purchase_month=true`                               | Итоги только за оплаченные месяцы ✅ |
| **Продукт «Gorbova Club — идеология»** (`prd_cb0dc5b0d157`) | `partial`, allowed_module_ids=[Видеоответы] — **Итогов нет**       | «Итоги месяца» **не открываются** ❌ |


Также обнаружено ограничение в рантайме: `useMonthGate` умеет считать месяц-гейт только по tariff-scoped правилам. Для product-only правила (как be85cbdd у продукта «Идеология»), даже если админ поставит `match_purchase_month=true`, гейт не сработает — сработает bypass и откроет всё сразу. Поэтому одного правки правила недостаточно, нужен код-фикс, чтобы «всё делалось через настройки администратора без хардкода», как вы попросили.

## План (Diagnose → Plan → Dry run → Execute → Verify)

### Часть 1 — Код: универсальный месяц-гейт для product-only правил

Файл `src/hooks/useMonthGate.ts`:

1. При обходе кандидатов на bypass дополнительно проверять `conditions.match_purchase_month === true`.
2. Если true — не считать урок «полностью открытым». Вместо этого:
  - подгрузить активные `tariffs` этого `product_id` (id, product_id, is_active);
  - для каждого урока, попавшего под allow-скоуп такого правила, добавить в payload RPC `has_month_purchase_bulk` тапл `(lesson_id, tariff_id, content_month)` **по каждому тарифу продукта**;
  - агрегировать OR: урок открыт, если пользователь оплатил этот месяц хотя бы по одному из тарифов продукта.
3. Гейт по-прежнему пропускает уроки без `content_month` и правила без `match_purchase_month`.
4. Совместимость: логика для tariff-scoped правил (BUSINESS, ИДЕОЛОГИЯ, FULL) не меняется.

### Часть 2 — Данные: правило продукта «Идеология»

Через `supabase--insert` обновить `access_rules` id `be85cbdd-3f3a-4c96-b319-4ca28ebfc9f7`:

- добавить `81cf626a-798e-4a17-aa14-8d55314b588d` (модуль «Итоги месяца») в `conditions.allowed_module_ids` рядом с уже имеющимся модулем «Видеоответы»;
- проставить `conditions.match_purchase_month = true`, чтобы применялся гейт по месяцам покупки продукта;
- `is_active`, `access_mode`, `product_id`, `target_ref` — не трогать.

Никаких новых таблиц, RPC, миграций — только данные и одна фронтовая функция.

### Часть 3 — Проверка (Verify / DoD)

1. Sanity в БД: подтвердить, что правило be85cbdd имеет оба модуля в allowlist и `match_purchase_month=true`.
2. Для пользователя без покупок продукта «Идеология» — «Итоги месяца» остаются закрыты с бейджем «Доступно за отдельную плату» (текст оставляем прежний).
3. Для пользователя с одним оплаченным `orders_v2` по любому из 4 тарифов продукта «Идеология» с `meta.deal_month = 'YYYY-MM'` — открывается ровно этот месяц Итогов.
4. Регресс: покупатель тарифа BUSINESS — по-прежнему видит только оплаченные месяцы; FULL — всё; ИДЕОЛОГИЯ (тариф Club) — только оплаченные месяцы.
5. Админ, меняя `allowed_module_ids` и галочку `match_purchase_month` в UI правил, получает предсказуемый результат без правок кода.

## Технические детали (для инженеров)

- **RPC не трогаем**: `has_month_purchase_bulk` уже принимает произвольные пары `(lesson_id, tariff_id, content_month)` — этого достаточно, чтобы прогнать все тарифы продукта.
- **Bypass-ветка `useMonthGate**` сейчас однозначно кладёт уроки в `bypassLessonIds/bypassModuleIds`. Новая ветка: если у правила `match_purchase_month=true`, оно НЕ добавляется в bypass-сеты, а порождает дополнительные payload-тюплы по всем `tariffs.id` этого `product_id` (только `is_active=true`).
- **Агрегация**: сохранить принцип OR-per-lesson, уже реализованный ниже; урок открыт, если пришёл `has_purchase=true` хоть по одному синтетическому ключу — независимо от того, tariff-scoped или product-scoped было исходное правило.
- **Кеш ключа `useEffect**` менять не нужно — набор `lesson_id` не меняется от новой ветки.
- **Затрагиваемая таблица**: только строка `access_rules.id = be85cbdd-3f3a-4c96-b319-4ca28ebfc9f7`, поле `conditions` (jsonb-merge).