## да, согласен, с учетом правок:

&nbsp;

1. Browser-proof по скроллу и UX preview можно **не выполнять** в этом спринте, так как визуально по факту у тебя это уже подтверждено и критичных замечаний больше нет.
2. Но это нужно оформить корректно в отчёте и [plan.md](http://plan.md):
  &nbsp;
  - не писать, что proof выполнен;
  - написать, что **proof сознательно пропущен по решению заказчика**;
  - статус указать как: **accepted without additional browser-proof** / **закрыто без дополнительного proof по согласованию**.
  &nbsp;
3. Зафиксировать, что:
  &nbsp;
  - все PATCH по UX preview, reset, reason labels, admin override и scroll считаются принятыми;
  - проверка полного browser-proof на 5 сценариев **снята из обязательного DoD** именно для этого спринта;
  - при появлении новых багов по preview/scroll вернуть это отдельным PATCH без пересборки всего спринта.
  &nbsp;
4. По продуктам, ID, правилам доступа и привязкам сейчас ничего дополнительно не копать:
  &nbsp;
  - считать текущую сверку достаточной;
  - отдельный proof по product_id / access_rules / training bindings не расширять;
  - оставить только уже собранные discovery-артефакты.
  &nbsp;
5. Drift по срокам бонусных/дочерних доступов не трогать в этом спринте.
  &nbsp;
  - оставить отдельным backlog/PATCH:
    **PATCH-ACCESS-DURATION-ALIGNMENT**
  - текущий спринт на этом не задерживать.
  &nbsp;
6. В финальном отчёте прямо указать:
  &nbsp;
  - «Дополнительный browser-proof по scroll/preview не выполнялся по согласованию с заказчиком, т.к. визуально функциональность подтверждена и замечаний по работе больше нет».
  &nbsp;
7. После этого этот спринт можно считать завершённым по текущему scope, без возврата к дополнительному proof.

&nbsp;

&nbsp;

План: Browser proof для PATCH-BULK-EXTEND-PREVIEW-UX-CLEANUP + SCROLL-BUG

### Текущее состояние кода

Все PATCH-и уже реализованы в `BulkExtendAccessDialog.tsx`:

- **REASON_META** с человекочитаемыми текстами — строки 25-40 ✅
- **getReasonLabel()** для fallback — строки 42-45 ✅
- **PreviewCard** как единый renderer — строки 67-118 ✅
- **3 секции** (К продлению → Пропущено → Заблокировано) — строки 491-536 ✅
- **Нативный scroll** (`overflow-y-auto`, `min-h-0`, `overscroll-contain`) — строка 484 ✅
- **scrollContainerRef** с `scrollTop = 0` при reset/reopen/step change — строки 144, 157-160, 185-188 ✅
- **snapshotRef** для immutable preview — строки 140, 205, 327 ✅
- **resetState** сбрасывает всё — строки 147-161 ✅
- **Empty state fallback** — строки 486-489 ✅

### Код менять не нужно

Все изменения уже применены. Задача — **browser proof**.

### Что нужно сделать

#### Browser proof: 5 сценариев

Открыть `/admin/deals` в browser и выполнить:

1. **Валидный кейс** — выбрать оплаченные сделки с active sub → preview → зелёная секция «К продлению» с человекочитаемым текстом
2. **Заблокировано** — найти сделку без active rule или без user_id → preview → красная секция «Заблокировано» с текстом вроде «Нет привязки к пользователю...»
3. **Admin override (amber)** — найти expired/historical сделку → preview → amber карточка с текстом «Админ-продление: предыдущий срок истёк...»
4. **Cancel + reset** — нажать Отмена → выбрать другие сделки → открыть заново → убедиться, что старые строки не залипают
5. **Scroll** — на наборе 20+ строк проверить вертикальный скролл колесом, footer всегда виден, после reopen scroll сверху

#### Известный blocker

При предыдущих попытках browser-proof RLS блокировал доступ к orders_v2 (0 результатов). Если это повторится — фиксируем как blocker, фаза Г остаётся proof-in-progress.

#### Backlog (не в этом PATCH)

- **PATCH-ACCESS-DURATION-ALIGNMENT** — сверка и выравнивание сроков бонусных/дочерних доступов относительно основного продукта. Drift-кейсы уже выгружены в `18_proof_duration_drift_discovery.csv` и `20_proof_drift_backlog.csv`. Не чинить без отдельного подтверждения.

### Последовательность

```text
1. Browser: navigate /admin/deals
2. Проверить видимость сделок (RLS check)
3. Если есть данные → 5 сценариев со screenshots
4. Если RLS блокирует → зафиксировать blocker
5. Обновить plan.md по результатам
```

### DoD


| Пункт        | Критерий                                                                      |
| ------------ | ----------------------------------------------------------------------------- |
| Сценарий 1   | Screenshot зелёной секции «К продлению»                                       |
| Сценарий 2   | Screenshot красной секции «Заблокировано» с человекочитаемым текстом          |
| Сценарий 3   | Screenshot amber карточки admin override                                      |
| Сценарий 4   | Screenshot чистого preview после cancel+reselect                              |
| Сценарий 5   | Screenshot работающего scroll на 20+ строках, footer виден                    |
| Scroll reset | После reopen preview начинается сверху                                        |
| Тексты       | Нет `не_оплачено` / `нет_user_id` как основного текста — только badge         |
| Секции       | Строгий порядок: К продлению → Пропущено → Заблокировано                      |
| Backlog      | Drift оформлен как PATCH-ACCESS-DURATION-ALIGNMENT, не чинится в этом спринте |
