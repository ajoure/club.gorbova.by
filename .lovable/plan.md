да, согласен, с учетом правок:

&nbsp;

1. Для бейджа успешных списаний не использовать приблизительный fallback по датам, если он не гарантирует точное количество именно успешных списаний.  
Если в данных нет надёжного поля с числом успешных списаний, бейдж лучше скрывать, чем показывать оценку.
2. В плане явно зафиксировать источник истины для бейджа:  

  - сначала брать явный счётчик успешных списаний из данных подписки / meta;
  - только если точно известно, что поле хранит именно successful charges;
  - если такой уверенности нет — count = null, бейдж не показывать.
3. &nbsp;
4. Пункт про mobile tooltip нужно уточнить:  
Tooltip не считать автоматически надёжным tap-решением для mobile.  
Зафиксировать так:  

  - desktop: hover/focus tooltip;
  - mobile: tap по бейджу с контролируемым открытием tooltip/popover, без зависимости от hover.
5. &nbsp;
6. whitespace-nowrap применять только к короткой строке бейджей, но не ко всем строкам метаданных целиком, иначе на узких экранах возможен некрасивый выезд текста.  
Для дат и сумм лучше сохранить компактность через leading-tight и аккуратные переносы, а не жёстко запрещать перенос везде.
7. В DoD уточнить отдельно:  

  - бейдж показывает только точное количество успешных списаний;
  - если точный count недоступен, бейдж не рендерится;
  - 0 показывается только когда система точно знает, что успешных списаний было ноль.
8. &nbsp;

&nbsp;

&nbsp;

Копируемый блок для Lovable:

План: мобильная оптимизация карточки подписки + круглый янтарный бейдж успешных списаний

&nbsp;

Файл: `src/components/admin/ContactDetailSheet.tsx`

&nbsp;

1. Бейдж количества успешных списаний

- заменить текущую серую плашку `{count} спис.` на компактный круглый янтарный индикатор;

- внутри только цифра, без текста;

- стиль: мягкий янтарный, вторичный по визуальному приоритету, не ярче статуса active;

- размер компактный, круглый, с аккуратным внутренним бликом;

- если `count === 0` — показывать `0`;

- если `count === null` — бейдж скрывать.

&nbsp;

2. Критически важное правило по данным

- бейдж должен показывать только **точное количество успешных списаний**;

- не использовать приблизительный расчёт по `created_at / last_charge_at / interval_days`, если он не гарантирует именно successful charges;

- источник истины:

  1) явное поле количества успешных списаний в данных подписки / meta;

  2) только если точно подтверждено, что это именно successful charges;

  3) если точного поля нет — `count = null`, бейдж не показывать.

&nbsp;

3. Tooltip / подсказка

- текст подсказки: `Количество успешных списаний по подписке`;

- desktop: hover/focus;

- mobile: tap по бейджу с контролируемым открытием tooltip/popover;

- не полагаться на hover-only поведение как на полноценное mobile-решение.

&nbsp;

4. Верхняя строка бейджей

- бейджи статуса, провайдера и счётчика держать в одну линию;

- убрать переносы именно у строки бейджей;

- карту `card_last4`) вынести в отдельную строку ниже.

&nbsp;

5. Метаданные карточки

- сделать блок компактнее по вертикали;

- уменьшить лишние отступы между строками;

- использовать `leading-tight` / `space-y-0.5`;

- не ставить жёсткий `whitespace-nowrap` на весь блок метаданных, чтобы не ломать узкие экраны;

- даты и суммы должны читаться аккуратно и без визуального мусора.

&nbsp;

Что не менять:

- бизнес-логику подписок;

- другие блоки карточки контакта;

- общую структуру секции, кроме описанных UI-улучшений.

&nbsp;

DoD:

- бейдж круглый, янтарный, внутри только цифра;

- бейдж показывает только точное число успешных списаний;

- при отсутствии точного значения бейдж скрыт;

- `0` показывается только если система точно знает, что успешных списаний ноль;

- tooltip на desktop работает по hover/focus, на mobile — по tap;

- строка бейджей в одну линию, карта отдельно;

- метаданные компактнее и визуально чище на mobile.

&nbsp;

## План: Мобильная оптимизация карточки подписки + круглый янтарный бейдж успешных списаний

### Что меняется

**Файл: `src/components/admin/ContactDetailSheet.tsx**`

#### 1. Бейдж количества списаний (строки 1958-1970)

Заменить текущую серую плашку `{count} спис.` на компактный круглый янтарный индикатор:

- Только цифра внутри, без текста «спис.»
- Стиль: `bg-amber-100/70 border-amber-200/50 text-amber-700 shadow-[inset_0_1px_0_rgba(255,255,255,0.5)]` — мягкий жёлто-янтарный с лёгким внутренним бликом
- Размер: `min-w-[20px] h-[20px] text-[10px] rounded-full`
- Визуально вторичный, не ярче статуса `active`

Обернуть в `<Tooltip>` из radix (уже импортирован) вместо нативного `title`:

- Текст: «Количество успешных списаний по подписке»
- Desktop: hover
- Mobile: tap/long-press — работает из коробки через radix `<Tooltip>` с `delayDuration={0}`

При `count === null` — бейдж скрыт. При `count === 0` — показывать `0`.

#### 2. Верхняя строка бейджей (строка 1948)

- Убрать `flex-wrap` — бейджи в одну линию
- Карту (`card_last4`) вынести из этого `div` в отдельную строку ниже
- Добавить `whitespace-nowrap` на строку бейджей

#### 3. Метаданные (строки 1977-1996)

- Уменьшить вертикальные отступы: убрать `mt-1` у первой строки, использовать `leading-tight` или `space-y-0.5` обёртку
- Добавить `whitespace-nowrap` на строки дат
- Суммы и даты не truncate

#### 4. Логика данных — без изменений

`getSubscriptionChargeCount` уже корректно ищет `successful_charges` / `charge_count` в meta, fallback по датам считает успешные (last_charge_at = дата последнего успешного). Формулировку в tooltip обновить на «успешных».

### Что НЕ меняется

- `src/utils/subscriptionChargeCount.ts` — логика расчёта без изменений
- Другие блоки карточки контакта
- Бизнес-логика подписок

### DoD

- Бейдж — круглый, янтарный, только цифра
- Tooltip работает на desktop (hover) и mobile (tap)
- Текст tooltip: «Количество успешных списаний по подписке»
- Строка бейджей в одну линию, карта отдельно
- Метаданные компактнее, без лишних переносов
- При `count = 0` показывается `0`, при `null` бейдж скрыт