## да, согласен, с учетом правок:

1. Перед заменой проверить фактические prop-контракты существующих `DatePicker` и `DateTimePicker`. Не создавать параллельные адаптеры, если компоненты уже принимают строковое значение или имеют готовые helpers сериализации.
2. Конвертацию дат вынести из JSX в чистые функции:
  &nbsp;
  ```text
  parseDateValue
  serializeDateValue
  parseDateTimeValue
  serializeDateTimeValue
  ```
  Пустое и невалидное значение не должно превращаться в `Invalid Date` или менять draft без действия пользователя.
3. Не использовать `new Date('YYYY-MM-DD')` напрямую: это может дать UTC-сдвиг даты. Для `date` разбирать строку по компонентам и создавать локальную дату через `new Date(year, month - 1, day)`.
4. Для `datetime` сохранить точный текущий контракт без timezone-конвертации:
  &nbsp;
  ```text
  YYYY-MM-DDTHH:mm
  ```
  Выбор даты не должен сбрасывать ранее выбранное время, а выбор времени — дату. При очистке даты итоговое значение очищается целиком.
5. Для поля `time` использовать канонический `Input`, а не нативный `type="time"`. Добавить:
  - placeholder `HH:mm`;
  - `inputMode="numeric"`;
  - нормализацию и проверку диапазона `00:00–23:59`;
  - отсутствие сохранения частично введённого невалидного времени как корректного значения.
6. Для поля `year` добавить:
  &nbsp;
  ```text
  inputMode="numeric"
  maxLength={4}
  ```
  и принимать только четыре цифры без изменения строкового формата хранения.
7. Правило col-span определять по типу и фактической длине options через отдельный helper. Не делать любой `select` автоматически широким: обычный короткий select остаётся в одной колонке, длинный — занимает две.
8. На мобильном проверить popover календарей:
  - не выходит за viewport;
  - не перекрывается контейнером с `overflow`;
  - остаётся доступным при экранной клавиатуре;
  - имеет достаточную ширину для ручного ввода.
9. Не скрывать полезную информацию вместе с техническим ID. Удалить только `pf-XXXXXX`; название, обязательность, help-текст и число документов сохраняются.
10. Добавить UI-тесты:
  - загрузка и сохранение `date`;
  - загрузка и сохранение `datetime` без timezone-shift;
  - очистка значений;
  - сохранение времени при смене даты;
  - узкие `year` и `number`;
  - отсутствие `pf-XXXXXX` в клиентском DOM;
  - одна колонка на mobile-классе и две на desktop;
  - существующие select/multiselect/checkbox не сломаны.
11. После изменения прогнать не только сборку, но и существующие тесты `PackageFieldsClientForm`, smart-date и session values. Форматы draft/payload должны остаться полностью совместимыми с RPC.

Остальной scope сохраняется: только UI-компонент анкеты, без изменений backend, RPC, БД и генерации.

&nbsp;

План: компактная анкета полей пакета на канонических компонентах

### Проблема

В `PackageFieldsClientForm` каждое поле растянуто на всю ширину, используются нативные `<input type="date|datetime-local|number>"` (бракованный системный календарь), `pf-XXXXXX` показывается клиенту. Это не соответствует дизайну платформы.

### Что меняется (только `src/components/ai-documents/packages/PackageFieldsClientForm.tsx`)

1. **Канонический календарь.** Все поля типа `date` заменяются на `DatePicker` (`@/components/ui/date-picker`) — единый компонент с popover-календарём, ru-локалью, ручным вводом, кнопками «Очистить/Сегодня». Формат хранения уже совпадает (`yyyy-MM-dd`).
2. **Канонический datetime.** Поля `datetime` заменяются на `DateTimePicker` (`@/components/ui/datetime-picker`) с табами «Дата/Время». В draft значение сериализуется как `YYYY-MM-DDTHH:mm` (как сейчас), с конвертацией в/из `{date: Date, time: "HH:mm"}` внутри `FieldRow`.
3. **Никаких нативных календарей.** Убираем `<input type="date">`, `<input type="datetime-local">`, `<input type="time">` (time — заменяем на маленький `<Input>` с маской `HH:mm` шириной ~120px; либо оставляем нативный time в узкой обёртке — он не страдает от уродского календаря, но всё равно сожмём по ширине).
4. **Сетка 2 колонки для компактных полей.** Список вопросов оборачиваем в `grid grid-cols-1 md:grid-cols-2 gap-3`. Поля `text` (Textarea), `multiselect` и `select` с длинными опциями принудительно занимают обе колонки (`md:col-span-2`). `number`, `year`, `date`, `datetime`, `time`, `checkbox`, обычный `select` — одна колонка.
5. **Узкие поля для коротких значений.** `year` — `<Input>` шириной `max-w-[140px]`; `number` (Номер приказа и т.п.) — `max-w-[260px]`.
6. **Чистый label.** Убираем `pf-XXXXXX` бейдж из лейбла клиента (это технический ID, клиенту не нужен). Бейдж «в N док.» оставляем — но мельче (`text-[9px] h-3.5`). Звёздочка `*` для обязательных — рядом с названием, в `text-destructive`.
7. **Хелп-текст.** Подсказка под полем `text-[10px] text-muted-foreground leading-snug` (сейчас `text-[11px]`), без переноса в отдельный абзац-блок.
8. **Карточка.** Внутренние отступы: `p-3 space-y-3` (вместо `p-4 space-y-4`). Заголовок «Поля пакета» — `text-xs font-semibold`. Прогресс-бейджи: компактные `h-5 text-[10px]`.

### Что НЕ трогаем

- `usePackageSessionFields`, RPC `upsert_session_field_values`, smart-date prefill, дедуп вопросов, валидация на сервере, форматы хранения (`yyyy-MM-dd`, `YYYY-MM-DDTHH:mm`, `HH:mm`, число строкой, JSON для multiselect) — всё совместимо.
- `DocumentPackageQuestionnairesView`, `PackageFieldsAssignmentPanel`, `PackageGenerationPanel`.
- Канонические компоненты `date-picker.tsx`, `datetime-picker.tsx`, `calendar.tsx` — используются как есть.

### DoD

- Никаких нативных `type=date`/`datetime-local` в анкете; все даты — через `DatePicker`/`DateTimePicker` с тем же стилем, что в `AdminEntitlements`, `EditDealDialog` и т.д.
- Год — узкое поле в 4 цифры, не на всю ширину.
- Лейблы без `pf-XXXXXX`.
- Поля выложены в 2 колонки на десктопе, 1 колонка на мобильном.
- Существующие значения корректно загружаются и сохраняются (та же сериализация).
- Сборка зелёная.