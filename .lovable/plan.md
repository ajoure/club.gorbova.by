да, согласен, с учетом правок:

1. По когорте **B** выбрать вариант **(a)**: откатывать только строки, где current == backup × 3 с tolerance 0.001.
2. Перед execute обязательно показать dry-run:
  - сколько строк A/B/C;
  - сколько будет откатано;
  - сколько останется без изменений;
  - отдельно Наталья Новикова before/after.
3. В audit_logs писать не только user, но и:
  - lesson_id;
  - rows_reverted;
  - rows_kept;
  - backup_table;
  - revert_backup_table.
4. Execute только после отдельного подтверждения dry-run.

В остальном план корректный.

## Diagnose

Тренинг «Бухгалтерия как бизнес» (`module_id=96a92019…`). Затронуто 2 урока:

- `96c970e6…` — «Тест: В какой роли вы находитесь сейчас» (V1, `pointA_rows[].income`)
- `6fb911a0…` — «Шаг 2: Анализ и формирование портфеля клиентов» (V2, `pointA_v2_rows[].monthly_income`)

В Шаге 3 (`1f69f658…`) воркшоп `ExternalProductWorkshop` импортирует `monthly_income` из Шага 2 через `loadPortfolioFromPreviousLesson` и подставляет в `current_price` — отдельной арифметики не делает.

### Что реально происходит

1. **В коде нет рантайм-×3.** Проверено `rg` по `src/lib/loadPortfolioFromPreviousLesson.ts`, `src/lib/diagnosticTableV1toV2.ts`, `src/components/lesson/**`, `ExternalProductWorkshop.tsx`, `DiagnosticTableBlock.tsx` — ни одна функция не умножает доход на 3, не делает USD→BYN-конверсию, ни одного триггера `income / monthly_income` в БД (`information_schema.triggers` чистый).
2. **×3 пришёл одной разовой миграцией от 04.05.2026** (chat msg #9229): UPDATE `pointA_rows[].income * 3` и `pointA_v2_rows[].monthly_income * 3` для двух уроков, бэкап в `lesson_progress_state_backup_byn_2026_05` (63 строки, RLS только superadmin). Тогда же баннер заменён на «Все суммы указывайте в BYN».
3. **Сейчас в БД**: значения у Натальи Новиковой (`8c3b6be5…`) — `1500/3000/3600/4500/12300/9000` — это явные ×3 от исходных `500/1000/1200/1500/4100/3000`. Шаг 3 импортирует именно эти 3×-значения, что и наблюдается в тикете ТКТ-26-26137.
4. **С 04.05 после миграции**: 11 строк (на 34 пользователя) были обновлены позже (часть — реальные правки в BYN, часть — те, кто продолжает заполнять и видит уже умноженные числа).

### Вывод

«Умножение» — это не активный код, а единственный артефакт one-shot миграции 04.05. Отключать нечего; нужно **откатить данные** до пред-миграционного состояния и оставить интерпретацию строго BYN.

## Plan (волна REVERT-BYN-x3-2026-05-13)

### Шаг 1. Pre-flight (read-only)

- Подтвердить, что `lesson_progress_state_backup_byn_2026_05` содержит pre-migration снимки для обоих lesson_id и для всех пользователей, чьи строки сейчас умножены.
- Сформировать три когорты по `(user_id, lesson_id)`:
  - **A. Чистый rollback**: текущая `state_json.pointA(_v2)_rows` строго равна backup × 3 (никто не редактировал после 04.05) → можно безопасно делить на 3.
  - **B. Смешанная**: часть строк совпадает с backup×3 (rollback), часть отличается (новые/правленые в BYN — не трогать).
  - **C. Полностью новая**: вся `state_json` отличается от backup или backup отсутствует → не трогать.

### Шаг 2. Dry-run

Сформировать proof-таблицу: на каждую `(user_id, lesson_id)` — counts по A/B/C, примеры до/после на 5 строк из каждой когорты, итог `rows_to_revert / rows_kept_as_is / rows_no_backup`. Никаких UPDATE.

### Шаг 3. Backup-of-backup

Перед UPDATE сделать второй снимок текущего `state_json` в `lesson_progress_state_backup_byn_x3_revert_2026_05_13` (та же схема + RLS superadmin), чтобы операция была обратима.

### Шаг 4. Execute (точечный rollback по строкам)

Один SQL-блок:

- Для когорты A: `state_json := backup_state_json_before` (полный rollback).
- Для когорты B: пройти по `pointA_rows[]` / `pointA_v2_rows[]` поэлементно через `jsonb_array_elements`; если `(row.id|_id, client) ∈ backup` и `current.income == backup.income * 3` (с `0.001` tolerance) — заменить значение на backup; иначе оставить.
- `updated_at = now()`.
- Каждый затронутый user пишется в `audit_logs` с `action='lesson.revert_byn_x3_2026_05_13'`, `meta` = counts + источник backup_id.

### Шаг 5. Verify

- Перепроверить данные Натальи Новиковой: значения должны быть `500 / 1000 / 1200 / 1500 / 4100 / 3000` и т.п.
- В Шаге 3 `current_price` (импорт из Шага 2) должен показать те же исходные суммы без ×3.
- Линтер `supabase` чистый, типы `types.ts` без изменений (структура таблиц не меняется).

### Шаг 6. Документация

- Создать `.lovable/proofs/revert_byn_x3_2026_05_13.md` (counts по когортам, before/after, ссылки на backup-таблицы, список аудит-actions).
- Обновить чек-баннера: фраза «Все суммы указывайте в BYN (белорусских рублях)» уже корректна — оставляем.
- Закрыть тикет ТКТ-26-26137 ссылкой на proof.

### STOP-guards

Не трогать: `payments_v2`, `orders_v2`, `allocate_document_number`, document scenarios, Contact Center, морфологию, RLS других таблиц, никакой hard-delete (только UPDATE с обратимым backup), никаких изменений в коде Шага 3 / `loadPortfolioFromPreviousLesson` (там нет арифметики, чинить нечего).

## Open question (нужно подтверждение)

Что делать с когортой B (пользователь после миграции дозаполнил часть строк новыми BYN-значениями, а старые остались ×3)?

- (a) Откатить только те строки, где `current == backup × 3` (рекомендация — безопасный default, не задевает явно введённые после 04.05 значения).
- (b) Полный rollback `state_json` целиком — потеряем все правки после 04.05 (рискованно, не рекомендую).
- (c) Не трогать B вообще — оставить смешанные стейты как есть, чинить вручную через поддержку.

Подтвердите вариант (по умолчанию выполняю (a)) — после этого делаю dry-run-проф и жду отмашки на execute.