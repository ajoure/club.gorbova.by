да, согласен, с учетом правок:

1. **Не менять** `HtmlIframePreview.tsx` **в этом патче.**  
`HTML Preview` — это браузерный tooltip от `iframe title`. Это не критический блокер hero.  
Чтобы не расширять scope, вынести в отдельный follow-up PATCH:
2. **Текущий патч должен быть строго про hero layout.**  
В рамках этого плана менять только:
  &nbsp;
  ```text
  site_pages.blocks[0].content.code
  ```
  и только CSS/минимальную HTML-геометрию hero, если без этого невозможно.
3. **Плашку “Гарантия безопасности” лучше вынести из конфликтной зоны.**  
По скринам она постоянно конфликтует с фото и CTA. Более стабильное решение:
  - не держать её absolute поверх фото;
  - сделать компактной тёмной полосой внутри того же HTML-документа сразу под hero;
  - без отдельного iframe;
  - якоря и модалки не ломаются.
  Это надёжнее, чем снова двигать `bottom`.
4. **Для 1366×768 нужен отдельный compact breakpoint.**  
Добавить специальный режим:
  &nbsp;
  ```css
  @media (min-width: 1025px) and (max-height: 820px) {
    /* compact desktop */
  }
  ```
  В нём уменьшить:
  - размер заголовка;
  - lead margin;
  - feature gaps;
  - вертикальные padding hero;
  - высоту CTA до 48–52px.
5. **Не делать hero жёстко** `height: 100vh`**, если внутри контента больше.**  
Лучше использовать управляемую структуру:
  &nbsp;
  ```css
  .ir-hero-v2__content {
    display: flex;
    flex-direction: column;
  }

  .ir-hero-v2__actions {
    margin-top: 18px;
    margin-bottom: 48px;
  }
  ```
  Цель — не растянуть hero, а гарантированно держать CTA внутри тёмной зоны.
6. **Следующую светлую секцию не “лечить” body background.**  
Нужно сделать нормальный переход:
  &nbsp;
  ```css
  .ir-hero-v2 {
    padding-bottom: 56px;
    background: ...;
  }

  .ir-hero-v2::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 56px;
    background: linear-gradient(...);
    pointer-events: none;
  }
  ```
  Тогда кнопки не будут висеть на белой границе.
7. **DoD по** `HTML Preview` **не должен блокировать сдачу hero.**  
Изменить DoD:
  &nbsp;
  ```text
  HTML Preview tooltip зафиксирован как отдельный follow-up, если источник — iframe title в React-компоненте.
  ```
  Иначе подрядчик опять полезет в React из-за CSS-задачи.
8. **Перед execute обязательно убрать/заменить старые конфликтующие CSS-маркеры.**  
Проверить и не оставить одновременно несколько блоков:
  - `lovable-hero-compact-v1`;
  - `lovable-hero-final-bottom-v1`;
  - новый финальный блок.
  Должен остаться один актуальный override, например:
9. **В Verify добавить фактическую геометрию CTA.**
  &nbsp;
  Обязательно приложить таблицу:
10. **Не сдавать отчет без скриншота 1366×768.**  
Это обязательный размер. Если он не проходит — не писать “вне DoD”.
11. **Mobile не должен наследовать desktop absolute/height.**  
Для mobile:

```css
@media (max-width: 1024px) {
  .ir-hero-v2 {
    height: auto !important;
    min-height: auto !important;
    max-height: none !important;
  }

  .ir-hero-v2__actions {
    flex-direction: column;
    margin-bottom: 24px;
  }
}
```

12. **Обязательное требование для [Lovable.dev](http://Lovable.dev):**

```text
План должен быть составлен на русском языке.
Отчет о выполненной работе должен быть составлен на русском языке.
Вся переписка, все пояснения и все результаты должны предоставляться только на русском языке.

План:
```

1. **Проблема**
  Текущая версия hero страницы «Идеологическая работа» / SITE-000018 не принята: CTA-кнопки частично обрезаны и находятся на границе светлого блока, плашка «Гарантия безопасности» конфликтует с фото/низом hero, переход к следующей секции резкий, а на странице появляется технический tooltip/лейбл `HTML Preview`.
2. **Диагностика**
  - Прочитан `docs/ENGINEERING_RULES.md`; работа будет идти по цепочке Diagnose → Plan → Dry run → Execute → Verify.
  - Найден источник технического `HTML Preview`: `src/components/shared/HtmlIframePreview.tsx` содержит iframe `title="HTML Preview"`. Это может давать браузерный tooltip при наведении на публичный/preview iframe.
  - По текущему скриншоту проблема не в одной мелкой CSS-величине, а в неверной общей геометрии hero: низ CTA попадает на границу следующей светлой секции.
  - Основной контент SITE-000018 хранится как HTML/CSS блока страницы, поэтому геометрию hero нужно исправлять в HTML/CSS страницы, не трогая схему БД, RPC, ссылки, тексты и бизнес-логику.
3. **Предлагаемое решение**
  - Остановить серию точечных `bottom/max-height` подкруток и зафиксировать нормальную desktop-геометрию hero через цельный override:
    - hero получает стабильную высоту/минимальную высоту для 1280×900, 1366×768 и 1440×1000;
    - внутренняя сетка hero распределяется так, чтобы CTA всегда были внутри тёмной зоны;
    - CTA поднимаются минимум на 40–60 px от перехода к следующей секции;
    - обе кнопки получают одинаковую высоту, одинаковую базовую ширину и безусловный `overflow: visible` внутри CTA-зоны;
    - снизу hero добавляется тёмный буфер/градиент, чтобы следующая светлая секция не «резала» кнопки.
  - Плашку «Гарантия безопасности» перевести в безопасное положение:
    - предпочтительно поднять и немного уплотнить внутри правой колонки, чтобы она не перекрывала низ фото и не конкурировала с CTA;
    - если на 1366×768 это всё равно конфликтует — использовать компактную тёмную полосу сразу под hero, но в том же HTML-документе, без отдельного iframe и без изменения якорей.
  - Убрать пользовательски видимый `HTML Preview`:
    - сначала проверить, есть ли строка `HTML Preview` внутри HTML блока SITE-000018;
    - если она есть в HTML — удалить её из HTML блока;
    - если это только `title` общего iframe-компонента, потребуется минимальное изменение `HtmlIframePreview.tsx`: заменить технический title на нейтральный/контекстный, потому что иначе CSS страницы не может убрать браузерный tooltip. Это единственный потенциальный конфликт с ограничением «не менять React-код»; если строго нельзя трогать React вообще, этот пункт будет STOP-guard и не будет выполнен без отдельного разрешения.
4. **Изменяемые компоненты**
  - HTML/CSS содержимое страницы SITE-000018 / `site_pages.blocks` для страницы `7e672fed-13f1-4ff1-8786-71a228a0c011`.
  - Возможно только при подтверждении необходимости: `src/components/shared/HtmlIframePreview.tsx` для удаления технического iframe title `HTML Preview` из пользовательского tooltip.
  - Временный технический способ применения патча может использовать существующий безопасный путь обновления содержимого страницы; новых постоянных RPC, таблиц или схем не добавлять.
5. **Что не будет изменено**
  - Не менять тексты hero, тексты кнопок, ссылки, `openModal('setup')`, якорь `#db`.
  - Не менять header/footer.
  - Не менять БД-схему, RLS, миграции, бизнес-таблицы, тарифы, продукты, RPC.
  - Не менять фото Катерины и URL изображения.
  - Не менять React-код, кроме случая, если `HTML Preview` доказанно приходит из iframe `title` и без этого DoD №6 невыполним.
6. **Dry-run**
  - Считать текущий HTML/CSS SITE-000018 и сохранить before-снимок.
  - Найти текущий блок `.ir-hero-v2`, `.ir-hero-v2__actions`, `.ir-hero-glass`, соседнюю секцию после hero и все правила `hero-fullbleed-override`.
  - Проверить, что патч затрагивает только нужную страницу и только нужный hero/style-блок.
  - Сформировать after HTML локально и проверить, что в нём сохранены:
    - CTA `openModal('setup')`;
    - ссылка/скролл к `#db`;
    - все исходные тексты;
    - отсутствие нового iframe.
  - STOP до execute, если найдено несколько конфликтующих hero-блоков или если planned diff затрагивает не SITE-000018.
7. **Execute**
  - Применить единый CSS/HTML-патч к hero SITE-000018:
    - переписать конфликтующие предыдущие CSS override-правила в один финальный блок;
    - зафиксировать desktop layout под 1280×900, 1366×768, 1440×1000;
    - добавить нижний тёмный буфер/градиент hero;
    - поднять CTA внутрь тёмной зоны;
    - привести CTA к одинаковой высоте и симметрии;
    - стабилизировать положение/размер плашки безопасности.
  - Удалить/нейтрализовать видимый `HTML Preview` только доказанным минимальным способом.
  - Не выполнять publish в рамках патча, если пользователь отдельно не попросит опубликовать.
8. **STOP-guards**
  - Остановиться, если 1366×768 не проходит: отчет не сдавать.
  - Остановиться, если CTA после патча остаются на границе светлой секции или обрезаются.
  - Остановиться, если патч требует изменения текстов, ссылок, `openModal`, `#db`, header/footer или БД-схемы.
  - Остановиться, если `HTML Preview` нельзя убрать без изменения React-кода, а ограничение «не менять React-код» остается абсолютным.
  - Остановиться, если dry-run показывает неожиданный rowcount или изменение не той страницы.
9. **DoD**
  - На 1280×900 обе CTA-кнопки полностью видны, не обрезаны, находятся внутри тёмного hero.
  - На 1366×768 обе CTA-кнопки полностью видны; этот размер обязателен.
  - На 1440×1000 обе CTA-кнопки полностью видны.
  - Нижняя граница hero аккуратная: нет белой полосы/разрыва, который режет кнопки.
  - Плашка «Гарантия безопасности» не перекрывает фото некрасиво и не конфликтует с CTA.
  - На пользовательской странице нет видимого `HTML Preview` tooltip/лейбла.
  - Клик «Настроить идеологическую работу» открывает setup-модалку.
  - Клик «Посмотреть 600+ готовых ответов» ведёт к `#db`.
  - Mobile 390×844 не сломан.
  - В отчёте приложить новые скриншоты: 1280×900, 1366×768, 1440×1000, 390×844.
  - Если 1366×768 не проходит — отчет не сдавать.
10. **Риски и зависимости**
  - Главный риск — `HTML Preview` может быть не частью HTML страницы, а браузерным tooltip от общего iframe title. Тогда чистым HTML/CSS патчем страницы его убрать нельзя.
  - Высота 1366×768 самая критичная: возможно потребуется более плотная desktop-композиция hero, а не просто увеличение отступов.
  - После успешной проверки для появления изменений на live `gorbova.by` потребуется Lovable Publish отдельным действием.