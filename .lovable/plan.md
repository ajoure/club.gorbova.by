да, согласен, с учетом правок:

1. **Убрать поддержку РР как блокер.**  
Поддержка — не предусловие. Ты уже отправил запрос, но подрядчик не должен ждать ответ, если нет фактической ошибки API.

Пункт 2 «Письмо в поддержку РР» оставить только как параллельное уточнение.

Он НЕ блокирует E2E, НЕ блокирует createOrder, НЕ блокирует getOrderStatus.

Если API возвращает ошибку — подрядчик дает точный redacted error и пишет, что именно нужно спросить.

2. **Подрядчик сам выполняет E2E-тесты в preview.**  
Не нужно перекладывать на тебя нажатие кнопок.

Вы уже залогинены в preview. Подрядчик должен сам выполнить E2E dry-run в preview/admin UI и предоставить proof: скрины, network/curl, SQL.

Не просить пользователя нажимать «Создать тестовую заявку», если доступ к preview уже есть.

3. **Убрать жесткий хардкод 9 900 RUB.**  
9 900 RUB — это внешний минимальный порог РР для кредитной заявки, а не наша бизнес-цена и не универсальная валюта платформы.

В core/E2E нельзя хардкодить 9900 RUB как единственный тестовый сценарий.

Сумма и валюта тестовой заявки должны быть параметрами debug/test action.

4. **Для текущего теста использовать 1 000 BYN, если API РР принимает BYN.**

В UI секции «Тестовое подключение» добавить/использовать поля:

- сумма тестовой заявки;

- валюта тестовой заявки.

&nbsp;

Для E2E по умолчанию проверить:

amount = 1000 BYN

amount_minor = 100000

currency = BYN

Если РР вернет ошибку по BYN — зафиксировать exact redacted response, не придумывать блокер.

5. **Исправить rr_test_ledger: сейчас он ошибочно ограничен только RUB.**  
Для core-интеграции это неправильно, потому что в будущем сумма будет приходить из продукта/тарифа и может быть BYN.

Add-only migration:

- заменить/расширить CHECK currency = 'RUB';

- разрешить минимум BYN и RUB;

- лучше сделать CHECK currency IN ('BYN','RUB') на текущий scope;

- amount_minor > 0 оставить.

6. **Adapter не должен быть RUB-only.**

RRPaymentProviderAdapter.createOrder должен принимать currency как параметр.

Запрещено внутри adapter принудительно подставлять RUB.

Если API РР по факту принимает только отдельные валюты — это должно быть выявлено runtime-тестом и зафиксировано.

7. **UI-кнопка теста должна быть не «Создать тестовую заявку 9 900 ₽», а нейтральная.**

Заменить текст:

«Создать тестовую заявку 9 900 ₽»

&nbsp;

на:

«Создать тестовую заявку»

&nbsp;

Рядом поля:

- сумма;

- валюта.

8. **Убрать преждевременную привязку к product wiring.**  
Сейчас тестируем только API и core. Но в плане нужно зафиксировать будущий принцип:

В будущем сумма и валюта будут браться из настройки продукта/тарифа/кнопки оплаты.

На текущем этапе это НЕ внедряется, но архитектура core не должна мешать этому.

9. **Не добавлять РР в публичные кнопки и продукты сейчас.**

PublicPayPage не трогать.

OfferAcquiringSettings не трогать.

Продукты/тарифы не трогать.

payments_v2/orders_v2/provider_events/domain_events не трогать.

10. **DoD переписать без ожидания поддержки.**

DoD E2E:

- подрядчик сам запускает тест из preview;

- создается тестовая заявка на 1000 BYN или фиксируется точная ошибка API;

- payment_url получен либо показан redacted error РР;

- getOrderStatus выполнен либо показан redacted error РР;

- валидная симулированная notification проверена обязательно;

- реальная notification проверена, если РР ее отправит;

- rr_test_ledger содержит запись, если createOrder успешен;

- боевые таблицы не изменились;

- секреты не утекли.

11. **Формат отчета исправить.**

Отчет о выполненной работе: E2E runtime proof Ресурс Развития

**Короткое сообщение подрядчику**

План нужно исправить.

&nbsp;

Не ждем поддержку РР как блокер. Запрос отправлен, но E2E надо запускать сейчас на уже внесенных test credentials.

&nbsp;

Тесты выполняй сам в preview/admin UI. Пользователь уже залогинен в preview, не перекладывай на него ручные клики.

&nbsp;

Убери жесткий хардкод 9 900 RUB. Это внешний минимальный порог РР, а не наша бизнес-валюта и не цена продукта. Core-интеграция должна принимать сумму и валюту параметрами. Для текущего E2E тестируй 1000 BYN, если API РР принимает BYN. Если API вернет ошибку — дай точный redacted response и напиши, что именно нужно уточнить.

&nbsp;

Нужно поправить текущий RUB-only guard:

- rr_test_ledger не должен быть только RUB;

- adapter не должен принудительно использовать RUB;

- UI тестовой заявки должен иметь поля сумма + валюта;

- кнопка должна называться «Создать тестовую заявку», а не «Создать тестовую заявку 9 900 ₽».

&nbsp;

PublicPayPage, OfferAcquiringSettings, продукты, тарифы, payments_v2, orders_v2, provider_events, domain_events, выдачу доступов и статистику не трогать.

&nbsp;

Будущая логика: сумма и валюта будут браться из продукта/тарифа/кнопки оплаты, но это отдельный следующий этап после стабильного core.

&nbsp;

## Полный план оставшейся работы по интеграции «Ресурс Развития»

### 0. Что уже сделано (не переделываем)

- **Core-слой:** `_shared/rr/{rr-config,rr-http,rr-adapter}.ts` — Basic Auth, безопасное логирование, маппинг статусов.
- **Изолированный ledger:** `rr_test_ledger` (только `RUB`, только префикс `rr_test_`, RLS admin-only).
- **Edge-функции:** `rr-test-create-order`, `rr-test-get-status`, `rr-notification` (MD5-guard, idempotency, работает только с тестовым ledger).
- **UI:** `RRSettingsCard.tsx` — секция «Тестовое подключение», таблица последних 20 заявок.
- **Креды пользователя внесены и подтверждены в БД:**
  - `config.mode = test`
  - `config_secrets`: `test_password`, `battle_password`, `secret_key`
  - `config`: `test_login = test-gorbova`, `battle_login = battle-gorbova`
- **Stop-guards:** `PublicPayPage`, `OfferAcquiringSettings`, `payments_v2`, `orders_v2`, `provider_events`, продуктовые кнопки, выдача доступов, статистика — **не трогаем**.

### 1. Что осталось сделать — по шагам

Порядок жёсткий: пункт 2 (письмо) идёт **параллельно** с пунктами 3–5 (не блокирует их), потому что креды у нас уже есть. Пункт 6 (продовое включение) стартует только после закрытия DoD пунктов 3–5 **и** получения ответа поддержки.

```
[2] Письмо в поддержку РР — уточнение контракта     ─┐
                                                     │  параллельно
[3] E2E dry-run: createOrder → payment_url          ─┤
[4] E2E: getOrderStatus → терминальный статус       ─┤
[5] E2E: rr-notification с реальной подписью РР     ─┘
                        │
                        ▼ (только после DoD 3–5 + ответа поддержки)
[6] Product wiring — отдельный план, согласуем позже
```

### 2. Письмо в поддержку РР (уточнение контракта, не запрос кред)

**Зачем:** креды есть, но 4 вещи нельзя проверить in-vitro — их знает только поддержка. Без подтверждения этих 4 пунктов нельзя гарантировать, что боевой webhook не будет false-reject'иться и что комиссия будет корректно учтена.

**Тема:** Уточнение по API-контракту для магазина test-gorbova / battle-gorbova

Здравствуйте!

Настраиваем интеграцию с вашим API. Тестовые и боевые креды уже получены и заведены на нашей стороне (`test-gorbova` / `battle-gorbova`, secret_key для webhook сохранён). Перед боевым запуском нужно уточнить 4 момента по контракту API, чтобы не отклонять валидные уведомления и корректно учитывать комиссию.

1. **Изоляция тестового магазина.** Подтвердите, пожалуйста, что заявки, созданные под `test-gorbova`, **не попадают в реальный процессинг**: не создают финансовых обязательств, не уходят в банк-эквайер, не отражаются в реестре платежей и не тарифицируются. Если для оплаты тестовой заявки нужны специальные тестовые карты — пришлите номера/условия.
2. **Подпись webhook.** Просьба подтвердить финальную схему, которую вы рассчитываете и присылаете:
  - какие поля входят в строку для подписи и в каком порядке (конкатенация или sorted keys),
  - схема — `md5(field1 + field2 + … + secret_key)` или другая,
  - в каком поле уведомления передаётся сама подпись (имя параметра),
  - HTTP-метод и Content-Type уведомления (`application/x-www-form-urlencoded` или `application/json`).
3. **Идемпотентность `createOrder`.** Что произойдёт при повторном вызове с уже использованным `external_id` (наш внутренний ID): вернётся та же заявка, ошибка, или создастся новая с другим внутренним ID РР? От этого зависит наша retry-политика при сетевых сбоях.
4. **Retry-политика уведомлений.** Сколько раз и с какими интервалами вы повторяете webhook, если наш endpoint ответил не 2xx? Какой HTTP-код считаете «доставлено» (только `200` или любой `2xx`)? Есть ли максимальный таймаут ожидания ответа?
5. **Комиссия и финальный статус.** В каком поле ответа `getOrderStatus` (или уведомления) передаётся сумма комиссии, в каких единицах (рубли/копейки), и на каком статусе она становится финальной? Какое значение статуса означает **успешную оплату**, после которой мы можем выдавать доступ клиенту?

Спасибо!

---

### 3. E2E dry-run: `createOrder` → `payment_url`

**Действие:** админ (вы) в preview открывает Админ → Интеграции → Разное → «Ресурс Развития» → «Создать тестовую заявку 9 900 ₽». Либо разово авторизуетесь в preview, и я вызываю `rr-test-create-order` вашим JWT сам.

**Проверяем:**

- HTTP 200 от `rr-test-create-order`,
- в ответе есть `payment_url`,
- в `rr_test_ledger` появилась строка со статусом `pending`, `external_id` вида `rr_test_*`,
- в `integration_sync_logs` — запись с `status='success'`, без утечки секретов,
- боевые таблицы (`payments_v2`, `orders_v2`, `provider_events`, `access_grant_ledger`, `entitlements`) — **0 новых строк**.

**Если РР вернёт ошибку** (например, 401 на test-хосте) — фиксирую точный status + redacted body, добавляю в п.2 письма как конкретный кейс.

### 4. E2E: `getOrderStatus` → терминальный статус

**Действие:** после ручного открытия `payment_url` в браузере и оплаты тестовой картой — вызов `rr-test-get-status` по `external_id`.

**Проверяем:**

- статус в ledger обновился на терминальный (`paid` / `succeeded` — по маппингу adapter'а),
- если РР вернул `commission` — `commission_minor` заполнен, иначе явно фиксируем «commission не пришёл на этом шаге, ждём в webhook».

### 5. E2E: `rr-notification` с реальной подписью

**Действие:** дождаться реального callback от РР на нашу `rr-notification` (после оплаты тестовой картой в п.4).

**Проверяем:**

- уведомление принято (HTTP 200),
- MD5-подпись валидна (guard пропустил),
- идемпотентность — повторный тот же callback не создаёт дубликат,
- поддельная нотификация с неправильной подписью → HTTP 401,
- запись в `integration_sync_logs` с `entity_type='rr_notification'`,
- боевые таблицы — по-прежнему 0 строк.

### DoD этапа (закрытие E2E runtime proof)

- Минимум 1 запись в `rr_test_ledger` с полным жизненным циклом: `pending → paid`.
- `payment_url` реально получен и открывался.
- `getOrderStatus` подтвердил терминальный статус.
- Валидный webhook от РР принят с корректной MD5.
- `commission_minor` либо заполнен, либо явно зафиксировано «РР не отдаёт на тестовом магазине».
- SQL-proof: `payments_v2`, `orders_v2`, `provider_events`, `access_grant_ledger`, `entitlements` — **0 сайд-эффектов**.
- Секреты не появляются в `config`, `payload_meta`, консоли, логах.
- Все 4 пункта из письма поддержки закрыты письменным ответом (сохраняется в `docs/integrations/rr/discovery.md` как append-only).

### 6. Product wiring (после закрытия DoD п.5 — отдельный план)

**Не входит в текущий этап**, только фиксирую scope, чтобы понимать, куда движемся:

- `rr-create-checkout` (production-путь, а не test),
- переключение `mode` инстанса РР на `live`,
- кнопка «Оплатить через РР» на `PublicPayPage` — где показывать, для каких офферов, с каким приоритетом относительно других провайдеров,
- запись успешных боевых платежей в `payments_v2` / `orders_v2` / `provider_events`,
- выдача доступов через `access_grant_ledger` / `entitlements`,
- учёт комиссии РР в основной статистике/сверке,
- notify-каналы (email/telegram) на успешный платёж.

Этот пункт согласуем **отдельным планом** после того, как п.5 подтверждён runtime proof'ом.

### Что нужно от вас прямо сейчас

1. **Отправьте письмо** из п.2 в поддержку РР (или дайте добро — сформулирую под ваш канал связи, если нужно короче/формальнее).
2. **Выберите, как двигать E2E dry-run** (п.3):
  - вариант A — сами нажимаете «Создать тестовую заявку 9 900 ₽» в UI и пишете «готово»;
  - вариант B — заходите в preview под админом, пишете «продолжай», и `rr-test-create-order` вызываю я вашим JWT.

Ответ поддержки РР не блокирует старт п.3–4, только финальное закрытие DoD и переход к п.6.