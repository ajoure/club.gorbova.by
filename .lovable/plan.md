# Обновлённый план: публикация PR #511 — SHA `c283f6f2db18391ede33aabcfa3ecb5e026b2310`

## Диагностика и наблюдения (read-only, выполнено)

- Локальный HEAD `6665aa2bb` и GitHub `origin/main` `6665aa2bb` отличаются от целевого `c283f6f2d` **только служебными `.lovable/`-коммитами** (plan/archive); дерево чистое; прикладной код идентичен `c283f6f2d`.
- Дельта `4fbd621e..c283f6f2` ограничена:
  - `src/components/ai-chat/PromptRunFlow.tsx` (+6/−1) — условная подпись под зоной загрузки: для `bank_statement_analysis` перечисляются только `PDF, XLSX/XLS, CSV, TXT, DOCX, JPG/JPEG, PNG, WebP`, что точно совпадает с константой `BANK_STATEMENT_EXTENSIONS` (`.pdf, .docx, .xls, .xlsx, .csv, .txt, .jpg, .jpeg, .png, .webp`); для остальных сценариев прежняя общая подпись без изменений;
  - `src/components/ai-chat/PromptRunFlow.test.tsx` (+40) — ровно 4 теста: подсказка перед загрузкой, файл >20 МБ, неподдерживаемый формат, более 5 файлов;
  - служебный `.lovable/plan.md` (plan-archive) — вне прикладного scope.
- Vitest перезапущен: **4/4 PASS** (278ms).
- Миграций, изменений базы, Edge Functions, Auth, доступов, тарифов, продуктов, подписок, платежей, секретов и production-данных в дельте нет. Коммерческой логики не касается.

## Предлагаемое выполнение

1. **Preflight / sync**
   - заново прочитать GitHub `refs/heads/main` без stale-cache;
   - подтвердить, что HEAD/origin отличаются от `c283f6f2d` только служебными `.lovable/`-коммитами, дерево чистое, прикладной диф от `4fbd621e0` — ровно два заявленных файла;
   - остановиться при любом новом прикладном коммите, грязном дереве или расширении scope.
2. **Dry run**
   - целевой Vitest `PromptRunFlow.test.tsx` (ожидание 4/4);
   - полный typecheck и production build штатными командами проекта;
   - без исправлений в рамках этого execute — остановка при любой ошибке.
3. **Publish**
   - опубликовать прикладную версию `c283f6f2` на существующий production-сайт `https://gorbova.lovable.app`;
   - не выполнять миграции, deploy функций и любые изменения данных или настроек;
   - известные 13 RLS-находок существуют до этого PR и не относятся к данному frontend-only дифу.
4. **Read-back после публикации**
   - подтвердить опубликованный URL и SHA/версию, а не только HTTP 200;
   - desktop (1280×800): в режиме «Анализ выписки» до загрузки видна подсказка с корректным списком форматов; три видимых ошибки — неподдерживаемый формат (XML), файл >20 МБ, шестой файл; текст не обрезан и не перекрыт;
   - mobile 390×844: те же проверки; отсутствие обрезания, горизонтального выхода и ухода элементов за экран;
   - отдельные production-скриншоты desktop и mobile без PII и секретов, привязанные к URL, SHA и viewport.

## STOP-guards

- Published SHA не соответствует `c283f6f2…`.
- Прикладной диф выходит за два заявленных файла.
- Любой test/typecheck/build не проходит.
- На опубликованной странице подпись противоречит whitelist, ошибка не видна или мобильная компоновка обрезана/перекрыта.
- Обнаруживается изменение backend, данных, функций, доступа или коммерческой логики.

## DoD

- Exact SHA подтверждён до и после Publish.
- Vitest 4/4, typecheck и build PASS.
- Production desktop и 390×844 показывают корректную подсказку до загрузки и видимые ошибки для всех трёх невалидных случаев.
- Два скриншота опубликованного результата, привязанные к URL, SHA и viewport.
- Backend, данные, функции, доступы и коммерческие настройки не изменены.
