# READ-ONLY отчёт + план импорта готовых текстов (ЦБ: cb20 + 20 поток)

Режим: READ-ONLY. Не выполнялось: запись в БД, миграции, STT, jobs, deploy, Publish, изменения кода.

## 1. Exact-code ревизия b782b41 — статус прежних замечаний

Пакет: 22 файла, whole-JSON SHA256 `0786a7cc…8783` PASS, все 22 пофайловых SHA256 PASS,
миграция `20260911152939_course_source_transcriptions.sql` SHA256 `c25b43de…5de1b` PASS (не изменена).

| Прежнее замечание | Статус | Основание в exact-коде |
|---|---|---|
| 404 самого видео смешивался с 404 субтитров | PASS | `video_http_404 → provider_not_found`, `provider_http_404 → subtitles_not_found` (разные source-статусы, отдельные счётчики в totals) |
| Неизвестная ревизия обрывала весь прогон | PASS | `provider_revision_unknown → source_revision_unknown` как статус источника, прогон продолжается |
| URL с `?t=`/`#chapter` молча пропускались | СНЯТО (было ошибочно) | `inventory.mjs` разбирает `url.pathname`; тест corpus.test.mjs:22 покрывает `?t=22#chapter` |
| FK на `auth.users` | Принято как проектное решение (user identity, не profile identity) |
| Лимит 10 млн (БД) против 1 млн (батч) | Принято: разные уровни |
| Миграция не идемпотентна | Принято: однократная, повтор контролируется историей и hash |
| 143 теста PASS | НЕ ПОДТВЕРЖДЕНО ЛОКАЛЬНО | plan mode блокирует создание файлов/запуск сьюта; статически в пакете 8 тест-файлов, 73 top-level `test(`. Подтверждение — по CI на b782b41 |

Блокеров нет. Импортёр по-прежнему без STT-вызовов, с replay/идемпотентностью, лимитом ≤3 источника/≤1 млн символов, проверкой alias-hash и снимка курса.

## 2. Реестр (реальные цифры этого прогона)

Scope: `cb20` + «20 поток». 68 уникальных alias, 114 видео-блоков.

- resolved 60, provider 404 — 8 (все восемь перепроверены сейчас: HTTP 404 подтверждён);
- готовых RU-субтитров 56, без RU 4 (1 только EN + 3 без дорожек);
- категории: base_only 19, shared 40, flow20_only 2, модуль «Конференции и прямые эфиры | 20 поток» — 7 видео (6 с RU, 1 только EN). Прежняя цифра «5 созвонов» уточнена;
- доказанных дат проведения созвонов нет — поля дат оставлены `null`.

Документы: сравнено 48 вкладок = 18 совпавших + 18 отраслевых (вне scope) + 12 пустых/без текста
(включая «Модуль 13» со ссылкой на второй документ). Совпадения 68–87/100, покрытие всех 10 децилей,
word ratio 0.98–1.04; у отраслевых 0–1/100.

Машиночитаемый реестр (без текста, URL и ПД) — `cb-knowledge-registry-v2.json`: totals, 48 документов
с per-decile и word ratio, 68 видео с canonical UUID, ревизией, длительностью, статусами субтитров,
метриками качества и полными product/module/lesson/block ID.

## 3. Что импортировать (без STT)

1. Все 56 видео с готовыми RU-субтитрами импортируются как транскрипты — это закрывает и 38 видео без
   Google-документа, и все созвоны 20 потока с RU. STT для них не запускается.
2. 18 совпавших документов подключаются как редакционная основа поверх соответствующих транскриптов
   (сверка по canonical UUID + hash), а не как замена.
3. Реальные пробелы = 4 видео без RU + 8 alias с provider 404. По ним сначала выяснение источника
   (замена/перезалив), только затем STT — отдельным разрешением.

## 4. Следующий шаг (после одобрения)

Managed dry-run exact-кода b782b41: отчёт по 68 источникам, счётчики статусов, проба одного урока,
проверка сохранения, затем малые идемпотентные батчи ≤3 источников с read-back и replay.
Полный текст остаётся `paid_private`/`unreviewed`, доступен только super_admin и доверенному worker;
sales projection — отдельно, по exact-source-hash.
