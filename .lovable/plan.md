&nbsp;

да, согласен, с учетом правок:

&nbsp;

1. Не скрывать строку Последний платёж, если last_charge_at = null. Показывать её всегда, но со значением —, иначе снова будет ощущение, что поле “пропало”, а не отсутствует в данных.
2. Подпись лучше сделать не Последний платёж, а Последнее списание, если это именно карточка provider_subscriptions. Это точнее для рекуррентной подписки. Если хотите сохранить общий язык для пользователя, допустимо Последний платёж / списание.
3. В плане зафиксировать, что источник для обеих дат именно:  

  - provider_subscriptions.created_at
  - provider_subscriptions.last_charge_at  
  Без fallback на другие таблицы, чтобы не смешивать разные модели дат в одной карточке.
4. &nbsp;
5. Добавить явный DoD:  

  - у подписки с заполненным last_charge_at отображаются обе даты;
  - у подписки без last_charge_at отображается Создана: <дата> и Последнее списание: —;
  - существующие поля Следующее списание и Доступ до не ломаются.
6. &nbsp;
7. Зафиксировать формат:  

  - использовать тот же формат и timezone, что уже применяется в этом блоке через formatPaymentTimeIANA;
  - не вводить новый форматтер и не менять формат остальных дат в карточке.
8. &nbsp;
9. Небольшая UI-правка:  

  - вставить новые строки рядом с Следующее / Доступ до, в том же визуальном блоке метаданных;
  - порядок лучше такой: Создана → Последнее списание → Следующее списание → Доступ до.
10. &nbsp;
11. В плане лучше прямо указать, что это патч только для синей карточки раздела Подписки из provider_subscriptions, чтобы подрядчик не полез менять второй блок подписок или сделки.

&nbsp;

&nbsp;

Итог: сам подход правильный, патч можно отдавать в работу после этих уточнений.

## План: Добавить дату создания и последнего платежа на плашку подписки

### Что видим

На скриншоте — раздел «Подписки» в карточке контакта (данные из `provider_subscriptions`). Сейчас показывается: название, ID, статус, провайдер, карта, следующее списание, доступ до. **Не показывается**: дата создания подписки и дата последнего платежа.

### Источники данных


| Поле             | Таблица                  | Колонка          | Статус                                                         |
| ---------------- | ------------------------ | ---------------- | -------------------------------------------------------------- |
| Дата создания    | `provider_subscriptions` | `created_at`     | Уже в select (строка 769), но не рендерится                    |
| Последний платёж | `provider_subscriptions` | `last_charge_at` | Есть в таблице (110 из 180 записей заполнены), **не в select** |


Новых функций и запросов не нужно.

### Изменения

**Файл: `src/components/admin/ContactDetailSheet.tsx**`

**1. Запрос (строка 768):** добавить `last_charge_at` в select provider_subscriptions:

```
id, provider, state, provider_subscription_id,
next_charge_at, amount_cents, currency, card_brand, card_last4, created_at, last_charge_at,
subscription_v2_id, meta,
```

**2. Рендер (после строки 1976, после блока «Доступ до»):** добавить две строки:

- **Создана:** `sub.created_at` — форматировать через `formatPaymentTimeIANA`
- **Последний платёж:** `sub.last_charge_at` — если есть, форматировать; если null — не показывать строку

Визуально — тот же стиль `text-xs text-muted-foreground`, что и у «Следующее» и «Доступ до».

### Что НЕ меняется

- Другие разделы карточки контакта
- Запросы к другим таблицам
- Никаких новых функций, edge functions, миграций