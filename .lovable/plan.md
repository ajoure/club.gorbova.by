## да, согласен, с учетом правок:

1. Перед добавлением `PackageFieldsClientForm` проверить, что `PackageGenerationPanel` и `DocumentPackageQuestionnairesView` не отображаются одновременно на одном экране. Не допустить двух одинаковых форм с независимыми кнопками сохранения для одной сессии.
2. После успешного сохранения значений форма должна инвалидировать существующий query/cache сессионных pf-значений, чтобы:
  - новые значения сразу отображались после сохранения;
  - счётчик обязательных полей и `canGenerate` обновлялись без перезагрузки страницы.
3. При отсутствии найденных pf-полей карточку «Поля пакета» не показывать. Не выводить клиенту пустой технический блок.
4. Сохранить дедупликацию полей: один `pf-XXXXXX`, встречающийся в нескольких документах, должен отображаться в общей форме только один раз.
5. Подсказку сформулировать без технических терминов и идентификаторов:
  - значения номера, даты, года и других общих полей заполняются один раз;
  - введённые данные автоматически используются во всех документах пакета;
  - роли участников настраиваются отдельно внутри каждого документа.
6. Добавить UI-тест:
  - общая форма находится над аккордеоном;
  - `PackageFieldsAssignmentPanel` внутри документов отсутствует;
  - ролевые назначения остаются;
  - существующие сохранённые значения загружаются;
  - после сохранения отображаются обновлённые значения.

Остальной scope сохраняется без изменений: только UI, без миграций, RPC и правок генерации.

&nbsp;

План: анкета документов как у ролей идеологии — клиент просто заполняет pf-поля

### Проблема

В «Анкеты документов» под каждым шаблоном сейчас рендерится `PackageFieldsAssignmentPanel` — это **админская диагностика** (read-only список найденных pf-токенов с кнопками «копировать `{{pf-XXXXXX}}`»). Клиенту это не нужно: он не вставляет токены, он должен **заполнить значения** (номер приказа, дата, год), как в анкете ролей.

Готовый компонент для этого уже есть — `PackageFieldsClientForm` (рендерит инпуты по типам, обязательность, smart-date prefill, сохраняет через `upsert_session_field_values`). Сейчас он подключён только в `PackageGenerationPanel`, а в `DocumentPackageQuestionnairesView` его нет. Это и есть причина жалобы.

### Что меняется (только UI, без бэка/миграций)

**Файл: `src/components/ai-documents/packages/DocumentPackageQuestionnairesView.tsx**`

1. Удалить per-document блок `<PackageFieldsAssignmentPanel ... />` из `ItemQuestionnaire` (строки ~496–499) и убрать его импорт.
2. Над аккордеоном документов (после карточки «ЮЛ/ИП пакета», только когда `sessionId` есть) отрисовать один `PackageFieldsClientForm`:
  ```
   <PackageFieldsClientForm
     sessionId={sessionId}
     packageTemplateId={packageTemplateId}
     sessionCreatedAt={sessionQuery.data?.created_at ?? null}
   />
  ```
   Для этого добавить `created_at` в `select` запроса `sessionQuery` (сейчас выбираются только `id, selected_legal_entity_id, legal_entity_locked_at`).
3. В подсказке над аккордеоном уточнить, что роли назначаются по каждому документу, а **значения полей** (номер, дата и т.п.) заполняются один раз сверху.

**Файл: `src/components/ai-documents/packages/PackageFieldsAssignmentPanel.tsx**`

- Оставить как есть (используется только админами в каталоге шаблонов как диагностический read-only). Из клиентской анкеты он уходит полностью.

### Что НЕ трогаем

- `PackageFieldsClientForm`, `usePackageSessionFields`, RPC `upsert_session_field_values`, `resolveSmartDatePrefill`, каталог pf-полей и его dialog, ролевые ассайнменты (`DocumentItemRoleAssignments`), генерация документов, миграции БД.
- Default-значения и smart-date prefill уже работают (берутся из `field.options.default_kind` и существующих сохранённых значений) — поведение «номер приказа предзаполнен, клиент может изменить» обеспечено существующим хуком.

### DoD

- На скриншоте `/document-generation` → «Анкеты документов» вверху (после ЮЛ/ИП) появляется единая карточка «Поля пакета» с реальными инпутами (Номер приказа — number, Дата приказа — date с prefill, и т.д.), значения сохраняются кнопкой «Сохранить значения».
- Внутри каждого аккордеона документа остаются **только** назначения ролей (как сейчас в «Идеология»), никаких «pf-000004 / copy» и поясняющих диагностических плашек.
- Сборка зелёная, RLS/политики не меняются, существующие values сохраняются и подтягиваются.