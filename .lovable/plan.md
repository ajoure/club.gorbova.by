## да, согласен, с учетом правок:

1. **Заголовок привести к обязательному формату**
  &nbsp;
  Сейчас указано `## План: ...`. Нужно:
  `План: Переоформить «Шаблоны документов» в стиле сделок и починить мобильную верстку`
2. **Добавить обязательный блок языка для Lovable**
  &nbsp;
  В начало плана добавить:
  План должен быть составлен на русском языке.  
  Отчет о выполненной работе должен быть составлен на русском языке.  
  Вся переписка, пояснения, diff-summary, proof и результаты должны быть только на русском языке.
3. **Уточнить, что это строго UI-only patch**
  &nbsp;
  Добавить явно:
  Патч не меняет document generation pipeline, token registry, template validation, storage, БД, RLS, RPC, edge-functions и бизнес-логику. Меняется только визуальное представление списка шаблонов/версий и мобильная обертка таблицы плейсхолдеров.
  Это важно, чтобы не нарушить принцип единого pipeline и запрет на параллельные механизмы генерации/токенов.  
4. **Не удалять Table-импорты до проверки использования**
  &nbsp;
  Формулировку заменить:
  `Table*` imports удалить только если после замены блока они больше не используются в файле. Если в файле есть другие таблицы — не трогать.
5. **Добавить read-only discovery перед правками**
  &nbsp;
  Перед execute добавить короткий обязательный шаг:
  - проверить фактический JSX-блок списка шаблонов в `StrictDocumentTemplatesManager.tsx`;
  - проверить текущие state-переменные выбора шаблона/версии;
  - проверить названия handlers для `select preview`, `make active`, `delete`;
  - проверить, как сейчас определяется `activeTemplateId/currentVersion`;
  - проверить, есть ли уже `Collapsible` imports в проекте;
  - проверить структуру `PlaceholdersCatalogTab.tsx`, чтобы не сломать desktop-таблицу.
6. **Состояние раскрытых шаблонов сделать устойчивым к смене выбранной версии**
  &nbsp;
  Сейчас написано: “по умолчанию открыт тот шаблон, чья версия выбрана в превью”. Нужно уточнить:
  - при первом рендере открыть активный/выбранный шаблон;
  - при клике по версии не схлопывать родительский шаблон;
  - если пользователь выбирает версию из другого шаблона, этот шаблон автоматически добавляется в `openTemplateIds`;
  - ручное закрытие/открытие сохраняется локально в рамках текущего mount.
7. **Не использовать конфликтующие названия** `activeTemplateId`**, если в файле другое имя**
  &nbsp;
  В плане лучше написать нейтрально:
  Использовать существующие state/derived-переменные выбранного шаблона и выбранной версии. Если `activeTemplateId` как переменной нет — не создавать параллельное состояние, а вывести ID из уже существующего selected/current version state.
8. **Подсветку активной версии описать отдельно от активного шаблона**
  &nbsp;
  Добавить:
  - карточка шаблона подсвечивается, если внутри него выбрана текущая версия;
  - строка версии подсвечивается, если это выбранная версия в превью;
  - `current` badge показывает именно текущую активную версию шаблона, а не выбранную в preview.
9. **Кнопку удаления в шапке шаблона уточнить по текущей логике**
  &nbsp;
  Нужно не допустить удаления шаблона, если сейчас в UI удаляется только версия. Добавить:
  Кнопку удаления шаблона добавлять только если в текущем менеджере уже существует handler удаления шаблона. Если сейчас реализовано только удаление версии/файла — не добавлять новую бизнес-операцию, а оставить существующие действия на уровне версии.
10. **Мобильная верстка карточек: добавить ограничения на длинные имена**

В карточках и строках версий добавить обязательные классы:

- `min-w-0` на текстовых контейнерах;
- `truncate` или `break-words` для `file_name`;
- правая зона бейджей/кнопок — `flex-wrap`;
- кнопки не должны выходить за ширину карточки на 390/414px.

11. `CollapsibleTrigger` **и вложенные кнопки**

Добавить техническое требование:

Если внутри `CollapsibleTrigger` размещаются action-кнопки, они должны иметь `type="button"` и `onClick={(e) => { e.preventDefault(); e.stopPropagation(); ... }}`. Иначе клик по удалению/активации может одновременно раскрывать/сворачивать карточку.

12. **Для** `PlaceholdersCatalogTab.tsx` **добавить точный safe-вариант**

Формулировку сделать жестче:

- внешний контейнер страницы не должен получать горизонтальный скролл;
- горизонтальный скролл только внутри блока таблицы;
- таблице задать `min-w-max` или существующую минимальную ширину, чтобы колонки не сжимались до нечитаемости;
- фильтры и кнопки сверху должны иметь `min-w-0`, `flex-wrap`, `max-w-full`.

13. **Добавить STOP-guards**

Включить в план:

- STOP, если для карточного вида требуется менять структуру данных шаблонов/версий;
- STOP, если текущий handler удаления относится к версии, а план требует удалить шаблон целиком;
- STOP, если для мобильной адаптации плейсхолдеров требуется менять состав колонок/действий;
- STOP, если правка затрагивает `useDocumentTemplates`, валидатор, edge-functions, RLS или БД.

14. **DoD дополнить regression-проверками**

Добавить:

- загрузка нового шаблона работает как раньше;
- открытие `TemplateMarkupDialog` работает как раньше;
- `FileNameTemplateEditor` работает как раньше;
- выбранная версия корректно открывает превью;
- текущая активная версия не меняется от простого открытия/закрытия аккордеона;
- удаление/активация версии работают после перехода desktop → mobile → desktop.

15. **Verify дополнить конкретными viewport**

Добавить:

- Chrome responsive: 390px, 414px, 768px, 888px, 1024px;
- на 390/414px отсутствует горизонтальный скролл всей страницы в `Шаблоны документов`;
- в `Плейсхолдерах` горизонтальный скролл есть только внутри таблицы.

В остальном план корректный: scope ограничен UI, не трогает БД/хуки/edge/RLS, решает основную проблему мобильной недоступности версий и не создает параллельную архитектуру генерации документов.

&nbsp;

План: переоформить «Шаблоны документов» в стиле сделок + починить мобильную верстку

### Контекст

- Канонический стиль для группировки/раскрытия — `src/components/admin/contact/ContactDealsTab.tsx`:
`Collapsible` + карточка с `border-l-4 border-l-indigo-300`, иконкой `Layers` в `bg-indigo-50`, бейджами справа, `ChevronDown` с поворотом. Использует `@/components/ui/collapsible`.
- Сейчас в `src/components/ai-documents/StrictDocumentTemplatesManager.tsx` шаблоны и их версии нарисованы плоской `<Table>` (строки 745–822). На мобильном (888px и меньше) таблица не помещается, версии не нажимаются — отсюда жалоба.
- Плейсхолдеры (`PlaceholdersCatalogTab.tsx`) — широкая `<Table>` с 9 колонками и фиксированными width (`w-[260px]`, `min-w-[240px]`), на мобильном уезжает за экран.

### Что делаем (только UI, бизнес-логика и данные не трогаются)

**1. `StrictDocumentTemplatesManager.tsx` — заменить таблицу шаблонов на аккордеон-список в стиле сделок.**

- Удаляем `<Table>/<TableHeader>/<TableBody>` блок (≈строки 744–823); imports `Table*` снимаем, если больше нигде не нужны в файле.
- Каждый шаблон рендерим карточкой `Collapsible` по канону из `ContactDealsTab`:
  - корневой `<div class="bg-card border border-border/60 border-l-4 border-l-indigo-300 rounded-lg shadow-sm overflow-hidden">`,
  - `CollapsibleTrigger` — кнопка-шапка: иконка `FileText` в `w-7 h-7 rounded-md bg-indigo-50` (вместо `Layers`, т.к. контекст «документ», а `Layers` зарезервирован за продуктами по memory `canonical-product-icon`), название шаблона, справа бейджи:
    - `{versions.length} версий` (secondary),
    - `current v{N}` (зелёный, если есть активная),
    - статус шаблона `active/draft` (как сейчас),
    - кнопка-иконка удаления (`Trash2`, с `e.stopPropagation`),
    - `ChevronDown` с `-rotate-90` когда закрыто.
  - `CollapsibleContent` — список версий (`space-y-1 px-2 pb-2`), каждая версия = строка как `DealRow`:
    - `flex flex-col sm:flex-row sm:items-center gap-1.5 sm:gap-2.5 px-2.5 py-2 rounded-md cursor-pointer hover:bg-accent/40`,
    - слева: маленькая иконка + `v{N} · {file_name}` + дата `dd.MM.yyyy HH:mm`,
    - справа: `ValidationBadge`, `current` badge, счётчик токенов, кнопка «Сделать активной» (вся `e.stopPropagation`).
- Состояние «открыт/закрыт» — локальный `Set<string>` шаблонов; по умолчанию открыт тот шаблон, чья версия выбрана в превью (`activeTemplateId`).
- Активный шаблон/версия подсвечиваются как и сейчас (фон `bg-muted/40`).

**2. Layout-grid сетки превью.**

- Сейчас `grid lg:grid-cols-2 gap-4` — на мобильном два блока становятся в столбец, это ок. Оставляем как есть, но левую колонку оборачиваем в `min-w-0`, чтобы длинные имена файлов не растягивали грид (фикс горизонтального скролла на 888px).

**3. `PlaceholdersCatalogTab.tsx` — мобильная адаптация без смены UX на десктопе.**

- Контейнер таблицы: `<div className="border rounded-lg overflow-hidden">` → добавляем `overflow-x-auto` на внутренний `div` со скроллом, чтобы таблица скроллилась горизонтально, а не ломала layout страницы.
- Шапку секций (`section-${section.id}` с `colSpan={9}`) и панель фильтров (`grid sm:grid-cols-12`) делаем `min-w-0` + проверяем, что кнопки в `flex-wrap`.
- На `<md` (мобилка) ширина таблицы остаётся, но появляется горизонтальный скролл внутри контейнера — кнопки «Разметить/Удалить/добавить» становятся доступны. Это минимальное безопасное изменение; полноценный card-вид для плейсхолдеров — отдельный спринт (выходит за рамки запроса «такой же стиль как у сделок» — у сделок нет аналога каталога полей).

### Что НЕ трогаем

- Бизнес-логика загрузки/активации/удаления шаблонов, версионирование, валидатор, edge-функции, RLS, схема БД.
- `useDocumentTemplates`, `canonical-template-*` функции.
- Превью-панель справа, диалоги `TemplateMarkupDialog`, `FileNameTemplateEditor`, аплоад.
- Каталог плейсхолдеров по содержимому (колонки, фильтры, действия) — только обёртка под скролл.
- `ContactDealsTab.tsx` — только читаем как референс стиля.

### DoD

1. `/admin/documents → Шаблоны документов`: список шаблонов рендерится карточками-аккордеонами в стиле сделок (`border-l-indigo-300`, иконка в `bg-indigo-50`, бейджи, `ChevronDown`).
2. Клик по шапке шаблона раскрывает/сворачивает версии; клик по версии открывает превью (как раньше).
3. Кнопки «Сделать активной» и «Удалить» работают и не триггерят toggle (есть `stopPropagation`).
4. Viewport 390/414/768px: карточки шаблонов помещаются без горизонтального скролла страницы; кнопки доступны.
5. `/admin/documents → Плейсхолдеры` на мобиле: таблица скроллится горизонтально внутри своего контейнера, кнопки разметки/удаления физически доступны (можно доскроллить и нажать).
6. Десктоп `/admin/documents` визуально не деградирует; превью-панель справа на `lg+` остаётся 2-колонной.

### Файлы под изменение

- `src/components/ai-documents/StrictDocumentTemplatesManager.tsx` (замена блока списка шаблонов на аккордеон).
- `src/components/ai-documents/PlaceholdersCatalogTab.tsx` (только обёртка скролла + `min-w-0` на фильтрах).