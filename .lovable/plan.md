## да, согласен, с учетом правок:

&nbsp;

1. **Критерий “есть текущее основание” нужно ужесточить.**
  Недостаточно проверки subscriptions_v2 или entitlements по факту наличия записи.
  есть_текущее_основание ставить только если одновременно подтверждены:
  &nbsp;
  - продукт/пакет реально относится к текущей модели доступа,
  - право подтверждается действующей подпиской/тарифом,
  - право подтверждается текущими правилами доступа/настройками продукта,
  - срок действия не истёк.
    Старый entitlement сам по себе не считается доказательством текущего основания.
  &nbsp;
2. **Не использовать products_[v2.is](http://v2.is)_active как единственный критерий “актуальности”.**
  Это слишком грубо и не покрывает ваш реальный регламент.
  В плане явно зафиксировать:
  &nbsp;
  - главный источник — текущие правила доступа в системе,
  - если продукт формально активен, но по текущему регламенту больше не должен выдавать доступы, он должен идти в категорию **“исторический/не выдающий текущий доступ”**,
  - если спецправило iBizness -> Ценный бухгалтер 1 поток реально существует в настройках/связях системы, учитывать его,
  - если такого правила в данных/конфиге нет — не додумывать, а отправлять в требуется_ручная_проверка.
  &nbsp;
3. **Добавь отдельную категорию в аудит:**
  &nbsp;
  - есть_техническая_запись_доступа_но_нет_текущего_основания
    Это критично, потому что именно такие случаи сейчас засоряют вкладку «Доступы» и дают ложное ощущение, что доступ должен работать.
  &nbsp;
4. **Фазу А перепиши точнее по результатам CSV.**
  Сейчас в плане написано, что есть_текущее_основание = “есть активная подписка или entitlement”.
  Это нужно заменить на:
  &nbsp;
  - “есть активная подписка/тариф **и** это подтверждается текущими правилами доступа системы”;
  - entitlement использовать только как одну из технических записей для сверки, а не как самостоятельное бизнес-основание.
  &nbsp;
5. **CSV 10_доступы_для_вкладки.csv нужно пересчитать по новым правилам.**
  Колонка можно_показывать_во_вкладке_доступы должна определяться не только по:
  &nbsp;
  - status in ('active','trial')
  - access_end_at > now()
    но и по:
  - подтверждённому текущему основанию доступа,
  - актуальности продукта по действующему регламенту,
  - отсутствию признака “исторический доступ без текущего основания”.
  &nbsp;
6. **Фаза В (“Доступы” в карточке контакта) должна опираться не только на subscriptions.**
  Нужен единый вычисляемый фильтр:
  &nbsp;
  - показывать только текущие активные доступы,
  - скрывать записи, которые технически active, но не подтверждены текущим основанием,
  - архивные/исторические — только под toggle.
    Иначе UI будет красивый, но логически неверный.
  &nbsp;
7. **Фаза Б3 (“создание/обновление записей доступа”) — добавить явный STOP-guard:**
  Запрещено создавать запись доступа, если кейс попал в одну из категорий:
  &nbsp;
  - историческая_покупка_срок_истёк,
  - исторический_продукт_без_текущего_права,
  - есть_техническая_запись_но_нет_текущего_основания,
  - основание_не_доказано.
  &nbsp;
8. **Фаза Б6 (“очистка просроченных записей доступа”) расширить.**
  Переводить в архив/expired нужно не только явно просроченные active entitlements, но и записи, которые:
  &nbsp;
  - формально active,
  - но не подтверждаются текущим основанием доступа по вашим правилам.
    Только после proof и отдельного dry-run.
  &nbsp;
9. **Фаза Г (“массовое продление доступа”) уточнить по предмету продления.**
  Продлевать можно не просто “сделку”, а только текущий подтверждённый доступ.
  Если сделка историческая, а активного валидного доступа нет — это не продление, а потенциальное незаконное восстановление доступа, значит должно быть заблокировано.
10. **В preview массового продления добавить отдельные причины блокировки:**

&nbsp;

&nbsp;

&nbsp;

- историческая покупка без текущего основания,
- продукт не участвует в текущей модели доступа,
- спецправило доступа не найдено в конфигурации,
- есть техническая запись, но нет подтверждённого основания.

&nbsp;

&nbsp;

&nbsp;

11. **В итоговом отчёте раздели “активные доступы” и “технически активные записи”.**
  Это две разные сущности.
  Иначе снова получится путаница между:

&nbsp;

&nbsp;

&nbsp;

- тем, что реально должно быть видно пользователю,
- и тем, что просто лежит в БД.

&nbsp;

&nbsp;

&nbsp;

12. **По формулировкам.**
  Везде заменить упрощённые формулировки вида:

&nbsp;

&nbsp;

&nbsp;

- “активный продукт = можно выдавать доступ”
- “есть entitlement = есть основание”
  на точные:
- “доступ подтверждён действующим правилом доступа системы”,
- “техническая запись доступа проверена на соответствие текущему регламенту”.

&nbsp;

&nbsp;

&nbsp;

13. **Добавь отдельный DoD для Phase A v2.**
  План считается корректным только если в артефактах отдельно посчитаны:

&nbsp;

&nbsp;

&nbsp;

- текущие валидные доступы,
- исторические покупки,
- технически активные, но невалидные записи,
- случаи по спецправилу iBizness -> ЦБ 1 поток,
- случаи, где правило не найдено и нужен manual review.

&nbsp;

&nbsp;

&nbsp;

14. **Последовательность этапов уточнить так:**
15. Пересчёт и переproof Phase A по строгим правилам текущего регламента
16. UI-фильтр вкладки «Доступы» на основе нового proof
17. Переписывание массового продления через безопасный flow
18. Только потом dry-run подпатчей по данным
19. Execute — отдельно, по каждому согласованному подпатчу

&nbsp;

&nbsp;

Главная недоработка текущего плана: он всё ещё местами подменяет ваш регламент упрощённой технической логикой. Это нужно убрать полностью.

&nbsp;

Обновлённый план: Сверка доступов и массовое продление

### ГЛАВНОЕ ПРАВИЛО

Источник истины — только действующая конфигурация системы: `products_v2`, `tariffs`, `subscriptions_v2`, `entitlements`, `access_rules` и существующие связи продуктов/тренингов/Telegram. Новая бизнес-логика не создаётся. Историческая покупка без текущего основания не даёт права на создание доступа.

---

### Часть 1 — Что реально выполнено и доказано

#### Фаза А: Read-only аудит (ВЫПОЛНЕНА)

**Batch ID:** `20260405_150724_33b048e2`

**Пути к CSV** (все в `/mnt/documents/audit_v2_20260405_150724_33b048e2/`):


| #   | Файл                                         | Строк (без заголовка) | Описание                                                        |
| --- | -------------------------------------------- | --------------------- | --------------------------------------------------------------- |
| 1   | `01_сводка.csv`                              | 9                     | Итоговые метрики                                                |
| 2   | `02_сделки_без_user_id.csv`                  | 217                   | Сделки без привязки к пользователю                              |
| 3   | `03_сделки_без_product_id.csv`               | 18                    | Сделки без привязки к продукту                                  |
| 4   | `04_отсутствующие_записи_доступа.csv`        | 140                   | Оплаченные сделки без entitlement/подписки                      |
| 5   | `05_просроченные_активные_подписки.csv`      | 5                     | Подписки со статусом active, но истёкшим сроком                 |
| 6   | `06_требуется_ручная_проверка.csv`           | 470                   | Неоднозначные случаи                                            |
| 7   | `07_защищенный_пропуск.csv`                  | 39                    | Staff/protected                                                 |
| 8   | `08_архивные_продукты.csv`                   | 0                     | Сделки по неактивным продуктам (пусто — 0 неактивных продуктов) |
| 9   | `09_исторические_без_текущего_основания.csv` | 301                   | Оплаченные, но срок истёк, нет текущего доступа                 |
| 10  | `10_доступы_для_вкладки.csv`                 | 862                   | Все записи доступа для отображения                              |


**Подтверждённые метрики:**

- Всего сделок: 2 708
- Без user_id: 452
- Без product_id: 18
- Активных подписок: 336
- Просроченных «активных»: 5
- Всего entitlements: 555
- Активных продуктов: 24, неактивных: 0

**Классификация в CSV (колонки):**

- `есть_текущее_основание` — есть активная подписка или entitlement
- `основание_историческое_и_истекло` — оплачено, но срок доступа в прошлом
- `основание_не_доказано` — нет ни подписки, ни entitlement
- `архивный_или_неактуальный_продукт` — продукт `is_active = false` (0 случаев)
- `защищенный_случай` — staff/protected пропуски

#### UI-патчи (ВЫПОЛНЕНЫ, требуют проверки)

- `ContactDetailSheet.tsx`: добавлен `showFinishedSubs` toggle, фильтрация на `active`/`trial` + не истёкший срок
- `UserSubscriptions.tsx`: аналогичная фильтрация
- `BulkExtendAccessDialog.tsx`: создан с preview и STOP-guards
- `BulkActionsBar.tsx`: добавлена кнопка «Продлить доступ»

---

### Часть 2 — Что заблокировано и ждёт dry-run/согласования

#### Фаза Б: Исправления данных — НЕ ВЫПОЛНЯТЬ

Все подпатчи ниже требуют отдельного dry-run и согласования:

**Б1. Восстановление user_id** (217 сделок без user_id)

- Dry-run: SELECT-запрос, показывающий `order_id → profile_id → user_id` для каждой строки
- Критерий: только однозначное соответствие через `profiles`
- Запись в `audit_logs` обязательна
- DoD: CSV «до/после» с proof по каждой строке

**Б2. Маппинг product_id** (18 сделок без product_id)

- Dry-run: SELECT через цепочку `payment → tariff → product`
- Только high-confidence (одна цепочка, один продукт)
- DoD: CSV «до/после»

**Б3. Перевод просроченных подписок в expired** (5 записей)

- Dry-run: SELECT конкретных 5 ID с текущим статусом и сроком
- Условие: `status = 'active'` И `access_end_at < now()`
- DoD: SQL before/after, запись в audit_logs

**Б4. Создание/обновление записей доступа** — ЗАБЛОКИРОВАНО

- Создавать запись доступа можно только если на текущую дату доказано действующее основание:
  - активный продукт (`is_active = true`)
  - действующий тариф/подписка
  - действующее правило доступа в `access_rules`
- Историческая покупка без текущего основания → `требуется_ручная_проверка`
- Архивный продукт → доступ не создаётся
- Proof-блок обязателен: для каждой строки указать конкретное основание

---

### Фаза В: Очистка вкладки «Доступы» (UI-патч)

**Текущее состояние:** Фильтрация по `status` и `access_end_at` уже реализована. Нет проверки `products_v2.is_active`.

**Требуемые доработки:**

1. В `ContactDetailSheet.tsx` — добавить в фильтр `activeSubscriptions` проверку: если связанный продукт `is_active = false`, не показывать в основном списке
2. Badge на вкладке «Доступы» уже считает только `activeSubscriptions.length` — корректно
3. Toggle «Показать завершённые» уже реализован — корректно

**DoD Фазы В:**

- Основной список: только `status in ('active', 'trial')` + срок не истёк + продукт активен
- Завершённые скрыты по умолчанию
- Toggle открывает архив
- Badge считает только текущие активные

---

### Фаза Г: Массовое продление доступа (переписать)

**Текущая проблема:** `BulkExtendAccessDialog` делает прямой `supabase.from("subscriptions_v2").update({ access_end_at })` — это обходит существующий безопасный flow.

**Исправление:** Заменить прямой update на вызов Edge-функции `grant-access-for-order` (уже используется в `GrantAccessFromDealDialog.tsx`):

```text
Было:
  supabase.from("subscriptions_v2").update({ access_end_at: row.newEnd })

Станет:
  supabase.functions.invoke("grant-access-for-order", {
    body: {
      orderId: row.orderId,
      customAccessDays: days,
      extendFromCurrent: true
    }
  })
```

**STOP-guards (уже реализованы, остаются):**

- Нет `user_id` → `заблокировано`
- Нет `product_id` → `заблокировано`
- Продукт `is_active = false` → `заблокировано`
- Сделка не оплачена → `пропустить`
- Нет текущего активного доступа → `заблокировано`
- Новый срок короче текущего → `заблокировано`

**Дополнительные guards:**

- Добавить проверку: если продукт архивный (`is_active = false`) → `заблокировано` (уже есть)
- Добавить запись в `audit_logs` после каждого batch

**DoD Фазы Г:**

- Прямой update `subscriptions_v2` убран
- Продление через `grant-access-for-order`
- Preview обязателен
- Все guards работают
- Audit log записывается

---

### Порядок выполнения

```text
1. Фаза В — UI-патч вкладки «Доступы» (добавить проверку is_active)
2. Фаза Г — переписать BulkExtendAccessDialog (убрать прямой update)
3. Фаза Б — только после отдельных dry-run по каждому подпатчу
```

### Критерии завершения (общие)

- Нет ни одной записи доступа, созданной без доказанного основания
- Вкладка «Доступы» показывает только текущие активные доступы
- Исторические покупки не превращаются в активные доступы
- Массовое продление работает через безопасный flow, не через прямой update
- Все изменения данных проходят через dry-run и согласование