да, согласен, с учетом правок:

1. **Backlog-файл — это минимальное изменение в репозитории, но не код**
  &nbsp;
  В DoD уточнить:
  Единственное допустимое изменение в репозитории — создание markdown-файла `.lovable/backlog/document_package_ideology_readonly_preview.md`. Код компонентов, БД, RLS, edge functions, types, storage не менять.
2. **Proof по** `/admin/documents` **не должен выполнять мутационные действия**
  &nbsp;
  Формулировку скорректировать:
  Проверить, что кнопки upload / markup / activate / delete видны и доступны по UI, но не выполнять фактическое удаление, активацию или загрузку тестового файла без отдельного разрешения.
  Иначе proof может случайно изменить данные.
3. **Markup/preview проверять read-only**
  &nbsp;
  Уточнить:
  Открыть существующий шаблон и проверить открытие preview/markup без сохранения изменений. Не нажимать действия, которые меняют шаблон, версию, разметку или статус.
4. **Mobile proof: tooltip на disabled-кнопке**
  &nbsp;
  Если tooltip технически не открывается на disabled button, разрешить proof через видимый helper-text рядом/под кнопкой:
  Если tooltip на disabled-кнопке не срабатывает из-за HTML disabled behavior, допустим proof видимой подписью «Генерация пакета будет подключена во второй фазе».
5. **Добавить console/network proof**
  &nbsp;
  В финальный отчет добавить:
  - ошибок в консоли нет;
  - сетевых 4xx/5xx на проверяемых страницах нет;
  - если есть pre-existing warning — явно указать, что он не связан с этим proof.
6. **Отдельно подтвердить отсутствие horizontal scroll всей страницы**
  &nbsp;
  В mobile proof добавить:
  - `document.body.scrollWidth <= window.innerWidth` или визуальный DevTools-proof;
  - допустим внутренний scroll только в локальных блоках выбора/таблиц, если он предусмотрен UI.
7. **Формат финального отчета**
  &nbsp;
  Сохранить требование:
  Сообщение должно начинаться строго с `Отчёт о выполненной работе: Пакет «Идеология» — proof фазы 1`
  Без блока `План:` в том же сообщении.

Остальное корректно: это безопасный follow-up proof без изменения функционального scope и без перехода к фазе 2.

&nbsp;

План: Follow-up proofs к фазе 1 пакета «Идеология»

План должен быть составлен на русском языке.
Отчёт о выполненной работе должен быть составлен на русском языке.
Вся переписка, пояснения, diff-summary, proof и результаты — только на русском языке.

## Контекст

Фаза 1 пакета «Идеология» условно закрыта. Замечания ревью:

1. Нет визуального proof, что `/admin/documents → Шаблоны документов` не задет (upload/markup/activate/delete видны).
2. Нет mobile proof для `/document-generation → Документы → Идеология` (390/414 px).
3. Нужно зафиксировать в backlog решение про read-only preview шаблонов для пользователя.
4. На будущее — не смешивать в одном сообщении «План:» и «Отчёт о выполнении:».

Кода/БД/edge/RLS трогать НЕ нужно. Это чисто верификация + одна заметка в backlog.

## Шаги

### 1. Regression proof `/admin/documents`

- Зайти под админом в preview (пароль среды Preview — `123456`, форма «Login as Developer»).
- Открыть `/admin/documents → Шаблоны документов` (`StrictDocumentTemplatesManager` без `readOnly`).
- Сделать скриншот списка шаблонов с видимыми кнопками: «Загрузить .docx», «Удалить», «Активировать версию», «Разметить», правый preview-пейн.
- Открыть один существующий шаблон → проверить, что markup/preview открывается без ошибок в консоли и без сетевых 4xx/5xx.
- Снять скрин открытого markup/preview.

### 2. Mobile proof `/document-generation → Документы → Идеология`

- Переключить viewport на 390×844, затем на 414×896.
- Залогиниться как обычный пользователь (если нужен — `123456`).
- Перейти `/document-generation → Документы → Идеология`.
- Снять скрины:
  - блок «Состав пакета» (read-only список или пустое состояние);
  - блок «Анкета» — мультиселекты «Юрлица/ИП» и «Физлица» открываются, элементы кликабельны;
  - блок «Сформировать пакет» — кнопка disabled, tooltip про фазу 2;
  - проверить отсутствие горизонтального скролла страницы.

### 3. Backlog-заметка

Создать файл `.lovable/backlog/document_package_ideology_readonly_preview.md` с одним вопросом для follow-up:

- решить, нужен ли пользователю read-only preview шаблонов в составе пакета (сейчас в `readOnly` скрыт весь правый preview-пейн вместе с мутациями); если нужен — выделить «view-only preview» отдельно от «mutation actions» в `StrictDocumentTemplatesManager`.

### 4. Финальный отчёт

Сообщение оформить строго как «Отчёт о выполненной работе: Пакет «Идеология» — proof фазы 1» (без смешения с «План:»), приложить скриншоты и краткие DoD-пункты.

## DoD

- Скрины `/admin/documents`: upload/markup/activate/delete видны, markup открывается без ошибок.
- Скрины mobile 390 и 414: анкета и состав пакета корректны, нет горизонтального скролла, кнопка генерации disabled.
- Создан `.lovable/backlog/document_package_ideology_readonly_preview.md`.
- Никаких изменений в коде компонентов, БД, RLS, edge functions, types, storage.
- Сообщение начинается с «Отчёт о выполненной работе:».

## Что НЕ делаем

- Не меняем `StrictDocumentTemplatesManager`, `DocumentPackageIdeologyView`, `AiPageContent`.
- Не создаём таблиц/edge/RLS.
- Не начинаем фазу 2 (категория в админке, серверная анкета, генерация пакета) — отдельным планом после approve этих proof.