# Payment Incident Discovery — Read-only

**Date:** 2026-07-26 (UTC)
**Reporter scope:** read-only investigation, no mutations, no provider API calls, no code changes.
**Customer:** Краковская Елена — `princessa_elena1@mail.ru`
**Provider subscription under review:** bePaid `sbs_2044b74f2b2841d0`
**Product:** «Подоходный налог ИП» (`products_v2.id = de36a695-6b66-4547-bdb8-e64aa85eeabc`)

---

## 1. Identity

| Field | Value |
|---|---|
| `profiles.id` | `a8508c1a-bf19-49f4-bb7f-38831a09a318` |
| `auth.users.id` | `f278876e-12c9-4a3f-af13-79aca7979859` |
| email | `princessa_elena1@mail.ru` |

## 2. Product / Tariff / Offers snapshot

Product: `de36a695-6b66-4547-bdb8-e64aa85eeabc` «Подоходный налог ИП».
Tariff: `0fb3db55-b6ba-44bf-8a0b-37bb040ab01a` «стандарт», `access_days = 30`.

Active offers on this tariff (as of read):

| offer_id | offer_type | payment_method | is_installment | installment_count | amount (BYN) | is_primary | button_label |
|---|---|---|---|---|---|---|---|
| `5dfc9ca5-…f91cc` | pay_now | full_payment | false | — | 350.00 | **true** | «Оплатить полную стоимость» |
| `7e9187ea-…e8ae` | pay_now | **internal_installment** | **false** | **2** | **390.00** | false | «Оплатить в рассрочку» |

`tariff_offers.meta.installment` on the second offer: `{ max_months: 2, interval_days: 30, first_payment_delay_days: 0, rounding_mode: "round_half_up_byn" }`. Acquiring pinned to bePaid (`allowed_payment_providers=["bepaid"]`, `customer_choice_enabled=false`). No `meta.recurring` / `meta.billing_mode` on either offer.

**Observation (offer-level inconsistency):** the installment offer declares `payment_method = internal_installment` and `installment_count = 2` yet stores `amount = 390.00` (i.e. total, not per-payment). The active installment payment link for the same offer (`payment_links.ded92390…`, `payment_type = subscription`, created 2026-07-17 — after the incident) uses `amount = 195.00` per payment. Two artifacts referencing the same offer disagree on whether `amount` is per-cycle or total.

## 3. Originating checkout (sbs_2044b74f2b2841d0)

Initial `orders_v2` row: `7b90ffc3-69ee-4cb4-b9d7-ca0a00b6b4d0`
- `status = paid`, `final_price = 390.00`, `currency = BYN`, `created_at = 2026-06-25 22:21:47Z`.
- `meta.source = "bepaid-create-subscription-checkout"`
- `meta.payment_flow = "provider_managed_checkout"`
- `meta.bepaid_uid = aff39f12-549c-4904-a9f9-6ab103101254`
- `meta.deal_month = "2026-06"`
- `meta.offer_id / meta.payment_link_id / meta.payment_link_token / meta.checkout_context / meta.pricing_snapshot / meta.installment / meta.button_id` — **all null** on the order row.

No `payment_links` row was used for this order (offer→link linkage is only visible through `subscriptions_v2.meta.offer_id`, see §4). The only installment `payment_links` row for this offer (`ded92390…`, subscription-type, `amount=19500` kopecks) was created on **2026-07-17**, i.e. after the checkout, and is currently `invalidated`.

## 4. Subscription snapshot

`subscriptions_v2.id = 8cf18c5d-0d88-49fd-9ed6-86171ec84e6c`

`subscriptions_v2.meta` (verbatim keys):
```
{
  "offer_id":            "7e9187ea-9b7d-48ed-b6d4-9ebf6284e8ae",  ← installment offer
  "tariff_name":         "стандарт",
  "billing_mode":        "monthly",
  "product_name":        "Подоходный налог ИП",
  "display_title":       "Подоходный налог ИП — стандарт",
  "interval_days":       30,
  "checkout_order_id":   "7b90ffc3-69ee-4cb4-b9d7-ca0a00b6b4d0",
  "bepaid_activated_at": "2026-07-25T22:30:10.816Z",
  "display_description": "Подписка. Автосписание каждые месяц. Можно отменить в любой момент.",
  "bepaid_subscription_id":       "sbs_2044b74f2b2841d0",
  "bepaid_subscription_created_at": "2026-06-25T22:18:08.433Z",
  "pending_provider_managed":     true
}
```

`provider_subscriptions` row `447c5842-d614-4d98-a1ba-4e5c5c77197e`:
- `state = active`
- `amount_cents = 39000` (= 390 BYN)
- `next_charge_at = 2026-08-24 22:21:44Z`
- `raw_data.plan = { infinite: true, amount: 39000, interval: 30, interval_unit: "day", … }` — **no `billing_cycles`**, i.e. bePaid was told to create an unbounded plan.

There is **no** `installment_payments` row for this user (0 rows). The internal-installment engine was never engaged for this subscription.

## 5. Payments on this subscription

| payment_id | order_id | amount | status | provider | created_at |
|---|---|---|---|---|---|
| `e37a3022-3366-45b3-b9a0-60504dbc22ee` | `7b90ffc3-…b4d0` (initial) | 390.00 BYN | succeeded | bepaid | **2026-06-25 22:21:49Z** |
| `e4718c57-21e9-4ef1-83a8-ae776bfea64d` | `6db26cde-…a10f409` (rebill) | 390.00 BYN | succeeded | bepaid | **2026-07-25 22:30:11Z** |

Rebill order `6db26cde-4683-4a86-b860-f9496a10f409`:
- `meta.source = "bepaid_rebill"`, `meta.payment_flow = "bepaid_subscription_charge"`, `meta.parent_order_id = 7b90ffc3-…b4d0`.

The customer has many earlier 250 BYN charges (throughout 2025-12 … 2026-06) tied to completely different orders/products — unrelated to `sbs_2044b74f2b2841d0` and out of scope of this incident.

## 6. Access / entitlements

Entitlement `f00344f1-5b75-4b28-abbb-6f650b016d92`:
- `product_id = de36a695-…`, `status = active`
- `created_at = 2026-06-25 22:21:47Z`
- `expires_at = 2026-08-25 20:59:59Z` (extended by the 2026-07-25 rebill via `granted_by = primary_order_fulfillment`)
- `order_id` currently pinned to the latest fulfilling order `6db26cde-…`.

Access is currently live and paid through 2026-08-25.

## 7. Reminder / notification outcomes

Not retrievable in this read pass: `order_notification_deliveries`, `provider_events`, and `audit_logs` do not expose the column names probed (`event_type`, `order_id`, `metadata`) in this schema — safe read attempts returned column-not-found. No mutation attempted. Recommend a follow-up read once the correct column names are confirmed; do not infer delivery state from silence.

## 8. What created `sbs_2044b74f2b2841d0` and why 390 BYN was charged

Reconstructed from stored snapshots only:

1. On **2026-06-25 22:18:08Z** the writer `bepaid-create-subscription-checkout` (see `orders_v2.meta.source`) created a bePaid subscription with plan `{ amount: 39000 kopecks, interval: 30 day, infinite: true }`. `provider_subscriptions.raw_data.plan.infinite = true`, no `billing_cycles`.
2. The offer selected by the customer, as recorded in `subscriptions_v2.meta.offer_id`, is `7e9187ea-…e8ae` — the **«Оплатить в рассрочку»** button (installment, `installment_count = 2`, `meta.installment.max_months = 2`).
3. Initial charge succeeded on 2026-06-25 22:21:49Z at 390.00 BYN; rebill charge succeeded exactly one 30-day cycle later, 2026-07-25 22:30:11Z, at 390.00 BYN. `next_charge_at = 2026-08-24`.

**Intended vs. materialized:**
- Customer intent (button pressed): installment «Оплатить в рассрочку», 2 cycles.
- Offer configuration is internally inconsistent — the offer stores `amount = 390.00` alongside `installment_count = 2`, while the later payment_link for the same offer stores per-cycle `195.00`. Whether 390 was meant as total (→ 195×2) or per-cycle (→ 390×2) cannot be resolved from stored data alone.
- What was materialized in bePaid: an **infinite recurring subscription** at 390 BYN / 30 days (not a finite `billing_cycles=2` plan). This does not match either reading of the installment offer, and it does not match the memory rule `installment-public-link-finite-subscription` which requires `plan.infinite=false, plan.billing_cycles=N` for installment flows.

**Classification:** originating checkout is **provider-managed recurring subscription** (as recorded), not a one-time payment and not a finite 2-payment installment. The 2026-07-25 22:30 charge is a regular subscription rebill, not an installment second payment.

## 9. Uncertainty (explicit)

- Cannot confirm from stored data whether the writer intended `plan.infinite=false, plan.billing_cycles=2` and fell back to infinite due to a code branch, or whether the entry point for this offer bypassed the finite-subscription path entirely.
- Cannot confirm which UI surface produced the click (no `checkout_context`, `payment_link_id`, `payment_link_token`, or `button_id` persisted on the order).
- Notification delivery outcomes not extracted (see §7).
- The 350 BYN «полная стоимость» offer and the 195 BYN installment `payment_links` snapshot both post-date the incident and cannot be used to reverse-engineer what the customer saw on 2026-06-25.

No secrets, card PAN, CVV, or tokenized PM references are reproduced in this report. No provider API calls were made.
