# v22.7 — Readiness / Cutover Sprint — RESULTS

## Статус

```
SPRINT = v22.7
TYPE = readiness-only (no code, no schema, no UI)
PREDECESSOR = v22.6 (implementation-complete, cutover-blocked)
EXECUTED = 2026-03-30
```

---

## 1. SQL Invariant Checks — RESULTS

All checks executed 2026-03-30T11:27–11:32Z.

| # | Check | Expected | Baseline | Post-runtime | Status |
|---|-------|----------|----------|-------------|--------|
| S1 | Total ledger rows | ≥5 | 248 | 248 | ✅ PASS |
| S2 | Parent pair violations | 0 | 0 | 0 | ✅ PASS |
| S3 | Batch target on non-batch_start | 0 | 0 | 0 | ✅ PASS |
| S4 | telegram_access target | 0 | 0 | 0 | ✅ PASS |
| S5 | Bare subscription target | 0 | 0 | 0 | ✅ PASS |
| S6 | Completed non-batch | 0 | 0 | 0 | ✅ PASS |
| S7 | Orphan children | 0 | 0 | 0 | ✅ PASS |
| S8 | Missing subject (chk_has_subject) | 0 | 0 | 0 | ✅ PASS |
| S9 | Action/status mismatch | 0 | 0 | 0 | ✅ PASS |
| S10 | Source event key duplicates | 0 | 0 | 0 | ✅ PASS |
| S11 | Missing post_check | analysis | 1 (revoke — by design) | 1 | ✅ PASS (expected) |
| S12 | cutover_at NOT SET | 0 rows | 0 rows | 0 rows | ✅ PASS |
| S13 | schema_ready_at | exists | **0 rows** | **0 rows** | ⚠️ MISSING |

### Action/status distribution

| action_type | status | count |
|---|---|---|
| extend | extended | 2 |
| grant | granted | 2 |
| revoke | revoked | 1 |
| skip | skipped | 243 |

### Source event type distribution

| source_event_type | count |
|---|---|
| cron | 244 |
| webhook | 4 |

**S1–S12 = ALL PASS. S13 = watermark missing (prerequisite, not blocker for runtime checks).**

---

## 2. Runtime Smoke — RESULTS

### Группа A: Verified in v22.6 (not re-tested)

| # | Path | Result | Ledger row |
|---|------|--------|-----------|
| R1 | grant-access-for-order | ✅ PASS | b604ba7c |
| R2 | subscriptions-reconcile (cancel_at) | ✅ PASS | 9578416b |

### Группа B: subscription-charge renew

| # | Path | Result | Detail |
|---|------|--------|--------|
| R3 | subscription-charge (renew/extend) | ⏸ BLOCKED_SAFE_GUARD | Invoked 2026-03-30, total=0 candidates. No eligible subs (all past-due are expired/canceled). No sandbox billing. Formal blocker for cutover. |

### Группа C: Deferred paths

| # | Path | Invoked? | Result | Detail |
|---|------|----------|--------|--------|
| R4 | subscriptions-reconcile (trial) | ✅ yes | ⏸ DEFERRED_NON_BLOCKING | trials_expired=1, but cancel_at IS NULL → no ledger row by design. Code exercised, no revoke condition met. |
| R5 | subscriptions-reconcile (expired access) | ✅ yes | ⏸ DEFERRED_NON_BLOCKING | access_expired=0 — no candidates in DB. |
| R6 | subscriptions-reconcile (tg sync) | ✅ yes | ⏸ DEFERRED_NON_BLOCKING | telegram_synced=0 — no candidates. |
| R7 | telegram-revoke-access | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Requires admin context + real user. No safe trigger. |
| R8 | telegram-check-expired | ✅ yes | ⏸ DEFERRED_NON_BLOCKING | 0 active records in telegram_access table. processed=0. |
| R9 | telegram-kick-violators | ✅ yes (via cron log) | ⏸ DEFERRED_NON_BLOCKING | violators_kicked=0 — no candidates. |
| R10 | cancel-trial | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Only 1 trial sub, would affect real user + call GetCourse API. |
| R11 | subscription-charge (trial_ended) | ❌ no | ⏸ DEFERRED_NON_BLOCKING | No eligible trial with charge attempt. |
| R12 | subscription-admin-actions:cancel | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Requires admin JWT. Created smoke fixture but cannot invoke without auth. |
| R13 | subscription-admin-actions:revoke | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Same — requires admin JWT. |
| R14 | subscription-admin-actions:delete | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Same — requires admin JWT. |
| R15 | subscription-admin-actions:refund+revoke | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Same — requires admin JWT + real order. |
| R16 | bepaid-report-import (row-level + batch) | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Requires actual bepaid import data. |
| R17 | getcourse-import-deals file (row-level + batch) | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Requires actual file import. |
| R18 | getcourse-import-deals API (row-level + batch) | ❌ no | ⏸ DEFERRED_NON_BLOCKING | Requires actual API import. |

### Summary

- **PASS (v22.6 verified):** 2 (R1, R2)
- **BLOCKED_SAFE_GUARD:** 1 (R3)
- **DEFERRED_NON_BLOCKING:** 15 (R4–R18)
  - Code invoked but 0 candidates: 5 (R4, R5, R6, R8, R9)
  - No safe trigger available: 10 (R7, R10–R18)
- **FAIL:** 0

---

## 3. Lineage / Parent Checks — RESULTS

| # | Check | Result | Detail |
|---|-------|--------|--------|
| L1 | Orphan children = 0 | ✅ PASS | Verified via SQL S7 |
| L2 | Primary→downstream parent flow | N/A | No downstream rows with parent keys exist yet (only 5 access-affecting rows total) |
| L3 | Queue meta lineage | N/A | No queue items processed in this sprint |

---

## 4. Post-Runtime SQL — RESULTS

| # | Check | Result |
|---|-------|--------|
| P1 | Re-run S1–S12 | ALL PASS (same as baseline, 0 new rows) |
| P2 | New rows action/status valid | N/A (0 new rows) |
| P3 | Orphan children still 0 | ✅ PASS |
| P4 | Coverage by source_event_type | cron:244, webhook:4 |

---

## 5. Findings

### F1: schema_ready_at watermark missing

`phase1_ledger_schema_ready_at` is not set in `app_settings`. This watermark was supposed to be set during Phase 0 schema creation. 

**Classification:** prerequisite gap, NOT a runtime blocker. Can be set as part of cutover preparation.

### F2: No new ledger rows from runtime smoke

All invoked functions returned 200 but wrote 0 new ledger rows because:
- No eligible candidates for any revoke/grant path
- No active telegram_access records
- No renewable subscriptions

This is consistent with the production data state, not a code bug.

### F3: Admin-actions paths require JWT

subscription-admin-actions requires valid admin JWT, which is not available in automated smoke. These paths can only be verified via manual admin UI action or a future integration test framework.

---

## 6. FINAL DECISION BLOCK

```
v22.7 READINESS SPRINT STATUS = COMPLETE
SQL INVARIANT CHECKS = ALL_PASS (S1-S12), S13 MISSING (watermark)

RUNTIME SMOKE:
  R1 grant-access-for-order          = PASS (v22.6)
  R2 subscriptions-reconcile         = PASS (v22.6)
  R3 subscription-charge renew       = BLOCKED_SAFE_GUARD
  R4 reconcile (trial)               = DEFERRED_NON_BLOCKING (0 candidates)
  R5 reconcile (expired)             = DEFERRED_NON_BLOCKING (0 candidates)
  R6 reconcile (tg sync)             = DEFERRED_NON_BLOCKING (0 candidates)
  R7 telegram-revoke-access          = DEFERRED_NON_BLOCKING (no safe trigger)
  R8 telegram-check-expired          = DEFERRED_NON_BLOCKING (0 candidates)
  R9 telegram-kick-violators         = DEFERRED_NON_BLOCKING (0 candidates)
  R10 cancel-trial                   = DEFERRED_NON_BLOCKING (no safe trigger)
  R11 sub-charge (trial_ended)       = DEFERRED_NON_BLOCKING (no candidates)
  R12 admin-actions:cancel           = DEFERRED_NON_BLOCKING (requires admin JWT)
  R13 admin-actions:revoke           = DEFERRED_NON_BLOCKING (requires admin JWT)
  R14 admin-actions:delete           = DEFERRED_NON_BLOCKING (requires admin JWT)
  R15 admin-actions:refund+revoke    = DEFERRED_NON_BLOCKING (requires admin JWT)
  R16 bepaid-report-import           = DEFERRED_NON_BLOCKING (requires import data)
  R17 getcourse-import file          = DEFERRED_NON_BLOCKING (requires import data)
  R18 getcourse-import API           = DEFERRED_NON_BLOCKING (requires import data)

LINEAGE CHECKS = L1 PASS, L2-L3 N/A
POST-RUNTIME SQL = P1-P4 PASS

CUTOVER_ALLOWED = NO

BLOCKERS:
  1. R3 subscription-charge renew = BLOCKED_SAFE_GUARD (no sandbox billing)
  2. S13 phase1_ledger_schema_ready_at watermark not set

DEFERRED_NON_BLOCKING (15 paths):
  - 5 paths invoked, 0 candidates available in production
  - 10 paths not invocable (require admin JWT / real import data / would affect real users)
  - All 15 paths verified as wrapped_in_code + contract_verified in v22.6

NEXT STEPS:
  1. Set phase1_ledger_schema_ready_at watermark (prerequisite)
  2. Either:
     a) Provision sandbox billing → re-test R3 → if PASS → cutover
     b) Formally approve BLOCKED_SAFE_GUARD → proceed to cutover with documented risk
  3. For DEFERRED paths: verify organically as production events trigger them
     (each will write ledger rows that can be validated post-facto)

phase1_ledger_cutover_at = NOT SET
```

---

## 7. Proof Files Updated

| File | Changes |
|------|---------|
| p0_ledger_runtime_smoke_proof.txt | Added v22.7 results block |
| p0_ledger_path_coverage_proof.txt | Updated runtime_status for R4-R18 with v22.7 findings |
| p0_invariant_report.txt | Updated SQL check results, deferred list |

---

## 8. Smoke Fixture Lifecycle

| Action | Detail |
|--------|--------|
| Created | subscription a7b8c9d0 (smoke_fixture=v22.7, user 2ef54ad1) |
| Purpose | admin-actions cancel smoke |
| Result | Could not invoke (requires admin JWT) |
| Cleaned up | Deleted via migration (WHERE meta->>'smoke_fixture' = 'v22.7') |
| Post-cleanup | 0 remaining fixtures |
