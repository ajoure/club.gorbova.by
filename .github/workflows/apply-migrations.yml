name: Supabase Migration & Deploy

# ============================================================
# DISABLED (no-op stub) — Lovable-only Supabase Deploy Model
# ------------------------------------------------------------
# This project is on Lovable-managed Supabase (project_ref =
# hdjgkjceownmmnrqqtuz). The operator has no SUPABASE_ACCESS_TOKEN
# or SUPABASE_DB_PASSWORD for this project, and never will:
# Supabase is owned by Lovable Cloud, not by the operator.
#
# Canonical deploy path for edge functions / migrations:
#   Lovable Cloud agent-deploy (internal).
#
# This workflow stays in the repo only for history. Running it
# always fails immediately and does NOT touch any Supabase secret.
# Do not re-enable. See .lovable/architecture/canonical_infrastructure_v1.md
# ============================================================

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "::error::GitHub deploy disabled for Lovable-managed Supabase."
          echo "::error::Use Lovable Cloud deploy only."
          exit 1
