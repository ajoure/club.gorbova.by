# Read-only routing details and corrected candidate arithmetic

Source: aimsg_01m2d9xk5dfvm9r8jf5y4g18qm

Отчет о выполнении: только SELECT и чтение `crm-routing.ts`. Записей, изменений кода, деплоя и Publish нет. HEAD `363601dff31bd43a545c2c4846865572b0da3822`, очередь свободна.

## 1. Воронки в 318 `binding_pending_stage_ambiguous`
Задействована ровно одна воронка: **«ЦБ | 1 ступень |»** (`a0000001-0000-0000-0000-000000000002`), через 8 product-биндингов (по одному на продукт):
Модуль: Розничная торговля — 51; ПВТ — 44; Грузо- и пассажироперевозки — 41; Строительство — 39; Маркетплейсы — 38; Производство — 36; Общественное питание — 35; Учёт у ИП — 34. Итого 318.

Стадии воронки: open — «Новая» (0), «В работе» (1), «Счет для ЮЛ» (2), «Рассрочка» (3), «Заявка на кредит» (4); closed_won — «Успешно» (1 шт.); closed_lost — «Отказ» (1 шт.). `is_default=true` — **ни у одной** стадии.

Причина неоднозначности единственная и структурная: терминальные стадии определяются однозначно (1 won + 1 lost), а pending — нет: 5 open-стадий и 0 default, поэтому строгое правило fail-closed. Стадию по умолчанию не предлагаю — выбор принадлежит владельцу процесса.

## 2. 58 `ambiguous_tariff_offers` — дубли конфигурации или разные маршруты
Группировка по кортежу (pipeline_id, pending, success, failed) среди активных `pay_now` офферов с `crm_routing.enabled=true`:

| продукт | тариф | сделок | enabled-офферов | различных маршрутов |
|---|---|---|---|---|
| Ценный бухгалтер \| 1 ступень 2.0 | Бизнес-леди | 13 | 3 | 1 |
| Ценный бухгалтер \| 1 ступень 2.0 | Главный бухгалтер | 11 | 3 | 1 |
| …\| 20 поток | Бизнес-леди | 10 | 2 | 1 |
| …\| 20 поток | Главный бухгалтер | 6 | 2 | **2** |
| …\| 20 поток | Бизнес-леди доступ подарок | 6 | 2 | 1 |
| …\| 20 поток | Бизнес-леди ранее учились | 4 | 2 | 1 |
| Подоходный налог ИП | стандарт | 4 | 2 | 1 |
| Ценный бухгалтер \| 1 ступень 2.0 | Бухгалтер | 3 | 3 | 1 |
| …\| 20 поток | Бухгалтер | 1 | 2 | 1 |

Итог: **52 из 58** — чисто дублирующие эквивалентные конфигурации (маршрут один и тот же, резолвер блокируется только из-за количества кандидатов). **6 сделок** — действительно разные маршруты: одна воронка «ЦБ | 1 ступень |» и одинаковые success/failed («Успешно»/«Отказ»), но разные pending: «Рассрочка» (оффер «Оплатить в 2 платежа») против «Новая» (оффер «Оплатить 100% картой»). Это осмысленное различие по способу оплаты, а не дубль.

## 3. Арифметика — прежний расчёт был несогласован
Подтверждаю расхождение: 429 − 110 = 319 ≠ 318. Независимый пересчёт по уточнённому ключу идентичности (получатель, продукт, тариф, оффер, цена, валюта, payment_plan, pricing_stage, flow, trial, компания, явный deal_month, access_days, replacement, состав; автоматические `planned_access_*` исключены):

- активных pending всего — **512**;
- групп с дублями — **109**;
- строк в этих группах — **423**;
- канонических (последний валидный чекаут: max `created_at`, тай-брейк по id) — **109**;
- неканонических кандидатов — **314** (423 − 109 = 314, сходится).

## 4. Пригодность к обратимой архивации
Из 314 кандидатов зависимости есть у 113: `psub+sub` 72, `sub` 21, `pev` 11, `pay` 6, `psub` 1, `psub+sub+task` 1, `led+notif+pay+pev` 1. Кандидатов моложе 24 часов — 0.

**Пригодны ровно 201 строка** — то же число, что и раньше, несмотря на исправленную арифметику кандидатов.

## 5. JSON-ссылки — проверены, не только типизированные FK
Для всех 201 «чистых» кандидатов отдельно просканированы: `provider_events.payload` (234 строки), `payment_reconcile_queue.raw_payload`, `tracking_id`, `description` (2753 строки), `audit_logs` с `entity_type='orders_v2'` (244 строки). Совпадений — **0 / 0 / 0**. Типизированные FK проверены по всем 30 колонкам, ссылающимся на `orders_v2.id`, плюс `provider_events.related_order_id` и `processed_order_id`/`matched_order_id`.

## Оставшиеся неопределённости
1. Для «ЦБ | 1 ступень |» не задана pending-стадия по умолчанию — без решения владельца 318 сделок не маршрутизируются.
2. 52 сделки блокируются дублирующими эквивалентными офферами: нужно решение, деактивировать лишние офферы или ослабить правило «ровно 1 кандидат» при идентичном кортеже.
3. 6 сделок требуют выбора pending-стадии по способу оплаты — автоматическое разрешение недопустимо.
4. Данные не содержат явных признаков когорты/периода/состава, поэтому «одинаковость» 201 строки доказана только отсутствием различий, а не наличием подтверждающего признака.

Персональные данные, идентификаторы клиентов и заказов, токены и URL не выводились.

Codex decision: equivalent routing tuples can be deduplicated without deactivating offers. The unique open stage named Новая at order_index 0 is the normal initial default; explicit offer-specific installment stages must remain. Six legacy orders with equal terminal stages can be routed by their proved terminal state, with explicit repair provenance rather than inventing historical payment methods. Own cleanup dry-run remains authoritative.

