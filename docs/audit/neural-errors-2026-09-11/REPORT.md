# Аудит «Нейросеть» и авансового отчёта — 11.09.2026

Проверенный GitHub SHA: `fa27282b0e6fba27a4196dfbd1bc8cf9a0e69050`.
Ветка: `codex/neural-errors-audit`. Это аудит, исправления не применялись.

## Что установлено по обращению

Итог: **14 подтверждённых дефектов кода, 2 замечания по доступу с неизвестной
runtime-экспозицией**. Production read-only аудит завершён 11.09.2026 21:21 UTC.
Ошибки не исправлялись. Причины исторических июльских отказов найдены ниже;
это не доказательство причины обращения со скриншота.

На пользовательском скриншоте приведена жалоба от 27 августа: при формировании
авансового отчёта появился общий текст `Edge Function returned a non-2xx status code`.
Скриншот не содержит HTTP-статуса, имени вызова, request ID и тела ответа.
Конкретная первопричина этого обращения пока НЕ установлена.

В текущем интерфейсе `/ai` содержит чат и сценарии. Авансовый отчёт находится
в `/document-generation` и использует внешнюю анкету. Цепочка по коду:
`ExternalDocumentFormPage → external-document-form → ai-generate-document-package
→ canonical-document-generate-strict → canonical-document-send`.
Последний шаг отправляет документы, поэтому submit не использовался как smoke-тест.

Актуальный опубликованный интерфейс проверен во встроенном браузере Codex:
чат открывается, меню показывает анализ баланса, ответ на запрос МНС и определение
шифра ОС; пакет «Авансовый отчёт» открывается, форма создания ссылки и история
загружаются. История содержит старые успешные и неуспешные попытки; это не
глобальная статистика и не доказательство причины августовского обращения.
В аккаунте владельца показано «Остаток чата сегодня: −1 / −1».
Опубликованный SHA не установлен; версия GitHub не приравнивается к production.

## Подтверждённые дефекты кода

Приоритет P1 — возможные потери/дубли документов, ложный успех или нарушение
состояния; P2 — диагностика и отображение. Локальное воспроизведение означает
исполнение кода с синтетическими данными и mock-ответами, без production-записей.

| ID | Приоритет | Дефект, условие и результат | Доказательство |
| --- | --- | --- | --- |
| F01 | P1 | Серверные причины non-2xx теряются. Внешняя анкета берёт `error.message`, не читая `error.context`; обязательное поле, истёкший доступ и реальный сбой показываются одинаково. Та же проблема есть в legacy-генераторе, корпоративном генераторе, выдаче ссылок и hook генерации пакета. | `src/pages/ExternalDocumentFormPage.tsx:129–136`; `src/hooks/useAiDocuments.ts:99–118`; `src/hooks/useCorporatePackageGeneration.ts:52–59`; `src/hooks/useAiDocumentPackageGeneration.ts:87–107`; `ClientPackageUsage.tsx:196–205`. Корпоративный hook воспроизведён локально. |
| F02 | P1 | При HTTP 200 от оркестратора с failed/blocked и без документов внешняя форма сохраняет `status=failed`, но `error_code=null` и заполненное `generated_at`; пользователю возвращается лишь `generation_failed`. Подробные ошибки items существуют в ответе/истории batch, но теряются на уровне submission. | `supabase/functions/external-document-form/index.ts:522–528`. |
| F03 | P1 | Повтор submit после сетевого обрыва/сбоя создаёт новую submission и новую session. Нет ключа попытки или восстановления существующей. Если первая попытка уже сгенерировала/отправила документ, повтор способен дублировать документ и отправку. | `external-document-form/index.ts:424–437,518–569`; `ExternalDocumentFormPage.tsx:125–139`; схема `20260726135417_generic_external_document_forms.sql:93–110`. Повторные отправки в production НЕ проверялись. |
| F04 | P2 | Ошибка загрузки owner_forms подменяется утверждением «администратор ещё не включил внешнюю анкету»: `formsQuery.isError` не обрабатывается, `data ?? []` ведёт в ветку `!form`. | `src/components/ai-documents/packages/ClientPackageUsage.tsx:134–145,225–230`. |
| F05 | P2 | Контракт числа документов расходится: сервер отдаёт `total_items` и `total_documents`, frontend ждёт `total`. Частичный успех показывает неопределённое число, сводка теряет знаменатель. | `ai-generate-document-package/index.ts:953–964`; `useAiDocumentPackageGeneration.ts:33,98`; `PackageGenerationPanel.tsx:456`. |
| F06 | P1 | «Новый чат» доступен во время запроса; clearChat не отменяет/не обесценивает ответ. Завершение старого запроса возвращает старый conversation_id и вставляет ответ в новый чат. | `AiPageContent.tsx:711–725`; `useAiChat.ts:221–248,337–345`. Воспроизведено локально. |
| F07 | P2 | Ошибка запроса вставляется как обычное сообщение assistant и в следующем запросе уходит модели в истории. Технический отказ становится частью диалога модели. | `useAiChat.ts:190–193,249–257`. Воспроизведено локально. |
| F08 | P2 | Месячная квота начинает месяц в 21:00 UTC первого числа вместо 21:00 UTC последнего дня предыдущего месяца. Первые сутки Минска выпадают; первые три часа нового месяца Минска считаются по старому UTC-месяцу. | `_shared/ai-access.ts:269–289`. Воспроизведены оба случая. |
| F09 | P1 | Ошибка чтения счётчика превращается в нулевой расход, а не в ошибку проверки. При сбое БД квота фактически пропускает запрос. Аналогичный подход есть у поминутного счётчика. | `_shared/ai-access.ts:285–290,356–364`. Сбой месячного/суточного чтения воспроизведён локально. |
| F10 | P1 | Ошибки сохранения не препятствуют успешному ответу. Чат не проверяет обе insert; legacy-генератор логирует saveErr и возвращает success; корпоративный генератор увеличивает successCount даже без savedDoc; обновление финального batch пакета также не проверяется. История может не содержать «успешный» результат. | `gorbova-ai-chat/index.ts:716–739`; `ai-generate-document/index.ts:428–461`; `ai-generate-corporate-package/index.ts:674–719`; `ai-generate-document-package/index.ts:919–936`. |
| F11 | P1 | Корпоративный номер генерируется один раз для scalarData внутри документа и ещё раз для каждого файла/записи. Новый номер не подставляется в renderData. Номер внутри DOCX может отличаться от имени/карточки. | `ai-generate-corporate-package/index.ts:131,185,479,526–534,602,716`. Применимо к шаблонам с `document.number`. |
| F12 | P1 | Legacy-генератор выбирает номер из 1000 вариантов за день, использует его в единственном пути пользователя и загружает с `upsert:true`. Коллизия перезапишет предыдущий файл, а старая запись продолжит ссылаться на этот путь. | `ai-generate-document/index.ts:35–42,403–412`. Активность legacy-endpoint в production требует проверки. |
| F13 | P2 | Безлимит в чате отображается как `−1 / −1`; существующий formatQuotaSlot не используется этим компонентом. | `AiPageContent.tsx:821–822`; `useAiAccess.ts:33–40`. Подтверждено в опубликованном интерфейсе владельца. |
| F14 | P2 | Итоговый console log считает `errors_count` и `blocked_count` через `.length` у числовых счётчиков. При любом числе ошибок оба значения в логе равны 0; item-level `results_summary` отдельно сохраняет причины. | `ai-generate-document-package/index.ts:968–969`; кодовая проверка, production-частота не установлена. |

## Отдельные замечания по доступу — требуют production-проверки

- **S01, высокий приоритет:** legacy `ai-generate-document` читает entity, person,
  signer по переданному ID через service-role без проверки владельца этих объектов.
  RLS клиента не ограничивает такой серверный запрос. Кодовый пробел подтверждён
  (`index.ts:144–183`), но актуальная доступность endpoint и дополнительные runtime-
  ограничения не проверялись. Чужие ID и реальные данные не использовались.
- **S02, высокий приоритет:** `ai-generate-document-package:112–137` доверяет
  декодированному JWT `role=service_role` и маркеру заголовка без проверки подписи
  внутри функции. Безопасность этой ветки зависит от реального gateway verify_jwt.
  Явной секции этой функции в текущем config.toml нет; production-настройка неизвестна.
  Нельзя считать эксплуатацию подтверждённой или безопасной по одному комментарию.

## Проверки

- Статический gate: **362 функции, 224 буквальные ссылки, отсутствующих исходников 0**.
  Это не проверка deploy, boot и реального выполнения.
- `npx tsc --noEmit -p tsconfig.app.json`: **PASS**.
- Узкие существующие тесты AI-routing, corporate helpers, документных классификаторов,
  имён файлов, полей пакетов и нормализации ошибок: **126/126 PASS**.
- Локальные диагностические воспроизведения: **6/6 подтвердили описанные дефекты**.
  Эти тесты намеренно проверяют наблюдаемое ошибочное поведение; они НЕ означают,
  что функциональность исправна, и не входят в стандартный test include.
- При первом запуске диагностического конфига mergeConfig объединил include-массивы
  и запустил общую suite. **251 файл / 1684 assertion PASS, но exit 1** из-за
  необработанного `ReferenceError: window is not defined` из таймера `input-otp`
  после teardown `src/components/auth/InlineEmailOtpForm.test.tsx`.
  Конфиг аудита исправлен, узкое воспроизведение повторено отдельно.
  Общую suite нельзя объявлять зелёной; ошибка OTP вне текущего AI-scope.

Повторить только диагностические воспроизведения:

```sh
npm ci --ignore-scripts --no-audit --no-fund
npx vitest run --config docs/audit/neural-errors-2026-09-11/vitest.config.ts docs/audit/neural-errors-2026-09-11/repro.test.ts
```

## Production-аудит через Lovable

Пользователь разрешил read-only переход и поручил проверять занятую очередь каждые
10 минут с автоматическим продолжением. Правило внесено в AGENTS.md, PR #445
merged. После завершения и публикации предшествующих UI-задач и приёмки
серверного этапа «Бота для продаж» наш запрос отправлен **11.09.2026 21:07:56 UTC**
в Plan через канонический чат. Message ID: `main:user#00000000008173#usr:6LOXNP5U`.
Первый ответ получен 21:12:19 UTC (`aimsg_01m294rsxxfk2sk61mcf4vw5q9`).
В 21:14:16 UTC отправлена одна консолидированная ревизия
`main:user#00000000008175#usr:7WLLJTUQ`: уточнение фактических JWT/retention,
item-level ошибок, обязательных полей, шаблона и конвертера. Повторная отправка
этих запросов не требуется.

Перед отправкой обновлён origin/main: `81b3ea6bbdf8da731763217f4c880613354985bb`.
Функции и UI рассматриваемой AI/document-цепочки не изменялись относительно
первоначального аудита; это не доказательство совпадения deployed-версий.
Запрос включает агрегаты логов/ошибок, JWT, шаблон авансового отчёта и конвертер.
Generation/submit/send, записи, изменения файлов, deploy и Publish запрещены.

## Проверенные production-агрегаты и границы вывода

Источник — read-only ответы канонического Lovable, 11.09.2026 21:12–21:18 UTC,
с консолидированной ревизией Codex и финальной сверкой прежних замечаний
21:21:35 UTC: `main:agent#00000012967832#don:TGJWKRUU`, запрос
`main:user#00000000008178#usr:LINQTSHB`. Это чтение существующей истории, не smoke.

| Объект | Вся доступная история | Последние 14 дней по запросу Lovable |
| --- | --- | --- |
| Batches | generated 72, blocked 18, failed 7, partial 5, error 2; всего 104 | generated 9; остальные перечисленные статусы 0 |
| Внешние submissions | generated 39; failed 26: 21 без error_code и документов, 5 unauthorized | generated 9; failed 0 |
| Документы | generated 300, pending 5, success 5, deleted 1; всего 311 | Отдельный счётчик не установлен |

Все 5 pending-документов не имеют файла (последние даты июнь–июль).
Среди generated 20 содержат непустой missing_tokens. Это состояния записей;
они не доказывают причину зависания, неправильность каждого файла или F10.
Отсутствие новых failed-строк не доказывает отсутствие клиентских/gateway сбоев.
Различие сумм total_items/total_documents может быть нормальным при повторении
документов по ролям и разных версиях схемы. F05 подтверждён контрактом кода,
а не сравнением несопоставимых исторических сумм.

**У всех 31 blocked/error items причина сохранена в meta.results[].errors.**
У 7 из 8 error-items дополнительно заполнено details.error. Первоначальный
вывод Lovable «ошибок нет даже в items» отклонён и отозван после проверки
правильного поля. Потеря причины относится к summary submission/UI, а не ко
всей истории batch. В blocked-items 92 вхождения кодов, в error-items 8:

| Статус | Нормализованный код/причина | Вхождений |
| --- | --- | --- |
| blocked | invalid_token_in_package_template:tableTotal с format=words | 27 |
| blocked | invalid_token_in_package_template:tableTotal | 27 |
| blocked | system_field_resolver_not_implemented | 19 |
| blocked | invalid_token_in_package_template:tableRepeat | 9 |
| blocked | role_assignment_missing | 8 |
| blocked | invalid_token_in_package_template:package.role | 2 |
| error | pf_required_value_missing | 3 |
| error | isSuperAdmin is not defined | 3 |
| error | active_version_invalid | 1 |
| error | template_or_version_missing | 1 |

Это вхождения кодов, не число уникальных попыток. Все датированы не позднее
27.07.2026; за последние 14 дней таких items не найдено. Привязки к обращению
27.08 нет. Старая ошибка isSuperAdmin не объявляется текущим runtime-дефектом.

Активный пакет авансового отчёта содержит один обязательный item, режим single.
Шаблон активен, текущая версия от 28.07.2026; путь исходного шаблона указан
в metadata. Наличие пути не доказывает наличие/доступность байтов файла.
Table-repeat TR-000001 содержит 6 колонок. Повтор pf-000025 в колонках 0 и 5
соответствует дате документа приобретения; pf-000024 — дата оплаты в колонке 4.
Гипотеза о неправильном дублировании даты снята.

Каталог пакета: 20 полей, 19 активных, 15 обязательных. Активная версия содержит
28 токенов. ФИО подотчётного лица выводится через ln-000018 (full/signature_short),
который читает назначение роли и full_name соответствующей персоны. Поэтому
отсутствие literal pf-000014 в версии **не доказывает потерю ФИО**. Код внешней
анкеты содержит общий auto-binding через role.metadata.external_form_person_binding
(`external-document-form/index.ts:445–509`); прямая строка pf-000014 в коде для
такого механизма не требуется. Фактическая настройка этой связи в role metadata
в полученных результатах не подтверждена: остаётся UNKNOWN, не новый дефект.

Конвертер настроен и включён, его хост соответствует production allowlist.
В audit_logs зарегистрировано 371 событие document.pdf_converted (последнее
11.09 17:29) и 0 document.pdf_failed. Это исторические события всей системы,
а не текущая проверка доступности конвертера и не доказательство исхода
авансового отчёта пользователя.

Фактические deployed SHA/версии функций, verify_jwt и доступность legacy endpoint
остались UNKNOWN. Каталог функций и GitHub source не заменяют deployed metadata.
Последние записи deploy_logs датированы 06.06; это не означает, что с тех пор
не было деплоев. Из отсутствия секции config нельзя выводить effective JWT.

Инструмент логов вернул лишь недавнее окно около 10 минут. Причина ограничения
выборки и реальный retention не подтверждены. Статистика HTTP за 14 дней и
августовский инцидент по этому источнику не восстановлены. Время 09:49 на
скриншоте — время обращения с неизвестной таймзоной, а не точное время генерации.
Пустая история за 27.08 не локализует отказ. Root cause обращения остаётся UNKNOWN.

## Приоритет исправлений после аудита

1. **Доступ:** получить deployed metadata JWT и проверить S01/S02; убрать доверие
   непроверенному role внутри функции. Gateway-политику выбирать отдельно для
   публичной анкеты и внутренних функций, не устанавливать `verify_jwt=false`
   всей цепочке.
2. **Документы:** идемпотентный submit с восстановлением попытки; обработка ошибок
   сохранения; единый corporate-номер; уникальный legacy-путь без перезаписи.
   Старые записи не чистить автоматически.
3. **Диагностика:** безопасные code/stage/request ID от сервера до UI, сохранение
   item-level причин в submission, корректные счётчики логов и deployed SHA.
   Не журналировать содержимое документов, промпты, секреты и signed URL.
4. **Чат и квоты:** обесценивание позднего ответа при New Chat; техническая ошибка
   отдельно от истории модели; границы месяца Europe/Minsk; явный отказ при
   ошибке чтения квоты; корректное отображение безлимита и ошибок загрузки форм.
5. **Шаблон:** менять table-repeat только после сопоставления поля с семантикой
   колонок и незаполненным шаблоном. Повтор поля сам по себе не дефект.

Это план, а не выполненные исправления. Текущая авторизация — только аудит.

Аудит ограничен перечисленными компонентами и read-only UI. Полное отсутствие
остальных ошибок, успешная генерация, доставка, мобильная приёмка и исправление
августовского инцидента НЕ подтверждены. Production, данные и код приложения
не менялись; в PR только отчёт и opt-in воспроизведения.
