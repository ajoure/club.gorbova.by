# CRM runtime compatibility correction

PR #481 merged at `1afceb6b307fb624853eb72ddf76fa74e959e074`; main checks PASS (273 files / 1815 tests). Canonical Lovable applied the four migration files and deployed all 25 functions. No archive, routing backfill or Publish was executed.

Runtime rollback tests found `payment_status` missing `partially_refunded`. PostgreSQL resolves enum literals even when a matching row does not exist. This broke checkout claims and the new payment-restore trigger. Original tests incorrectly modeled payment status as text, hiding the incompatibility. Lovable containment removed only the new payment-restore trigger; existing payment writes work, but checkout/rollup RPCs remain blocked until the correction commits. Receipt: `main:agent#00000012983637#don:7NTM3BF6`.

Correction: one additive migration appends the status already recognized by all deployed read predicates and restores the exact temporarily removed trigger. It does not rewrite payment/order rows or change existing status meanings. No production writer is changed to produce the new value. Previously applied files and their hashes remain intact. The migration must COMMIT before invoking RPC tests because PostgreSQL disallows use of newly added enum values in the transaction that added them.

Tests now use the actual six-value production enum. A regression test first reproduces checkout and payment-insert failures, then applies the correction and verifies both succeed; every payment state is accepted, trigger restoration is idempotent. Existing claim, provider-switch, expiry, charge-hold, Stripe invoice, money-rollup, RR and archive/late-money tests pass with the enum fixture (12/12).

Required continuation: merge correction after checks, canonical Lovable managed apply only `20260913153500_crm_payment_status_compatibility.sql`, exact new merged SHA. No redeploy needed for unchanged functions; verify deployed code is identical to prior SHA. Verify enum/trigger/RPC rights, rollback-only runtime tests, no schema/data deltas beyond the additive repair. Revisit failed checkout/webhook logs within the deployment interval for any genuine events needing idempotent recovery; no real charge, cancellation or notification smoke. Then exact archive dry-run, Codex-reviewed batches, routing preview/apply, read-back, Publish and desktop/mobile UI acceptance. Goal remains active.
