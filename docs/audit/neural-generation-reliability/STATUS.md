# Нейросеть: до работающего авансового отчёта

Цель пользователя от 13.09.2026: закрыть весь аудит и проверить получение
корректного авансового отчёта. Диагностика ошибки не является этой приёмкой.

## Реестр

| Пункт | Состояние | Что требуется для закрытия |
| --- | --- | --- |
| F01/F02/F04/F05/F14 | Опубликованы PR476, ea81d44f; повторная регрессия в текущем scope | Проверить вместе с реальной генерацией |
| F03 | Подготовлены атомарная регистрация попытки, неизменный request_id, fingerprint, чтение статуса, восстановление после перезагрузки | Ревизия, миграция, deploy, параллельный submit/read-back, отсутствие повторной доставки |
| F06/F07 | Подготовлены изоляция ответов/загрузки чата и исключение технических ошибок из истории модели | CI, deploy/Publish, UI |
| F08/F09 | Подготовлены минские границы, точные счётчики и отказ при ошибке чтения | CI, deploy всех потребителей shared helper, read-back/runtime |
| F10 | Подготовлены проверяемые сохранения обмена чата, документа, batch/session | Проверка отказов сохранения и успешного runtime |
| F11/F12 | Подготовлены единый номер в render/snapshot/filename и UUID в неизменяемых storage paths (прежний короткий формат номера сохранён) | Проверить два файла, содержимое и номера |
| F13 | Подготовлен общий форматтер квот с безлимитом | ПК/mobile после Publish |
| S01 | Добавлены ограничения владельца для legacy и связанного corporate генератора | Негативные тесты, ревизия live ACL/deployed runtime |
| S02 | Удалено доверие декодированному role; нужен точный локальный service secret | Согласованность двух managed functions и положительный внутренний вызов, отказ подделке |
| Исходный отказ 27.08 | Причина не установлена | Корреляция доступной истории, действующая форма, воспроизводимая попытка и готовый файл |

Подготовленный PR: #479, ветка codex/neural-generation-reliability.

Ни один подготовленный пункт не объявлен опубликованным или закрытым.

## Очередь и продолжение

Lovable: канонический project `796a93b9-74cc-403c-8ec5-cafdb2a5beaa`, IAB tab3.
«Продукты 3» завершил нужную публикацию и освободил чат. Наша диагностика
принята 13.09 08:44:58 UTC (request 8424) и завершена. Подтверждены
активная форма, шаблон v10, 65 submissions: 39 generated, 21 failed без кода,
5 unauthorized. За 27.08 связанных сохранённых попыток не найдено;
конкретная причина остаётся UNKNOWN. Отсутствие записей не доказывает этап сбоя.
Ревизия точного diff завершена: миграция совместима. Два подтверждённых
блокера — незавершённая подготовка после claim и вызов sender без каналов —
исправлены локально. Замечание о trusted-вызове без владельца опровергнуто
текущим guard и исполняемым тестом: profile_not_found до batch/audit.
Привязка роли подтверждена в role.metadata: pf14 ФИО, pf15 должность,
pf16 подразделение. Старый вывод о её отсутствии отозван.
Heartbeat `lovable` ACTIVE, каждые 10 минут. Не дублировать.

Lovable Plan изменил только .lovable/plan.md вопреки read-only запросу;
в нашем PR восстановлено его прежнее содержимое ec3f8fe51. Сырой план
сохранён в локальном Temp отдельно, его неподтверждённые выводы не приняты.
Read-only DB/functions/logs только через канонический Lovable.

## Следующий запрос Lovable (только plan-only/read-only)

1. Проверить HEAD/чистоту и актуальную схему external submissions, ограничения,
   RLS/ACL и счётчики. Найти записи обращения 27.08 по доступным связям, без
   выгрузки персональных данных. Отсутствие логов не доказывает причину.
2. Проверить актуальную внешнюю форму авансового отчёта: активность, доступ
   владельца, привязку реквизитов, поля/табличные группы, LN18 и metadata
   external_form_person_binding, актуальную версию шаблона и конвертер.
3. Проверить deployed availability/auth/JWT legacy/package/external/strict;
   сверить внутренние вызовы после ротации ключей без вывода ключей/токенов.
4. Ревизия этого diff и миграции, включая rollout: новый UI сохраняет request_id; старые открытые страницы получают
   серверный ключ из семантики анкеты. Повтор с изменёнными attachment paths
   отклоняется как конфликт, а не запускается второй раз. Не отключать
   gateway-проверку и не ослаблять доступ ради прохождения теста.
5. Предложить контролируемый runtime-тест действующей анкеты с синтетическими
   реквизитами/расходами и отключённой email/Telegram-доставкой. Не создавать
   пользователей/CRM-контактов, не отправлять сообщения. Проверить, что этот
   тест использует те же field/role/template/renderer пути. Сначала план,
   точные ожидаемые записи и способы проверить/сохранить готовые DOCX/PDF.

## Границы

Изменение schema ещё не применено. Исторические 21 failed-строка с прежней
датой генерации не переписаны: любые исправления истории — по фактической
корреляции, dry-run и отдельным ожидаемым счётчикам внутри этой цели.
Ошибки прав/квот нельзя исправлять выдачей лишнего доступа или обходом Auth.
Реальная доставка клиентам не является тестом. Августовскую причину нельзя
объявлять найденной на основании синтетической проверки сегодняшнего кода.

## Локальная проверка подготовленного кода

- 1780 Vitest-тестов в 266 файлах PASS; в том числе исполняемые handlers
  внешней анкеты и legacy-генератора с синтетическими сервисами.
- 331 sales-тест PASS, TypeScript app PASS, build PASS, Edge contracts PASS
  (365 исходников, 225 вызовов).
- PGlite проверил уникальный индекс: два конкурентных claim → одна запись,
  второй получает 23505; старые строки не изменены.
- Повтор HTTP submit во время генерации и после окончания не вызывает
  повторный renderer/sender. Изменённый payload с тем же ID → 409.
- Чтение статуса после перезагрузки не запускает генерацию. При недоступном
  статусе форма не разрешает отправить повторно.
- Локальные мок-сервисы не доказывают работоспособность production-конвертера,
  реальных шаблонов, gateway или готовность авансового отчёта.

Предварительный deploy scope: external-document-form,
ai-generate-document-package, ai-generate-document,
ai-generate-corporate-package, gorbova-ai-chat, ai-access-status.
Связанный asset-classifier импортирует другие, неизменённые функции доступа;
его deployment не требуется только из-за новых helpers.
Миграция: 20260913082426_neural_submission_idempotency.sql.
Не merge/deploy до ревизии и CI; окончательный source SHA ещё не определён.

Дополнительно подтверждено чтением sender: при обоих отключённых каналах
он возвращает no_action_requested. В PR внешний handler пропускает sender,
сохраняет delivery_skipped=true и UI сообщает об отключённой доставке;
такой результат не выдаётся за подтверждённую отправку. Production ещё не изменён.

При отказе подготовки до вызова генератора сохраняются failed, стадия
preparation и safe_to_retry=true; только после подтверждённого сохранения
UI позволяет явно вернуться к анкете с новым request_id. Автоматических
повторов нет. При неизвестном исходе генерации или неподтверждённом save
новая попытка остаётся запрещена: время ожидания не доказывает отсутствие файла.
56 направленных тестов после ревизии PASS: три отказа INSERT до генерации,
прежний link-checkpoint failure, trusted-вызов без владельца, безопасный возврат,
нулевая доставка, коллизия короткого номера без перезаписи файла.
Согласованный deployment расширен на canonical-document-send и
canonical-document-generate-strict для синхронного обновления внутренней цепочки;
исходники этих двух функций не меняются.

## Приёмка backend 13.09.2026

PR479 merged: a6715ef4356ea600d34bb7e8c65da447dfda5124, 09:15:03 UTC.
Полный CI: 1790 тестов / 266 файлов, 331 sales, types/build/contracts PASS.
Read-only ревизия и сверка исправленных замечаний Lovable PASS.

Миграция применена managed-инструментом под версией 20260913091746;
20260913082426 отсутствует в migration history. Managed sync e1088297c
содержит фактически применённый SQL и обновлённые типы. Исходный,
никогда не применённый файл удаляется из GitHub, применённый файл неизменён.
Проверка PGlite воспроизвела отказ двух копий: request_id already exists.
Теперь тест выполняет все миграции этого контракта по порядку, чтобы такой
дубликат не проходил незамеченным. Никаких повторных SQL/изменений history.

Восемь функций развёрнуты из a6715ef4; свежая production-сборка в Lovable
и локально PASS. Негативные пробы: отсутствующая ссылка 404, неавторизованный
package, поддельный внутренний ключ/роль и strict без токена — 401.
65 прежних submissions до/после миграции совпали по контрольному отпечатку.

Контрольная фаза A через действующий внешний submit, без доставки:
- submission +1, session +1, batch +1, document +1, новый номер +1;
- 4 обычных значения, 2 строки расходов, 1 неактивная синтетическая персона
  и 1 назначение роли, 0 вложений, 2 файла;
- generated, error_code NULL, delivery_skipped=true, delivery_complete=false;
- документ 88d9ba97-7537-4800-b67b-82b989a4bdf7; DOCX 131197 байт, PDF 57379 байт;
- 12.34 + 87.66 = 100.00, сумма прописью и подотчётное лицо подставлены,
  missing_tokens пуст; точный визуальный контроль Codex ещё предстоит;
- одинаковый request_id и payload: replay, ноль новых записей/файлов/номеров;
  изменённый payload с тем же ID: 409; чтение статуса без изменений;
- писем/Telegram 0, пользователей/CRM-контактов/платежей/доступов не меняли.

Номер в БД 13.09/1, в заголовке 1309/1: это установленный режим
number_display_format=ddmm_slash_seq, введённый миграцией 20260728044639
и соответствующим кодом 28.07. Форматирование не является потерей номера
или случайным вторым номером F11. Проверка должна сравнивать значение с учётом
явной настройки; порядковый номер и дата те же. Бизнес-настройка не меняется.

Открыто: самостоятельный визуальный контроль обоих файлов, Publish,
контрольный submit через опубликованную анкету, ПК/mobile, отзыв тестовой ссылки.
Причина конкретного обращения 27.08 остаётся UNKNOWN, а не объявляется найденной
из результата сегодняшнего теста.
