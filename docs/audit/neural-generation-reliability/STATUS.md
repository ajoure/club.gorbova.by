# Нейросеть: до работающего авансового отчёта

Цель пользователя от 13.09.2026: закрыть весь аудит и проверить получение
корректного авансового отчёта. Диагностика ошибки не является этой приёмкой.

## Реестр

| Пункт | Состояние | Что требуется для закрытия |
| --- | --- | --- |
| F01/F02/F04/F05/F14 | Опубликованы PR476, ea81d44f; повторная регрессия в текущем scope | Проверить вместе с реальной генерацией |
| F03 | Подготовлены атомарная регистрация попытки, неизменный request_id, fingerprint, чтение статуса, восстановление после перезагрузки | Ревизия, миграция, deploy, параллельный submit/read-back, отсутствие повторной доставки |
| F06/F07 | Подготовлены изоляция ответов/загрузки чата и исключение технических ошибок из истории модели | CI, deploy/Publish, UI |
| F08/F09 | Подготовлены минские границы, точные счётчики и отказ при ошибке чтения | CI, deploy всех потребителей shared helper, read-back/runtime |
| F10 | Подготовлены проверяемые сохранения обмена чата, документа, batch/session | Проверка отказов сохранения и успешного runtime |
| F11/F12 | Подготовлены единый номер в render/snapshot/filename и UUID в неизменяемых storage paths | Проверить два файла, содержимое и номера |
| F13 | Подготовлен общий форматтер квот с безлимитом | ПК/mobile после Publish |
| S01 | Добавлены ограничения владельца для legacy и связанного corporate генератора | Негативные тесты, ревизия live ACL/deployed runtime |
| S02 | Удалено доверие декодированному role; нужен точный локальный service secret | Согласованность двух managed functions и положительный внутренний вызов, отказ подделке |
| Исходный отказ 27.08 | Причина не установлена | Корреляция доступной истории, действующая форма, воспроизводимая попытка и готовый файл |

Ни один подготовленный пункт не объявлен опубликованным или закрытым.

## Очередь и продолжение

Lovable: канонический project `796a93b9-74cc-403c-8ec5-cafdb2a5beaa`, IAB tab3.
На начало работы занят «Продукты 3». Последний просмотренный ответ
`main:agent#00000012980218#don:6TVXBCIF` относится к ревизии чужого scope;
completed не означает освобождение. Наш новый запрос ещё не отправлен.
Heartbeat `lovable` обновлён и ACTIVE, каждые 10 минут. Не дублировать.

После завершения чужого scope и его нужной публикации — немедленно продолжить
разрешённую plan-only диагностику. Read-only DB/functions/logs только через
канонический Lovable. Код и SQL создаёт Codex в этом worktree, один PR.

## Следующий запрос Lovable (только plan-only/read-only)

1. Проверить HEAD/чистоту и актуальную схему external submissions, ограничения,
   RLS/ACL и счётчики. Найти записи обращения 27.08 по доступным связям, без
   выгрузки персональных данных. Отсутствие логов не доказывает причину.
2. Проверить актуальную внешнюю форму авансового отчёта: активность, доступ
   владельца, привязку реквизитов, поля/табличные группы, LN18 и metadata
   external_form_person_binding, актуальную версию шаблона и конвертер.
3. Проверить deployed availability/auth/JWT legacy/package/external/strict;
   сверить внутренние вызовы после ротации ключей без вывода ключей/токенов.
4. Ревизия этого diff и миграции, включая rollout: новая функция требует
   request_id; старые открытые страницы должны обновиться. Не отключать
   gateway-проверку и не ослаблять доступ ради прохождения теста.
5. Предложить контролируемый runtime-тест действующей анкеты с синтетическими
   реквизитами/расходами и отключённой email/Telegram-доставкой. Не создавать
   пользователей/CRM-контактов, не отправлять сообщения. Проверить, что этот
   тест использует те же field/role/template/renderer пути. Сначала план,
   точные ожидаемые записи и способы проверить/сохранить готовые DOCX/PDF.

## Границы

Изменение schema ещё не применено. Исторические 21 failed-строка с прежней
датой генерации не переписаны: любые исправления истории — по фактической
корреляции, dry-run и отдельным ожидаемым счётчикам внутри этой цели.
Ошибки прав/квот нельзя исправлять выдачей лишнего доступа или обходом Auth.
Реальная доставка клиентам не является тестом. Августовскую причину нельзя
объявлять найденной на основании синтетической проверки сегодняшнего кода.

## Локальная проверка подготовленного кода

- 1780 Vitest-тестов в 266 файлах PASS; в том числе исполняемые handlers
  внешней анкеты и legacy-генератора с синтетическими сервисами.
- 331 sales-тест PASS, TypeScript app PASS, build PASS, Edge contracts PASS
  (365 исходников, 225 вызовов).
- PGlite проверил уникальный индекс: два конкурентных claim → одна запись,
  второй получает 23505; старые строки не изменены.
- Повтор HTTP submit во время генерации и после окончания не вызывает
  повторный renderer/sender. Изменённый payload с тем же ID → 409.
- Чтение статуса после перезагрузки не запускает генерацию. При недоступном
  статусе форма не разрешает отправить повторно.
- Локальные мок-сервисы не доказывают работоспособность production-конвертера,
  реальных шаблонов, gateway или готовность авансового отчёта.

Предварительный deploy scope: external-document-form,
ai-generate-document-package, ai-generate-document,
ai-generate-corporate-package, gorbova-ai-chat, ai-access-status.
Связанный asset-classifier импортирует другие, неизменённые функции доступа;
его deployment не требуется только из-за новых helpers.
Миграция: 20260913082426_neural_submission_idempotency.sql.
Не merge/deploy до ревизии и CI; окончательный source SHA ещё не определён.
